# Dummy example used for impact-based characteristic prompts.
# Keep it concise, realistic, and strictly following the expected format so the LLM mirrors it.
IMPACT_DUMMY_EXAMPLE = """## Sensitive Data Handling prevents exposure of secrets like credentials, API keys, and tokens.
### Issue: Hardcoded credentials are present in the code which can leak via VCS or logs.
### SDH-100
### Severity: Critical
```python
# Vulnerable (example)
USERNAME = "admin"
PASSWORD = "password123"
```
### Solution: Move secrets to environment variables or a secret manager and validate inputs.
```python
import os

username = os.getenv("APP_USERNAME")
password = os.getenv("APP_PASSWORD")

if not username or not password:
    raise ValueError("Missing required credentials in environment")
```
"""

maintainability = [
    {
        "characteristic": "Cyclomatic Complexity",
        "description": "",
        "abbreviation": "CYC-100",
        "example": '## Cyclomatic Complexity is a software metric used to measure the complexity of a program. A high cyclomatic complexity indicates a high number of paths through the code, making it more challenging to understand, test, and maintain. ### Issue: The function `process_data` has a high cyclomatic complexity due to multiple nested conditional statements and loops, making it difficult to maintain and test.\n\n ### CYC-100\n\n ### Severity: Medium\n\n\n```python\ndef process_data(data):\n    if data:\n        if isinstance(data, list):\n            for item in data:\n                if item > 0:\n                    print("Positive item")\n                else:\n                    print("Non-positive item")\n        elif isinstance(data, dict):\n            for key in data:\n                if key.startswith(\'a\'):\n                    print("Key starts with \'a\'")\n                else:\n                    print("Key does not start with \'a\'")\n        else:\n            print("Unknown data type")\n    else:\n        print("No data provided")\n```\n\n ### Solution: Refactor the function by breaking it into smaller, more focused functions to reduce cyclomatic complexity and improve maintainability.\n\n:\n```python\ndef process_list(data_list):\n    for item in data_list:\n        if item > 0:\n            print("Positive item")\n        else:\n            print("Non-positive item")\n\ndef process_dict(data_dict):\n    for key in data_dict:\n        if key.startswith(\'a\'):\n            print("Key starts with \'a\'")\n        else:\n            print("Key does not start with \'a\'")\n\ndef process_data(data):\n    if not data:\n        print("No data provided")\n        return\n\n    if isinstance(data, list):\n        process_list(data)\n    elif isinstance(data, dict):\n        process_dict(data)\n    else:\n        print("Unknown data type")\n```',
    },
    {
        "characteristic": "Modularity",
        "description": "",
        "abbreviation": "MOD-100",
        "example": "## Modularity refers to the practice of dividing a software system into distinct, independent modules that each handle a specific functionality. This allows for better maintainability, as modular code is easier to read, debug, and test. Additionally, changes to one module do not heavily impact other parts of the system, promoting flexibility and scalability.\n\n ### Issue: The given function handles too many responsibilities, violating the Single Responsibility Principle (SRP), which is a key aspect of modularity. The function performs data fetching, data validation, and data transformation all within the same block of code.\n\n ### MOD-100\n\n ### Severity: High\n\n\n```python\ndef process_data_and_save(url, filename):\n    # Fetch data from URL\n    response = requests.get(url)\n    data = response.json()\n\n    # Validate data\n    if not isinstance(data, list):\n        raise ValueError(\"Data should be a list\")\n\n    # Process data\n    processed_data = [item['value'] for item in data if 'value' in item]\n\n    # Save processed data\n    with open(filename, 'w') as file:\n        json.dump(processed_data, file)\n```\n\n ### Solution: Break down the function into smaller, modular functions where each function focuses on a single responsibility: fetching data, validating data, processing data, and saving data. This will improve readability, testability, and overall maintainability.\n\n:\n```python\ndef fetch_data(url):\n    response = requests.get(url)\n    return response.json()\n\ndef validate_data(data):\n    if not isinstance(data, list):\n        raise ValueError(\"Data should be a list\")\n    return data\n\ndef process_data(data):\n    return [item['value'] for item in data if 'value' in item]\n\ndef save_data(filename, data):\n    with open(filename, 'w') as file:\n        json.dump(data, file)\n\ndef process_data_and_save(url, filename):\n    data = fetch_data(url)\n    validated_data = validate_data(data)\n    processed_data = process_data(validated_data)\n    save_data(filename, processed_data)\n```\n",
    },
    {
        "characteristic": "Simplicity",
        "description": "",
        "abbreviation": "SIM-100",
        "example": "## Simplicity refers to the practice of writing clear, easy-to-understand code. Simple code is more maintainable, as it reduces complexity, makes debugging easier, and ensures that future developers can quickly understand and modify the software without confusion.\n\n ### Issue: The code contains an overly complex conditional statement, combining multiple conditions into one line, which reduces readability and increases the chances of introducing bugs.\n\n ### SIM-100\n\n ### Severity: Medium\n\n\n```python\nif user.is_authenticated() and (user.has_permission('admin') or user.has_permission('moderator')) and not user.is_suspended():\n    grant_access(user)\n```\n\n ### Solution: Split the complex conditional statement into multiple lines with meaningful variable names to improve readability and reduce cognitive load.\n\n :\n```python\nis_authenticated = user.is_authenticated()\nhas_required_permission = user.has_permission('admin') or user.has_permission('moderator')\nis_not_suspended = not user.is_suspended()\n\nif is_authenticated and has_required_permission and is_not_suspended:\n    grant_access(user)\n```\n",
    },
]

performance = [
    {
        "characteristic": "Algorithm Efficiency",
        "description": "",
        "abbreviation": "ALG-100",
        "example": '## Algorithm Efficiency refers to optimizing code to use minimal computational resources, such as time and memory, to accomplish tasks effectively. In web development, efficient algorithms contribute to faster load times, lower server loads, and an improved user experience.\n\n ### Issue: The code retrieves user data within a loop, causing multiple database queries (known as the N+1 query problem). This approach increases database load, slowing down the application. \n\n ### ALG-100\n\n ### Severity: High\n\n\n```python\ndef get_user_emails(user_ids):\n    emails = []\n    for user_id in user_ids:\n        user = db.query("SELECT email FROM users WHERE id = ?", user_id)\n        emails.append(user.email)\n    return emails\n```\n\n ### Solution: Optimize the code by fetching all user emails in a single database query, reducing the number of queries from N to 1. \n\n\n```python\ndef get_user_emails(user_ids):\n    users = db.query("SELECT email FROM users WHERE id IN ?", user_ids)\n    emails = [user.email for user in users]\n    return emails\n```',
    },
    {
        "characteristic": "Caching Mechanism",
        "description": "",
        "abbreviation": "CAC-100",
        "example": "## A caching mechanism stores frequently accessed data in temporary storage to reduce retrieval time, thereby significantly improving software performance by minimizing redundant computations or database queries. \n\n ### Issue: The web application retrieves user data from the database on every request without caching, leading to increased load and slower response times. \n\n ### CAC-100\n\n ### Severity: High\n\n\n```python\ndef fetch_user_data(user_id):\n    user = database.get_user(user_id)\n    return user\n```\n\n ### Solution: Implement a caching system to store user data after the first retrieval, which reduces the need for repeated database calls for the same data. \n\n\n```python\ncache = {}\n\n\ndef fetch_user_data(user_id):\n    if user_id in cache:\n        return cache[user_id]\n    else:\n        user = database.get_user(user_id)\n        cache[user_id] = user\n        return user\n```",
    },
    {
        "characteristic": "Concurrency and Parallelism",
        "description": "",
        "abbreviation": "CAP-100",
        "example": "## Concurrency and Parallelism involve executing multiple tasks simultaneously or in overlapping time periods, significantly improving software performance by making efficient use of resources and reducing execution time. \n\n ### Issue: The code makes multiple HTTP requests to different APIs sequentially, resulting in slow performance and inefficient utilization of network resources.\n\n ### CAP-100\n\n ### Severity: Medium\n\n\n```python\nimport requests\n\ndef fetch_data(url):\n    response = requests.get(url)\n    return response.json()\n\nurls = [\n    'https://api.example.com/data1',\n    'https://api.example.com/data2',\n    'https://api.example.com/data3'\n]\n\nresults = []\nfor url in urls:\n    data = fetch_data(url)\n    results.append(data)\n```\n\n ### Solution: Implement asynchronous I/O using the `aiohttp` library to perform HTTP requests concurrently, reducing total execution time by leveraging network I/O efficiently. \n\n\n```python\nimport asyncio\nimport aiohttp\n\nasync def fetch_data(session, url):\n    async with session.get(url) as response:\n        return await response.json()\n\nasync def main():\n    urls = [\n        'https://api.example.com/data1',\n        'https://api.example.com/data2',\n        'https://api.example.com/data3'\n    ]\n    async with aiohttp.ClientSession() as session:\n        tasks = [fetch_data(session, url) for url in urls]\n        results = await asyncio.gather(*tasks)\n    print(results)\n\nasyncio.run(main())\n```",
    },
    {
        "characteristic": "Data Structures",
        "description": "",
        "abbreviation": "DAT-100",
        "example": "## Data structures are methods of organizing and storing data in a computer so that it can be accessed and modified efficiently. Choosing the appropriate data structure is crucial for software performance, as it affects the speed and efficiency of data retrieval and manipulation. \n\n ### Issue: The code uses a list of dictionaries to store user data and performs a linear search to find a user by ID. This results in O(n) time complexity for each search, leading to performance degradation with large datasets. \n\n ### DAT-100\n\n ### Severity: High\n\n\n```python\nuser_data = [\n    {'id': 1, 'name': 'Alice'},\n    {'id': 2, 'name': 'Bob'},\n    # ... potentially thousands more entries\n]\n\ndef find_user(user_id):\n    for user in user_data:\n        if user['id'] == user_id:\n            return user\n    return None\n```\n\n ### Solution: Use a dictionary with user IDs as keys to store user data. This allows for O(1) average-case time complexity when retrieving users by ID. \n\n\n```python\nuser_data = {\n    1: {'id': 1, 'name': 'Alice'},\n    2: {'id': 2, 'name': 'Bob'},\n    # ... potentially thousands more entries\n}\n\ndef find_user(user_id):\n    return user_data.get(user_id)\n```",
    },
    {
        "characteristic": "Database Queries",
        "description": "",
        "abbreviation": "DBQ-100",
        "example": "## A database query is a request to retrieve or manipulate data stored in a database. Efficient database queries are essential for software performance because they directly affect the speed and scalability of applications, especially those dealing with large datasets. \n\n ### Issue:The code executes a database query inside a loop to fetch order details, leading to multiple database hits and causing significant performance degradation.\n\n ### DBQ-100\n\n ### Severity: High\n\n\n```python\n# Fetch order details for a list of order IDs\norder_ids = [101, 102, 103, 104, 105]\norder_details = []\nfor order_id in order_ids:\n    details = db.execute(\"SELECT * FROM orders WHERE id = ?\", (order_id,))\n    order_details.append(details)\n```\n\n ### Solution: Modify the code to fetch all required order details in a single query using SQL's IN clause, reducing the number of database calls and improving performance. \n\n\n```python\n# Fetch all order details in a single query\norder_ids = [101, 102, 103, 104, 105]\norder_details = db.execute(\"SELECT * FROM orders WHERE id IN ({seq})\".format(\n    seq=','.join(['?']*len(order_ids))), order_ids)\n```",
    },
    {
        "characteristic": "Function Calls",
        "description": "",
        "abbreviation": "FUN-100",
        "example": "## Function calls are invocations of predefined blocks of code that perform specific tasks. They are essential for code reusability and modularity. In terms of software performance, excessive or inefficient function calls, especially within loops or critical paths, can lead to significant overhead and slow down application execution. \n\n ### Issue: In a data analysis application for processing large datasets in the finance industry, a function that performs a heavy computation is called inside a loop without optimization, causing performance degradation due to redundant calculations.\n\n ### FUN-100\n\n ### Severity: High\n\n\n```python\ndef analyze_transactions(transactions):\n    results = []\n    for transaction in transactions:\n        risk_score = calculate_risk(transaction)\n        results.append(risk_score)\n    return results\n\n\ndef calculate_risk(transaction):\n    market_volatility = get_market_volatility()  # Expensive operation\n    return transaction.amount * market_volatility\n\n\ndef get_market_volatility():\n    # Fetching real-time market data\n    data = fetch_market_data()\n    volatility = compute_volatility(data)\n    return volatility\n```\n\n ### Solution: Optimize the code by minimizing unnecessary function calls within the loop. Cache the result of `get_market_volatility()` since market volatility does not change for each transaction in the current context. \n\n\n```python\ndef analyze_transactions(transactions):\n    results = []\n    market_volatility = get_market_volatility()  # Cached outside the loop\n    for transaction in transactions:\n        risk_score = calculate_risk(transaction, market_volatility)\n        results.append(risk_score)\n    return results\n\n\ndef calculate_risk(transaction, market_volatility):\n    return transaction.amount * market_volatility\n```",
    },
    {
        "characteristic": "Hardware Utilization",
        "description": "",
        "abbreviation": "HAR-100",
        "example": "## Hardware Utilization refers to the efficient use of hardware resources like CPU, memory, disk I/O, and network bandwidth by software applications. In web and app development, optimizing hardware utilization is crucial for enhancing performance, reducing latency, and improving the user experience. \n\n ### Issue: In a mobile app, images are being loaded and processed at their original high resolutions, leading to excessive memory usage and slow performance, especially on devices with limited resources. \n\n ### HAR-100\n\n ### Severity: High\n\n\n```python\ndef load_images(image_paths):\n    images = []\n    for path in image_paths:\n        image = load_image(path)  # Loads full-resolution image\n        images.append(image)\n    return images\n```\n\n ### Solution:  Implement image resizing or thumbnail generation to load images at resolutions appropriate for the display size. This reduces memory consumption and improves rendering speed, optimizing hardware utilization. \n\n\n```python\ndef load_images(image_paths):\n    images = []\n    for path in image_paths:\n        image = load_image(path)\n        resized_image = resize_image(image, target_size=(800, 600))  # Resize to appropriate resolution\n        images.append(resized_image)\n    return images\n```",
    },
    {
        "characteristic": "I/O Operations",
        "description": "",
        "abbreviation": "IOO-100",
        "example": "## I/O Operations refer to the processes by which a program reads data from or writes data to external sources like files, databases, or network services. Efficient handling of I/O operations is crucial for software performance because these operations are typically slower than in-memory computations. Inefficient I/O can lead to bottlenecks, increased latency, and reduced application throughput. \n\n ### Issue: In a web application developed for handling client data submissions, the server performs blocking file I/O operations within the request-handling function. This leads to poor performance and scalability issues, as each client request waits for the file operation to complete before the server can process the next request. \n\n ### IOO-100\n\n ### Severity: High\n\n\n```python\nfrom flask import Flask, request\n\napp = Flask(__name__)\n\n@app.route('/submit', methods=['POST'])\ndef submit_data():\n    data = request.form['data']\n    with open('data.txt', 'a') as file:\n        file.write(data)  # Blocking I/O operation\n    return 'Data submitted successfully'\n\nif __name__ == '__main__':\n    app.run()\n```\n\n ### Solution:  Implement asynchronous I/O operations or delegate file writing to a background thread or task queue. This approach prevents the main thread from blocking, allowing the server to handle multiple client requests concurrently and improving overall performance. \n\n\n```python\nfrom flask import Flask, request\nfrom threading import Thread\n\napp = Flask(__name__)\n\ndef write_to_file(data):\n    with open('data.txt', 'a') as file:\n        file.write(data)\n\n@app.route('/submit', methods=['POST'])\ndef submit_data():\n    data = request.form['data']\n    Thread(target=write_to_file, args=(data,)).start()  # Non-blocking I/O\n    return 'Data submitted successfully'\n\nif __name__ == '__main__':\n    app.run()\n```",
    },
    {
        "characteristic": "Memory Leaks",
        "description": "",
        "abbreviation": "MEM-100",
        "example": "## A memory leak occurs when an application allocates memory but fails to release it after it's no longer needed. In software performance, memory leaks can lead to increased memory usage over time, causing slowdowns, crashes, and inefficient resource utilization—critical issues in web and app development where resources are limited. \n\n ### Issue:  In a Flask web application, each client request opens a file but does not close it after processing. This results in open file descriptors accumulating over time, leading to a memory leak that degrades server performance and can cause the application to crash. \n\n ### MEM-100\n\n ### Severity: High\n\n\n```python\nfrom flask import Flask, request\n\napp = Flask(__name__)\n\n@app.route('/process')\ndef process_file():\n    file = open('data.txt', 'r')\n    data = file.read()  # Missing file.close()\n    return data\n```\n\n ### Solution:  Ensure that all opened files are properly closed after their use is complete. This can be achieved by using context managers (the `with` statement) that automatically handle the opening and closing of resources. \n\n\n```python\nfrom flask import Flask, request\n\napp = Flask(__name__)\n\n@app.route('/process')\ndef process_file():\n    with open('data.txt', 'r') as file:\n        data = file.read()  # File is automatically closed\n    return data\n```",
    },
    {
        "characteristic": "Network Calls",
        "description": "",
        "abbreviation": "NET-100",
        "example": "## Network Calls refer to the interactions where a program requests data or services from another system over a network. They are crucial in software performance because inefficient network calls can lead to high latency, increased load times, and a poor user experience. \n\n ### Issue:The code makes a separate network call for each stock symbol in the list, which can significantly slow down the application due to the overhead of multiple network requests. \n\n ### NET-100\n\n ### Severity: High\n\n\n```python\nimport requests\n\ndef fetch_stock_prices(symbols):\n    prices = {}\n    for symbol in symbols:\n        response = requests.get(f\"https://api.finance.com/price/{symbol}\")\n        if response.status_code == 200:\n            prices[symbol] = response.json()['price']\n    return prices\n```\n\n ### Solution: Modify the code to fetch all stock prices in a single network call by requesting multiple symbols at once. This reduces the number of network calls, decreasing latency and improving performance. \n\n```python\nimport requests\n\ndef fetch_stock_prices(symbols):\n    symbols_param = ','.join(symbols)\n    response = requests.get(f\"https://api.finance.com/prices\", params={'symbols': symbols_param})\n    if response.status_code == 200:\n        return response.json()['prices']\n    return {}\n```",
    },
    {
        "characteristic": "Redundant Code and Loops",
        "description": "",
        "abbreviation": "RCL-100",
        "example": "## Redundant code and loops refer to unnecessary repetitions in code, such as executing the same operations multiple times or including loops that perform redundant iterations. This can significantly degrade software performance by increasing execution time and resource consumption. \n\n ### Issue: In a data processing script for a financial application, a loop unnecessarily iterates over the same transaction dataset multiple times, causing performance issues. \n\n ### RCL-100\n\n ### Severity: High\n\n\n```python\n# Original code\ntransactions = [100, 200, -50, 400, -150]\ntotal_deposits = 0\ntotal_withdrawals = 0\n\nfor amount in transactions:\n    if amount > 0:\n        total_deposits += amount\n\nfor amount in transactions:\n    if amount < 0:\n        total_withdrawals += amount\n```\n\n ### Solution: Combine the loops to iterate over the dataset only once, calculating both deposits and withdrawals in a single pass to improve performance. \n\n```python\n# Improved code\ntransactions = [100, 200, -50, 400, -150]\ntotal_deposits = 0\ntotal_withdrawals = 0\n\nfor amount in transactions:\n    if amount > 0:\n        total_deposits += amount\n    elif amount < 0:\n        total_withdrawals += amount\n```",
    },
    {
        "characteristic": "Space Complexity",
        "description": "",
        "abbreviation": "SPA-100",
        "example": "## Space Complexity refers to the amount of memory used by an algorithm as a function of the input size. It is a critical factor in software performance, particularly when dealing with large data sets or systems with memory constraints, such as cloud-based applications, big data processing, or real-time systems. Efficient space management ensures scalability and reduces the risk of memory overflow or excessive swapping, which can severely degrade performance.  \n\n ### Issue: In a web application handling large data batches, the code reads and processes an entire dataset from the database into memory at once, leading to high space complexity and potential memory issues when dealing with very large datasets (e.g., in data analytics or machine learning pipelines). This leads to O(n) space complexity and can easily overwhelm memory for large datasets. \n\n ### SPA-100\n\n ### Severity: Critical\n\n```python\ndef fetch_and_process_all_users():\n    # Fetching all users from the database at once\n    users = db.session.query(User).all()  # Loads the entire dataset into memory\n    for user in users:\n        process_user(user)\n```\n\n ### Solution: The solution is to use a generator-based approach to fetch and process users in smaller batches. This reduces the space complexity to O(1), since the code now processes one batch at a time instead of holding all the data in memory. This is an industry-standard practice in fields like data processing, machine learning pipelines, and cloud services, where data sizes can be massive. \n\n```python\ndef fetch_and_process_users_in_batches(batch_size=100):\n    offset = 0\n    while True:\n        # Fetching users in batches to reduce memory usage\n        users = db.session.query(User).limit(batch_size).offset(offset).all()\n        if not users:\n            break\n        for user in users:\n            process_user(user)\n        offset += batch_size\n```",
    },
    {
        "characteristic": "Time Complexity",
        "description": "",
        "abbreviation": "TIM-100",
        "example": " ## Time Complexity refers to the amount of time an algorithm takes to run as a function of the input size. It is crucial in software performance as it determines how efficiently a program will scale when handling larger datasets or more complex computations. \n\n ### Issue:  In a web application handling search queries, the code iterates through all entries in a list for each search query, resulting in O(n^2) time complexity. This approach becomes inefficient when handling large data sets, as the performance degrades significantly, making real-time or near-real-time operations difficult. \n\n ### TIM-100\n\n ### Severity: Critical\n\n```python\ndef search_users(query, users):\n    results = []\n    for user in users:  # O(n)\n        if query in user.name:  # O(n) inside loop, making it O(n^2)\n            results.append(user)\n    return results\n```\n\n ### Solution: The issue can be resolved by using more efficient data structures or algorithms, such as indexing the users' names using a hash table or using a more efficient search algorithm like binary search, assuming the data is sorted. In this case, using Python's built-in filter or search functions can improve the time complexity to O(n). \n\n```python\ndef search_users(query, users):\n    # Using a list comprehension for linear search, keeping it O(n) but more efficient\n    return [user for user in users if query in user.name]\n```",
    },
]

reliability = [
    {
        "characteristic": "Error Handling and Exception Management",
        "description": "",
        "abbreviation": "ERR-100",
        "example": "## Error Handling and Exception Management are critical in software development as they ensure that a program can gracefully handle unexpected situations without crashing. This enhances software reliability by providing consistent performance and improving user experience even when errors occur.\n\n ### Issue: The API endpoint lacks proper exception handling for database operations, which may lead to server crashes if an exception occurs.\n\n ### ERR-100\n\n ### Severity: High\n\n\n```python\n@app.route('/api/data')\ndef get_data():\n    data = database.fetch_data()\n    return jsonify(data)\n```\n\n ### Solution: Implement try-except blocks to handle potential exceptions during database operations and return appropriate HTTP responses.\n\n :\n```python\n@app.route('/api/data')\ndef get_data():\n    try:\n        data = database.fetch_data()\n        return jsonify(data)\n    except DatabaseError as e:\n        return jsonify({'error': 'Database error occurred'}), 500\n    except Exception as e:\n        return jsonify({'error': 'An unexpected error occurred'}), 500\n```",
    },
    {
        "characteristic": "Robustness to External Changes",
        "description": "",
        "abbreviation": "ROB-100",
        "example": "## Robustness to External Changes refers to the ability of software to function correctly despite changes in external factors such as system environments, third-party services, or external libraries. This is significant in software reliability because it ensures the software remains stable and dependable even when external dependencies evolve.\n\n ### Issue: The code uses hard-coded database connection parameters, which can cause failures if the database credentials or host change.\n\n ### ROB-100\n\n ### Severity: High\n\n\n```python\ndef connect_to_database():\n    connection = Database.connect(\n        host='localhost',\n        user='admin',\n        password='password123'\n    )\n    return connection\n```\n\n ### Solution: Utilize configuration files or environment variables to manage database connection parameters, allowing the application to adapt to external changes without code modifications.\n\n :\n```python\nimport os\n\ndef connect_to_database():\n    connection = Database.connect(\n        host=os.environ.get('DB_HOST', 'localhost'),\n        user=os.environ.get('DB_USER', 'admin'),\n        password=os.environ.get('DB_PASSWORD', 'password123')\n    )\n    return connection\n```",
    },
    {
        "characteristic": "Consistency in Data and State Management",
        "description": "",
        "abbreviation": "CON-100",
        "example": '## Consistency in Data and State Management refers to maintaining uniform and accurate data across all parts of an application during its execution. This is significant for software reliability because inconsistent data can lead to unexpected behaviors, errors, and system crashes, undermining user trust and system integrity.\n\n ### Issue: The code modifies a shared variable `counter` across multiple threads without proper synchronization, leading to race conditions and inconsistent state.\n\n ### CON-100\n\n ### Severity: High\n\n\n```python\nimport threading\n\ncounter = 0\n\ndef increment():\n    global counter\n    for _ in range 1000():\n        counter += 1\n\nthreads = []\nfor _ in range(10):\n    t = threading.Thread(target=increment)\n    t.start()\n    threads.append(t)\n\nfor t in threads:\n    t.join()\n\nprint("Final counter value:", counter)\n```\n\n ### Solution: Use a threading lock to synchronize access to the shared variable, ensuring that only one thread modifies it at a time.\n\n :\n```python\nimport threading\n\ncounter = 0\nlock = threading.Lock()\n\ndef increment():\n    global counter\n    for _ in range(1000):\n        with lock:\n            counter += 1\n\nthreads = []\nfor _ in range(10):\n    t = threading.Thread(target=increment)\n    t.start()\n    threads.append(t)\n\nfor t in threads:\n    t.join()\n\nprint("Final counter value:", counter)\n```',
    },
    {
        "characteristic": "Resource Management",
        "description": "",
        "abbreviation": "RES-100",
        "example": "## Resource Management refers to the efficient and effective handling of system resources such as memory, files, and network connections during software execution. Proper resource management is crucial for software reliability because it prevents resource leaks, ensures optimal performance, and avoids system crashes or unexpected behavior.\n\n ### Issue: The code opens a file but does not close it after reading, leading to a potential file descriptor leak.\n\n ### RES-100\n\n ### Severity: High\n\n\n```python\ndef read_data(file_path):\n    file = open(file_path, 'r')\n    data = file.read()\n    return data\n```\n\n ### Solution: Use a context manager to automatically handle the opening and closing of the file.\n\n :\n```python\ndef read_data(file_path):\n    with open(file_path, 'r') as file:\n        data = file.read()\n    return data\n```",
    },
    {
        "characteristic": "Testing and Coverage",
        "description": "",
        "abbreviation": "TAC-100",
        "example": "## Testing and Coverage are essential for software reliability as they ensure that the code functions correctly and that all parts of the code are tested to identify potential bugs.\n\n ### Issue: The code lacks unit tests, resulting in low test coverage and potential undetected errors.\n\n ### TAC-100\n\n ### Severity: High\n\n\n```python\ndef calculate_discount(price, discount_rate):\n    discounted_price = price - (price * discount_rate)\n    return discounted_price\n```\n\n ### Solution: Implement unit tests to increase test coverage and verify the correctness of the `calculate_discount` function.\n\n :\n```python\nimport unittest\n\nclass TestCalculateDiscount(unittest.TestCase):\n    def test_standard_discount(self):\n        self.assertEqual(calculate_discount(100, 0.2), 80)\n\n    def test_zero_discount(self):\n        self.assertEqual(calculate_discount(100, 0), 100)\n\n    def test_full_discount(self):\n        self.assertEqual(calculate_discount(100, 1), 0)\n\nif __name__ == '__main__':\n    unittest.main()\n```",
    },
    {
        "characteristic": "Failover and Redundancy Mechanisms",
        "description": "",
        "abbreviation": "FAI-100",
        "example": "## Failover and Redundancy Mechanisms are methods that ensure a system remains operational even when some components fail. They are crucial for software reliability as they prevent downtime and maintain continuous service availability.\n\n ### Issue: The application lacks a failover mechanism for its database connection. If the primary database fails, the application cannot connect to a backup, leading to service disruption.\n\n ### FAI-100\n\n ### Severity: High\n\n\n```python\nimport sqlite3\n\ndef get_data(query):\n    conn = sqlite3.connect('primary.db')\n    cursor = conn.cursor()\n    cursor.execute(query)\n    result = cursor.fetchall()\n    conn.close()\n    return result\n```\n\n ### Solution: Implement a failover by attempting to connect to a secondary database if the primary connection fails.\n\n :\n```python\nimport sqlite3\n\ndef get_data(query):\n    try:\n        conn = sqlite3.connect('primary.db')\n    except sqlite3.OperationalError:\n        conn = sqlite3.connect('secondary.db')\n    cursor = conn.cursor()\n    cursor.execute(query)\n    result = cursor.fetchall()\n    conn.close()\n    return result\n```",
    },
    {
        "characteristic": "Boundary Conditions",
        "description": "",
        "abbreviation": "BOU-100",
        "example": "## Boundary conditions refer to the edge cases or extreme values at which an application, function, or API operates. In web development, ensuring proper handling of boundary conditions is essential for preventing security vulnerabilities, performance issues, and application crashes. Testing these conditions improves software reliability by ensuring that the system behaves predictably at limits such as maximum or minimum input lengths, large data payloads, or unexpected user behaviour.\n\n ### Issue: In the following web application code for handling user registration, the boundary condition for password length is not adequately checked. The current implementation does not enforce a maximum length, leading to potential performance issues, buffer overflows, or security vulnerabilities like denial of service (DoS) attacks with excessively large payloads.\n\n ### BOU-100\n\n ### Severity: High\n\n\n```python\n@app.route('/register', methods=['POST'])\ndef register_user():\n    data = request.json\n    username = data.get('username')\n    password = data.get('password')\n\n    if len(password) < 8:\n        return jsonify({'error': 'Password too short, must be at least 8 characters.'}), 400\n    \n    # Code continues to register the user\n```\n\n ### Solution: Implement both minimum and maximum boundaries for password length to prevent performance and security issues. Additionally, ensure other edge cases like empty strings or excessively large payloads are handled properly. This improves the reliability of the web application by enforcing sensible input constraints.\n\n :\n```python\n@app.route('/register', methods=['POST'])\ndef register_user():\n    data = request.json\n    username = data.get('username')\n    password = data.get('password')\n\n    # Check if password is None or an empty string\n    if not password:\n        return jsonify({'error': 'Password cannot be empty.'}), 400\n\n    # Check for minimum and maximum length boundary conditions\n    if len(password) < 8:\n        return jsonify({'error': 'Password too short, must be at least 8 characters.'}), 400\n    elif len(password) > 64:\n        return jsonify({'error': 'Password too long, must be less than 64 characters.'}), 400\n\n    # Code continues to register the user\n```",
    },
    {
        "characteristic": "Concurrency Control",
        "description": "",
        "abbreviation": "COC-100",
        "example": '## Concurrency Control is the management of simultaneous operations on shared resources to prevent conflicts and ensure data integrity. It is crucial for software reliability because improper handling can lead to race conditions, deadlocks, and inconsistent data, causing unpredictable behavior and system crashes.\n\n ### Issue: The code does not synchronize access to the shared variable `counter`, resulting in a race condition.\n\n ### COC-100\n\n ### Severity: High\n\n\n```python\nimport threading\n\ncounter = 0\n\ndef increment():\n    global counter\n    for _ in range(100000):\n        counter += 1\n\nthreads = []\nfor _ in range(5):\n    t = threading.Thread(target=increment)\n    threads.append(t)\n    t.start()\n\nfor t in threads:\n    t.join()\n\nprint("Final counter value:", counter)\n```\n\n ### Solution: Implement a `threading.Lock` to synchronize access to `counter`, ensuring that only one thread modifies it at a time.\n\n :\n```python\nimport threading\n\ncounter = 0\nlock = threading.Lock()\n\ndef increment():\n    global counter\n    for _ in range(100000):\n        with lock:\n            counter += 1\n\nthreads = []\nfor _ in range(5):\n    t = threading.Thread(target=increment)\n    threads.append(t)\n    t.start()\n\nfor t in threads:\n    t.join()\n\nprint("Final counter value:", counter)\n```',
    },
    {
        "characteristic": "Recovery and Error Reporting",
        "description": "",
        "abbreviation": "REC-100",
        "example": '## Recovery and Error Reporting refers to the software\'s ability to handle errors gracefully and provide meaningful feedback, ensuring the system remains stable and reliable. It is significant in software reliability because it prevents crashes, aids in troubleshooting, and enhances user experience by managing unexpected conditions effectively.\n\n ### Issue: The code does not handle exceptions during an API call, which can lead to unhandled exceptions and program crashes if the API fails or returns an error.\n\n ### REC-100\n\n ### Severity: High\n\n\n```python\ndef fetch_data(api_url):\n    response = requests.get(api_url)\n    return response.json()\n```\n\n ### Solution: Implement error handling to catch exceptions that may occur during the API call, such as connection errors or invalid responses, allowing the program to handle these situations without crashing.\n\n :\n```python\ndef fetch_data(api_url):\n    try:\n        response = requests.get(api_url, timeout=5)\n        response.raise_for_status()\n        return response.json()\n    except requests.exceptions.RequestException as e:\n        print(f"Error fetching data from {api_url}: {e}")\n        return None\n```',
    },
    {
        "characteristic": "Dependency Management",
        "description": "",
        "abbreviation": "DEP-100",
        "example": "## Dependency Management is the practice of managing external libraries and packages that a software project relies on. It is crucial for software reliability as it ensures consistent builds, prevents version conflicts, and reduces security risks associated with outdated or unmaintained dependencies.\n\n ### Issue: The code dynamically installs and imports a package at runtime, leading to unpredictable behavior and security vulnerabilities due to uncontrolled dependency versions.\n\n ### DEP-100\n\n ### Severity: High\n\n\n```python\nimport os\n\ndef fetch_data(url):\n    os.system('pip install requests')\n    import requests\n    response = requests.get(url)\n    return response.json()\n```\n\n ### Solution: Remove the dynamic installation and import of dependencies within the code. Instead, specify all dependencies in a `requirements.txt` file and install them prior to running the application to ensure a controlled and reproducible environment.\n\n :\n```python\nimport requests\n\ndef fetch_data(url):\n    response = requests.get(url)\n    return response.json()\n```\n\nAnd `requirements.txt`:\n```\nrequests==2.25.1\n```",
    },
    {
        "characteristic": "Analyzability",
        "description": "",
        "abbreviation": "ANA-100",
        "example": "## Analyzability is the ease with which software can be understood in terms of its structure, purpose, and functionality. It is significant in software reliability because high analyzability enables developers to identify defects, understand code behavior, and implement changes efficiently, leading to more stable and dependable software systems.\n\n ### Issue: The code contains complex and deeply nested conditional logic, uses non-descriptive function and variable names, and lacks comments or documentation. This complexity makes it difficult to understand and analyze the code, reducing its analyzability and increasing the risk of errors during maintenance.\n\n ### ANA-100\n\n ### Severity: High\n\n\n```python\ndef process(data):\n    result = []\n    for item in data:\n        if item[0] == 'A':\n            val = compute1(item)\n            result.append(val)\n        elif item[0] == 'B':\n            val = compute2(item)\n            result.append(val)\n        else:\n            if len(item) > 5:\n                val = compute3(item)\n                result.append(val)\n            else:\n                val = compute4(item)\n                result.append(val)\n    return result\n\ndef compute1(x):\n    return x[1]*2 + x[2]*3\n\ndef compute2(x):\n    return x[1]*3 - x[2]*2\n\ndef compute3(x):\n    return x[1]*x[2]\n\ndef compute4(x):\n    return x[1]+x[2]\n```\n\n ### Solution: Refactor the code to improve readability and analyzability by:\n\n- Using descriptive function and variable names that reflect their purpose.\n- Simplifying the nested conditional logic to make the control flow clearer.\n- Adding comments to explain the functionality of complex sections.\n\nThis will make the code easier to understand, maintain, and modify, thereby enhancing its reliability.\n\n :\n```python\ndef process_items(items):\n    results = []\n    for item in items:\n        # Process based on the first element indicating the type\n        if item[0] == 'A':\n            calculation_result = calculate_type_A(item)\n        elif item[0] == 'B':\n            calculation_result = calculate_type_B(item)\n        else:\n            calculation_result = calculate_based_on_length(item)\n        results.append(calculation_result)\n    return results\n\ndef calculate_type_A(item):\n    # Calculate for type A items\n    return item[1] * 2 + item[2] * 3\n\ndef calculate_type_B(item):\n    # Calculate for type B items\n    return item[1] * 3 - item[2] * 2\n\ndef calculate_based_on_length(item):\n    # Determine calculation based on item length\n    if len(item) > 5:\n        return item[1] * item[2]\n    else:\n        return item[1] + item[2]\n```",
    },
    {
        "characteristic": "Modifiability",
        "description": "",
        "abbreviation": "MOD-100",
        "example": "## Modifiability refers to the ease with which a software system can be changed to correct defects, improve functionality, or adapt to a new environment. It is significant in software reliability because a highly modifiable system can evolve over time without introducing new errors, ensuring consistent performance and reliability.\n\n ### Issue: The code contains hard-coded file paths, making it difficult to modify and adapt to different environments or directory structures.\n\n ### MOD-100\n\n ### Severity: High\n\n\n```python\ndef read_config():\n    with open('/usr/local/app/config/settings.conf', 'r') as file:\n        config = file.read()\n    return config\n```\n\n ### Solution: Use configuration files or environment variables to specify file paths, enhancing modifiability and portability.\n\n :\n```python\nimport os\n\ndef read_config():\n    config_path = os.getenv('CONFIG_PATH')\n    with open(config_path, 'r') as file:\n        config = file.read()\n    return config\n```",
    },
    {
        "characteristic": "Testability",
        "description": "",
        "abbreviation": "TES-100",
        "example": "## Testability refers to the ease with which software can be tested to ensure it functions correctly under various conditions. High testability allows for efficient detection and isolation of defects, significantly enhancing software reliability by facilitating thorough and effective testing processes.\n\n ### Issue: The `fetch_and_process_data` function directly calls an external API and processes its response inline without abstraction. This tight coupling makes it difficult to mock the API calls during unit testing, hindering testability and increasing the risk of unreliable software due to untested scenarios.\n\n ### TES-100\n\n ### Severity: High\n\n\n```python\nimport requests\n\ndef fetch_and_process_data(user_id):\n    response = requests.get(f'https://api.example.com/data/{user_id}')\n    if response.status_code == 200:\n        data = response.json()\n        # Process data directly\n        result = data_processing_function(data)\n        return result\n    else:\n        raise Exception('Failed to fetch data')\n```\n\n ### Solution: Refactor the function to separate external API interactions from data processing logic by introducing abstraction and dependency injection. This allows the API calls to be mocked during testing, enhancing testability and reliability by enabling isolated testing of both the API interaction and the data processing logic.\n\n :\n```python\ndef fetch_data(api_client, user_id):\n    response = api_client.get_data(user_id)\n    if response.status_code == 200:\n        return response.json()\n    else:\n        raise Exception('Failed to fetch data')\n\ndef fetch_and_process_data(api_client, user_id):\n    data = fetch_data(api_client, user_id)\n    # Process data separately\n    result = data_processing_function(data)\n    return result\n\n# Example usage:\n# api_client = ApiClient()\n# result = fetch_and_process_data(api_client, user_id)\n```",
    },
]

resilience = [
    {
        "characteristic": "Exception Handling",
        "description": "",
        "abbreviation": "EXC-100",
        "example": '## Exception Handling is a programming practice that allows developers to manage runtime errors gracefully, enhancing software resilience by preventing crashes and maintaining consistent application behavior.\n\n ### Issue: The code does not handle exceptions that may occur during a database transaction in a financial application, potentially leading to data inconsistency or system crashes.\n\n ### EXC-100\n\n ### Severity: Critical\n\n```python\ndef process_transaction(account_id, amount):\n    balance = get_account_balance(account_id)\n    new_balance = balance - amount\n    update_account_balance(account_id, new_balance)\n    log_transaction(account_id, amount)\n```\n\n ### Solution: Introduce exception handling to ensure any errors lead to a rollback, maintaining data integrity.\n\n```python\ndef process_transaction(account_id, amount):\n    try:\n        balance = get_account_balance(account_id)\n        new_balance = balance - amount\n        update_account_balance(account_id, new_balance)\n        log_transaction(account_id, amount)\n    except Exception as e:\n        rollback_transaction()\n        log_error(f"Transaction failed for account {account_id}: {e}")\n        raise\n```',
    },
    {
        "characteristic": "Retry Mechanism",
        "description": "",
        "abbreviation": "RET-100",
        "example": "## Retry Mechanism is a technique that involves automatically reattempting a failed operation to handle transient errors, thereby enhancing software resilience by ensuring continuity in the face of temporary issues.\n\n ### Issue: In an e-commerce application, the code lacks a retry mechanism when processing payment transactions via an external payment gateway, risking transaction failures due to momentary network glitches. \n\n ### RET-100\n\n ### Severity: High\n\n```python\ndef process_payment(order_id, payment_details):\n    response = payment_gateway.charge(payment_details)\n    if response.status == 'success':\n        update_order_status(order_id, 'paid')\n    else:\n        log_error(f\"Payment failed for order {order_id}: {response.error}\")\n```\n\n ### Solution: Introduce a retry mechanism to reattempt the payment transaction a specified number of times before failing.\n\n```python\ndef process_payment(order_id, payment_details, retries=3, delay=2):\n    for attempt in range(retries):\n        response = payment_gateway.charge(payment_details)\n        if response.status == 'success':\n            update_order_status(order_id, 'paid')\n            return True\n        else:\n            log_error(f\"Attempt {attempt + 1} failed for order {order_id}: {response.error}\")\n            time.sleep(delay)\n    log_error(f\"All attempts failed for order {order_id}. Payment could not be processed.\")\n    return False\n```",
    },
    {
        "characteristic": "Circuit Breaker Pattern",
        "description": "",
        "abbreviation": "CBP-100",
        "example": '## Circuit Breaker Pattern is a design strategy that prevents an application from repeatedly trying to perform an operation likely to fail, enhancing software resilience by allowing the system to maintain functionality during external service failures.\n\n ### Issue:  In a cloud-based file storage service, the code continually attempts to upload files to an unstable external storage API without a circuit breaker, potentially causing thread exhaustion and degraded system performance. \n\n ### CBP-100\n\n ### Severity: High\n\n```python\ndef upload_file(file_data):\n    response = external_storage_api.upload(file_data)\n    if response.status_code == 200:\n        return True\n    else:\n        log_error("File upload failed")\n        return False\n```\n\n ### Solution: Implement the Circuit Breaker Pattern to monitor the success and failure rates of the external API calls, temporarily halting requests when failures exceed a threshold.\n\n```python\ndef upload_file(file_data):\n    if circuit_breaker.is_open():\n        log_error("Circuit breaker open. Skipping file upload.")\n        return False\n    try:\n        response = external_storage_api.upload(file_data)\n        if response.status_code == 200:\n            circuit_breaker.reset()\n            return True\n        else:\n            circuit_breaker.record_failure()\n            log_error("File upload failed")\n            return False\n    except Exception as e:\n        circuit_breaker.record_failure()\n        log_error(f"Exception during file upload: {e}")\n        return False\n```',
    },
    {
        "characteristic": "Timeouts",
        "description": "",
        "abbreviation": "TIM-100",
        "example": '## Timeouts are predefined limits that terminate operations if they exceed a certain duration, preventing system resources from being tied up indefinitely. They are crucial for software resilience as they help maintain system responsiveness and prevent cascading failures in distributed systems.\n\n ### Issue: In a microservices-based healthcare application, the code makes external API calls to retrieve patient data without setting timeouts. This can lead to the application hanging if the external service is unresponsive, affecting the ability to provide timely patient care.  \n\n ### TIM-100\n\n ### Severity: High\n\n```python\ndef get_patient_data(patient_id):\n    response = requests.get(f"https://api.healthcare.com/patient/{patient_id}")\n    if response.status_code == 200:\n        return response.json()\n    else:\n        log_error(f"Failed to retrieve data for patient {patient_id}")\n        return None\n```\n\n ### Solution: Implement timeouts in the API calls to ensure the application doesn\'t hang indefinitely.\n\n```python\ndef get_patient_data(patient_id):\n    try:\n        response = requests.get(f"https://api.healthcare.com/patient/{patient_id}", timeout=5)\n        if response.status_code == 200:\n            return response.json()\n    except requests.Timeout:\n        log_error(f"Request timed out for patient {patient_id}")\n        return None\n    except requests.RequestException as e:\n        log_error(f"Request failed for patient {patient_id}: {e}")\n        return None\n```',
    },
    {
        "characteristic": "Fallback Strategies",
        "description": "",
        "abbreviation": "FAL-100",
        "example": '## Fallback Strategies are alternative methods or procedures that are activated when the primary system fails or is unavailable. They are significant in software resilience as they ensure continuity of service, minimizing disruption and maintaining user experience during failures.\n\n ### Issue: In a financial trading platform, the code relies solely on a single market data feed without a fallback strategy. If the primary data feed becomes unavailable, traders will not receive real-time market data, potentially leading to significant financial losses. \n\n ### FAL-100\n\n ### Severity: High\n\n```python\ndef get_market_data():\n    data = primary_data_feed.fetch()\n    if data:\n        process_data(data)\n    else:\n        log_error("Failed to retrieve market data")\n```\n\n ### Solution: Implement a fallback strategy by adding an alternative data feed. If the primary data feed fails, the system should automatically switch to the secondary data feed.\n\n```python\ndef get_market_data():\n    data = primary_data_feed.fetch()\n    if data:\n        process_data(data)\n    else:\n        log_warning("Primary data feed failed. Switching to secondary.")\n        data = secondary_data_feed.fetch()\n        if data:\n            process_data(data)\n        else:\n            log_error("Both data feeds failed. Unable to retrieve market data")\n```',
    },
    {
        "characteristic": "Rate Limiting",
        "description": "",
        "abbreviation": "RAT-100",
        "example": "## Rate Limiting is a technique used to control the frequency of incoming requests to a system. It is significant in software resilience as it helps prevent system overload, ensures fair usage among users, and protects against denial-of-service attacks.\n\n ### Issue: In a social media application, the API endpoint for posting comments does not implement rate limiting. This allows users to send an excessive number of requests in a short period, potentially overloading the server and degrading performance for all users. \n\n ### RAT-100\n\n ### Severity: High\n\n```python\nfrom flask import Flask, request\n\napp = Flask(__name__)\n\n@app.route('/post_comment', methods=['POST'])\ndef post_comment():\n    user_id = request.form['user_id']\n    comment = request.form['comment']\n    save_comment_to_database(user_id, comment)\n    return 'Comment posted successfully'\n```\n\n ### Solution: Implement rate limiting to restrict the number of requests a user can make within a specific time frame. This prevents abuse and ensures the system remains responsive for all users.\n\n```python\nfrom flask import Flask, request\nfrom flask_limiter import Limiter\nfrom flask_limiter.util import get_remote_address\n\napp = Flask(__name__)\nlimiter = Limiter(app, key_func=get_remote_address)\n\n@app.route('/post_comment', methods=['POST'])\n@limiter.limit(\"5 per minute\")\ndef post_comment():\n    user_id = request.form['user_id']\n    comment = request.form['comment']\n    save_comment_to_database(user_id, comment)\n    return 'Comment posted successfully'\n```",
    },
    {
        "characteristic": "Bulkheading",
        "description": "",
        "abbreviation": "BUL-100",
        "example": "## Bulkheading is a design pattern that isolates different components or services of an application into separate pools or partitions. This prevents a failure in one component from cascading to others, thereby enhancing software resilience by ensuring that issues in one area do not bring down the entire system.\n\n ### Issue: The original code lacks isolation between services, risking overall system stability during individual service failures.\n\n ### BUL-100\n\n ### Severity: High\n\n```python\n# Shared resources\nthread_pool = ThreadPool(max_workers=100)\ndb_connection_pool = DBConnectionPool(max_connections=50)\n\ndef handle_request(request):\n    # All services use the same thread and DB connection pools\n    thread_pool.submit(process_booking, request.booking_info)\n    thread_pool.submit(process_payment, request.payment_info)\n    thread_pool.submit(send_notification, request.user_contact)\n```\n\n ### Solution: Implement bulkheading by allocating separate thread pools and database connection pools for each microservice. This ensures that if one service fails or becomes resource-intensive, it does not impact the others.\n\n```python\n# Separate resources for each service\nbooking_thread_pool = ThreadPool(max_workers=50)\npayment_thread_pool = ThreadPool(max_workers=30)\nnotification_thread_pool = ThreadPool(max_workers=20)\n\nbooking_db_pool = DBConnectionPool(max_connections=25)\npayment_db_pool = DBConnectionPool(max_connections=15)\nnotification_db_pool = DBConnectionPool(max_connections=10)\n\ndef handle_request(request):\n    # Each service uses its own thread and DB connection pools\n    booking_thread_pool.submit(process_booking, request.booking_info, db_pool=booking_db_pool)\n    payment_thread_pool.submit(process_payment, request.payment_info, db_pool=payment_db_pool)\n    notification_thread_pool.submit(send_notification, request.user_contact, db_pool=notification_db_pool)\n```",
    },
    {
        "characteristic": "Configuration Management",
        "description": "",
        "abbreviation": "CON-100",
        "example": "## Configuration Management is the practice of systematically handling changes to a system's configuration, ensuring consistency and traceability. It is significant in software resilience as it allows applications to adapt to changes smoothly, minimizes downtime, and prevents configuration-related failures.\n\n ### Issue:  In a cloud-based e-commerce platform, the database connection settings are hard-coded within the application code. This makes updating configurations cumbersome and risky, as any change requires code modification and redeployment, increasing the likelihood of errors and system downtime. \n\n ### CON-100\n\n ### Severity: High\n\n```python\ndef connect_to_database():\n    connection = DatabaseConnection(\n        host='localhost',\n        port=5432,\n        user='admin',\n        password='password',\n        database='ecommerce_db'\n    )\n    return connection\n```\n\n ### Solution: Externalize the configuration settings using environment variables or a configuration file. Implementing configuration management allows for dynamic updates without changing the codebase, enhancing resilience by reducing downtime and the risk of configuration errors.\n\n```python\nimport os\n\ndef connect_to_database():\n    connection = DatabaseConnection(\n        host=os.getenv('DB_HOST', 'localhost'),\n        port=int(os.getenv('DB_PORT', 5432)),\n        user=os.getenv('DB_USER', 'admin'),\n        password=os.getenv('DB_PASSWORD', 'password'),\n        database=os.getenv('DB_NAME', 'ecommerce_db')\n    )\n    return connection\n```",
    },
    {
        "characteristic": "Dependency Isolation",
        "description": "",
        "abbreviation": "DEP-100",
        "example": "## Dependency Isolation is the practice of decoupling software components to reduce interdependencies, allowing parts of a system to function independently. This enhances software resilience by ensuring that failures or changes in one component do not adversely affect other components.\n\n ### Issue:  In a fintech application handling online transactions, the payment processing module is tightly coupled to a specific third-party payment gateway. If this gateway experiences downtime or issues, the entire payment processing halts, impacting the application's ability to process transactions. \n\n ### DEP-100\n\n ### Severity: Critical\n\n```python\ndef process_payment(amount, card_info):\n    response = third_party_gateway.charge(amount, card_info)\n    if response.success:\n        update_transaction_status('success')\n    else:\n        update_transaction_status('failure')\n```\n\n ### Solution: Implement dependency isolation by introducing an abstraction layer for the payment gateway. This allows the application to switch between multiple payment gateways seamlessly, ensuring continuity even if one gateway fails.\n\n```python\nclass PaymentProcessor:\n    def charge(self, amount, card_info):\n        raise NotImplementedError\n\nclass ThirdPartyGateway(PaymentProcessor):\n    def charge(self, amount, card_info):\n        # Implementation for third-party gateway\n        pass\n\nclass AlternativeGateway(PaymentProcessor):\n    def charge(self, amount, card_info):\n        # Implementation for alternative gateway\n        pass\n\ndef process_payment(amount, card_info):\n    try:\n        processor = get_available_payment_processor()\n        response = processor.charge(amount, card_info)\n        if response.success:\n            update_transaction_status('success')\n        else:\n            update_transaction_status('failure')\n    except Exception:\n        update_transaction_status('failure')\n```",
    },
    {
        "characteristic": "State Management",
        "description": "",
        "abbreviation": "STA-100",
        "example": "## State Management is the practice of controlling the state of an application or system over time, ensuring data consistency and reliability. It is significant in software resilience because proper state management prevents errors, data loss, and allows systems to recover gracefully from failures.\n\n ### Issue:In an e-commerce application, the code stores user shopping cart data in server-side memory without persistence. If the server restarts or if multiple server instances are used (like in a load-balanced environment), the shopping cart data can be lost or become inconsistent, leading to a poor user experience and potential loss of sales. \n\n ### STA-100\n\n ### Severity: High\n\n```python\n# In-memory storage of shopping carts\ncarts = {}\n\ndef add_to_cart(user_id, item):\n    if user_id not in carts:\n        carts[user_id] = []\n    carts[user_id].append(item)\n```\n\n ### Solution: Implement persistent state management by storing the shopping cart data in a centralized database or persistent storage system. This ensures the cart state is maintained across server restarts and multiple instances, enhancing software resilience and providing a consistent user experience.\n\n```python\ndef add_to_cart(user_id, item):\n    cart = get_cart_from_database(user_id)\n    if not cart:\n        cart = []\n    cart.append(item)\n    save_cart_to_database(user_id, cart)\n```",
    },
    {
        "characteristic": "Connection Management",
        "description": "",
        "abbreviation": "COM-100",
        "example": '## Connection Management involves efficiently handling the creation, usage, and closure of connections to external resources like databases or network services. It is significant in software resilience because proper connection management prevents resource leaks, avoids exhaustion of limited resources, and ensures the application remains stable under load.\n\n ### Issue: In an online banking application, the code opens a new database connection for each transaction but fails to close it after use. This leads to a gradual accumulation of open connections, eventually exhausting the database\'s maximum allowed connections and causing the application to crash or become unresponsive.\n\n ### COM-100\n\n ### Severity: Critical\n\n```python\ndef process_transaction(transaction_details):\n    connection = Database.connect()\n    result = connection.execute("INSERT INTO transactions VALUES (?)", transaction_details)\n    return result\n```\n\n ### Solution: Implement proper connection management by ensuring that each database connection is closed after its operation is complete. Using connection pooling or context managers can automate this process and prevent resource leaks, enhancing software resilience.\n\n```python\ndef process_transaction(transaction_details):\n    with Database.connect() as connection:\n        result = connection.execute("INSERT INTO transactions VALUES (?)", transaction_details)\n    return result\n```',
    },
    {
        "characteristic": "Monitoring and Logging",
        "description": "",
        "abbreviation": "MOL-100",
        "example": "## Monitoring and Logging are practices that involve tracking an application's performance and recording significant events or errors. They are significant in software resilience because they enable early detection of issues, facilitate troubleshooting, and ensure the system remains reliable and available.\n\n ### Issue: In an industrial automation system, the code responsible for controlling machinery operations lacks proper logging of errors and events. This absence makes it difficult to monitor system health or diagnose problems, potentially leading to unnoticed failures and costly production downtime.\n\n ### MON-100\n\n ### Severity: High\n\n```python\ndef control_machine(operation):\n    result = machine.execute(operation)\n    if result:\n        return True\n    else:\n        # No logging implemented\n        return False\n```\n\n ### Solution: Implement comprehensive logging to record both successful operations and failures. This enhances software resilience by allowing real-time monitoring and quicker diagnosis of issues, thereby minimizing downtime and maintaining production efficiency.\n\n```python\nimport logging\n\ndef control_machine(operation):\n    result = machine.execute(operation)\n    if result:\n        logging.info(f\"Operation '{operation}' executed successfully.\")\n        return True\n    else:\n        logging.error(f\"Failed to execute operation '{operation}'.\")\n        return False\n```",
    },
    {
        "characteristic": "Thread Safety",
        "description": "",
        "abbreviation": "THR-100",
        "example": '## Thread Safety refers to the ability of a program or code segment to function correctly in a multi-threaded environment, where multiple threads are executing simultaneously. It is significant in software resilience because thread-unsafe code can lead to race conditions, data corruption, and unpredictable behavior, compromising the stability and reliability of the application.\n\n ### Issue: In a banking application, the code updates a shared account balance without any synchronization mechanisms. Multiple threads accessing and modifying the balance simultaneously can lead to inconsistent or incorrect account balances.\n\n ### THR-100\n\n ### Severity: High\n\n```python\n# Shared account balance\naccount_balance = 1000\n\ndef withdraw(amount):\n    global account_balance\n    if account_balance >= amount:\n        account_balance -= amount\n        print(f"Withdrawn {amount}, new balance is {account_balance}")\n    else:\n        print("Insufficient funds")\n\n# Multiple threads attempting to withdraw funds\nthread1 = Thread(target=withdraw, args=(700,))\nthread2 = Thread(target=withdraw, args=(500,))\n\nthread1.start()\nthread2.start()\nthread1.join()\nthread2.join()\n```\n\n ### Solution: Implement thread synchronization using a lock to ensure that only one thread can access and modify the shared account balance at a time. This prevents race conditions and ensures data consistency.\n\n```python\nimport threading\n\n# Shared account balance\naccount_balance = 1000\nbalance_lock = threading.Lock()\n\ndef withdraw(amount):\n    global account_balance\n    with balance_lock:\n        if account_balance >= amount:\n            account_balance -= amount\n            print(f"Withdrawn {amount}, new balance is {account_balance}")\n        else:\n            print("Insufficient funds")\n\n# Multiple threads attempting to withdraw funds\nthread1 = threading.Thread(target=withdraw, args=(700,))\nthread2 = threading.Thread(target=withdraw, args=(500,))\n\nthread1.start()\nthread2.start()\nthread1.join()\nthread2.join()\n```',
    },
    {
        "characteristic": "Data Resilience",
        "description": "",
        "abbreviation": "DAT-100",
        "example": '## Data Resilience refers to the ability of a system to withstand and recover from data failures, ensuring data integrity, availability, and consistency. It is significant in software resilience because it prevents data loss and corruption, which are critical for maintaining trust and functionality in applications.\n\n ### Issue: In a healthcare application, patient records are stored in a single local database without any backups or replication. If the database server crashes or data becomes corrupted, all patient information could be lost, leading to severe legal and operational consequences.\n\n ### DAT-100\n\n ### Severity: High\n\n```python\ndef save_patient_record(record):\n    connection = sqlite3.connect(\'patient_records.db\')\n    cursor = connection.cursor()\n    cursor.execute("INSERT INTO patients VALUES (?, ?, ?)", record)\n    connection.commit()\n    connection.close()\n```\n\n ### Solution: Implement data resilience by integrating a robust database system with backup and replication capabilities. Using a distributed database or regular backups ensures that patient data is preserved even if one server fails.\n\n```python\ndef save_patient_record(record):\n    connection = connect_to_redundant_database_cluster()\n    cursor = connection.cursor()\n    cursor.execute("INSERT INTO patients VALUES (%s, %s, %s)", record)\n    connection.commit()\n    connection.close()\n```',
    },
    {
        "characteristic": "Immutable Infrastructure",
        "description": "",
        "abbreviation": "IMM-100",
        "example": "## Immutable Infrastructure is a paradigm where servers or components are replaced rather than modified. This approach enhances software resilience by eliminating configuration drift, reducing deployment errors, and ensuring consistency across environments.\n\n ### Issue: In a cloud-based deployment for a microservices application, the code manually updates configurations and dependencies on running servers. This practice can lead to inconsistencies, configuration drift, and makes it difficult to recover or replicate the environment in case of failures.\n\n ### IMM-100\n\n ### Severity: High\n\n```python\ndef update_server():\n    # Manually install dependencies and update configurations\n    os.system('sudo apt-get update')\n    os.system('sudo apt-get install -y dependency-package')\n    with open('/etc/config.yaml', 'w') as config_file:\n        config_file.write('new configurations')\n```\n\n ### Solution: Implement Immutable Infrastructure by using pre-configured machine images or containers that contain all necessary dependencies and configurations. Deploy new instances using these images instead of modifying existing ones, enhancing resilience by ensuring consistency and ease of recovery.\n\n```python\ndef deploy_new_instance():\n    # Use a pre-built Docker image for deployment\n    os.system('docker pull myapp:latest')\n    os.system('docker run -d myapp:latest')\n```",
    },
    {
        "characteristic": "Health Checks",
        "description": "",
        "abbreviation": "HEA-100",
        "example": "## Health Checks are automated tests that verify the health and responsiveness of an application or service at regular intervals. They are significant in software resilience because they enable early detection of issues, allowing systems like load balancers or orchestrators to reroute traffic or restart services, thereby minimizing downtime and maintaining service availability.\n\n ### Issue: In a cloud-based online retail application, the web server does not implement any health check endpoints. As a result, if the application becomes unresponsive or crashes, the load balancer cannot detect the failure and continues to send user requests to the unhealthy server, leading to poor user experience and potential loss of sales.\n\n ### HEA-100\n\n ### Severity: High\n\n```python\ndef start_server():\n    app.run(host='0.0.0.0', port=80)\n```\n\n ### Solution: Implement a health check endpoint that returns the application's status. This allows the load balancer to periodically check the application's health and remove it from the rotation if it is unresponsive, enhancing software resilience by ensuring that user requests are only sent to healthy instances.\n\n```python\ndef start_server():\n    app.run(host='0.0.0.0', port=80)\n\n@app.route('/health', methods=['GET'])\ndef health_check():\n    return 'OK', 200\n```",
    },
    {
        "characteristic": "Disaster Recovery",
        "description": "",
        "abbreviation": "DIS-100",
        "example": '## Disaster Recovery is the process of planning and implementing strategies to restore software systems and data after catastrophic events. It is significant in software resilience because it ensures business continuity, minimizes downtime, and protects against data loss from disasters like hardware failures, cyber-attacks, or natural disasters.\n\n ### Issue: In a cloud-based customer relationship management (CRM) application, the code stores customer data on a single server without any backup or replication mechanisms. If the server fails or data becomes corrupted, all customer information could be permanently lost, leading to severe operational and financial repercussions.\n\n ### DIS-100\n\n ### Severity: High\n\n```python\ndef save_customer_data(customer):\n    connection = sqlite3.connect(\'customer_data.db\')\n    cursor = connection.cursor()\n    cursor.execute("INSERT INTO customers VALUES (?, ?, ?)", customer)\n    connection.commit()\n    connection.close()\n```\n\n ### Solution: Implement a disaster recovery strategy by setting up automated backups and data replication to secondary servers or cloud storage. Utilize database systems that support replication and failover to ensure data can be recovered and services can be restored quickly after a disaster.\n\n```python\ndef save_customer_data(customer):\n    connection = get_redundant_database_connection()\n    cursor = connection.cursor()\n    cursor.execute("INSERT INTO customers VALUES (?, ?, ?)", customer)\n    connection.commit()\n    connection.close()\n```',
    },
    {
        "characteristic": "Network Resilience",
        "description": "",
        "abbreviation": "NET-100",
        "example": '## Network Resilience refers to the ability of a network to maintain its performance and recover quickly from disruptions or failures. It is significant in software resilience because it ensures continuous availability and reliability of services, even in the face of network issues such as latency, outages, or attacks.\n\n ### Issue: In a financial trading platform, the code performs synchronous API calls to an external market data service without handling potential network failures. If the external service becomes unavailable or experiences high latency, the trading platform may become unresponsive, leading to missed trades and financial losses.\n\n ### NET-100\n\n ### Severity: High\n\n```python\nimport requests\n\ndef get_market_data(symbol):\n    response = requests.get(f"https://api.marketdata.com/{symbol}")\n    if response.status_code == 200:\n        return response.json()\n    else:\n        raise Exception("Failed to retrieve market data")\n```\n\n ### Solution: Implement network resilience by adding retry mechanisms with exponential backoff and timeout settings. This ensures that temporary network issues are handled gracefully, reducing the risk of the application becoming unresponsive due to external service failures.\n\n```python\nimport requests\nimport time\nfrom requests.exceptions import RequestException\n\ndef get_market_data(symbol, retries=3, backoff_factor=2, timeout=5):\n    attempt = 0\n    while attempt < retries:\n        try:\n            response = requests.get(f"https://api.marketdata.com/{symbol}", timeout=timeout)\n            response.raise_for_status()\n            return response.json()\n        except RequestException as e:\n            attempt += 1\n            wait = backoff_factor ** attempt\n            print(f"Attempt {attempt} failed: {e}. Retrying in {wait} seconds...")\n            time.sleep(wait)\n    raise Exception("All retry attempts failed. Unable to retrieve market data")\n```',
    },
    {
        "characteristic": "Resource Management",
        "description": "",
        "abbreviation": "RES-100",
        "example": "## Resource Management involves the efficient and effective deployment and allocation of an application's resources, such as memory, file handles, network connections, and CPU usage. It is significant in software resilience because proper resource management prevents resource leaks, ensures optimal performance, and maintains the stability and reliability of applications under various conditions.\n\n ### Issue: In a healthcare data processing system, the code opens multiple file handles to read patient records but does not close them after use. This leads to a gradual exhaustion of available file descriptors, causing the application to fail when it can no longer open new files.\n\n ### RES-100\n\n ### Severity: High\n\n```python\ndef process_patient_records(file_paths):\n    for path in file_paths:\n        file = open(path, 'r')\n        data = file.read()\n        # Process the data\n        # No file.close() called\n```\n\n ### Solution: Implement proper resource management by ensuring that all opened resources, such as file handles, are properly closed after their usage. Utilizing context managers (the `with` statement in Python) ensures that resources are automatically released, even if errors occur during processing. This prevents resource leaks and maintains the application's ability to handle multiple files without failure.\n\n```python\ndef process_patient_records(file_paths):\n    for path in file_paths:\n        with open(path, 'r') as file:\n            data = file.read()\n            # Process the data\n        # File is automatically closed after the with block\n```",
    },
]

unittestability = [
    {
        "characteristic": "Boundary Conditions",
        "description": "",
        "abbreviation": "BOU-100",
        "example": '## Boundary conditions refer to the extreme input values or limits under which a software system operates, such as maximum or minimum allowable inputs. Testing these conditions is vital in unit testing to ensure that the software behaves correctly at its operational edges, preventing errors that might occur in real-world scenarios.\n\n ### Issue: In a financial trading application, the function to execute trades fails when processing the maximum allowable trade amount due to an incorrect boundary condition check. This could lead to legitimate high-value trades being rejected.\n\n ### BOU-100\n\n ### Severity: Critical\n\n```python\ndef execute_trade(trade_amount):\n    if trade_amount < 1000000:\n        # Proceed with trade execution\n        print("Trade executed.")\n    else:\n        print("Trade amount exceeds the limit.")\n```\n\n ### Solution: Modify the boundary condition to include the maximum allowable trade amount by changing the condition from less than 1,000,000 to less than or equal to 1,000,000.\n\n```python\ndef execute_trade(trade_amount):\n    if trade_amount <= 1000000:\n        # Proceed with trade execution\n        print("Trade executed.")\n    else:\n        print("Trade amount exceeds the limit.")\n```',
    },
    {
        "characteristic": "Dynamism",
        "description": "",
        "abbreviation": "DYN-100",
        "example": "## Dynamism refers to the use of dynamic features in programming, such as dynamic typing, reflection, or runtime modifications. These features provide flexibility and adaptability in software development but can complicate unit testability by making it harder to predict and control program behavior during testing.\n\n ### Issue: The use of dynamic function calls makes it difficult to mock dependencies during unit testing.\n\n ### DYN-100\n\n ### Severity: High\n\n```python\ndef process_data(data_source):\n    processor = globals()[data_source.get_processor()]()\n    return processor.process(data_source.get_data())\n```\n\n ### Solution: Replace dynamic function calls with dependency injection to make dependencies explicit and facilitate mocking during unit testing.\n\n```python\ndef process_data(data_source, processor):\n    return processor.process(data_source.get_data())\n```",
    },
    {
        "characteristic": "Environment Dependencies",
        "description": "",
        "abbreviation": "ENV-100",
        "example": "## Environment Dependencies refer to the reliance of software components on the external environment, such as environment variables, configuration files, or external services. These dependencies can hinder software unit testability by making tests dependent on specific environmental setups, which can lead to flaky tests and reduce portability across different testing environments.\n\n ### Issue: The code directly accesses environment variables, creating a dependency on the external environment and complicating unit testing.\n\n ### ENV-100\n\n ### Severity: High\n\n```python\ndef connect_to_database():\n    db_host = os.getenv('DB_HOST')\n    db_port = os.getenv('DB_PORT')\n    connection = create_connection(host=db_host, port=db_port)\n    return connection\n```\n\n ### Solution: Utilize dependency injection by passing configuration parameters to the function, thereby decoupling the code from the external environment and enhancing testability.\n\n```python\ndef connect_to_database(db_host, db_port):\n    connection = create_connection(host=db_host, port=db_port)\n    return connection\n```",
    },
    {
        "characteristic": "State Management",
        "description": "",
        "abbreviation": "STA-100",
        "example": '## State Management refers to how an application maintains and handles its data and state across different components and throughout its lifecycle. Proper state management is essential for ensuring that the application\'s behavior is predictable and consistent, which in turn enhances unit testability by allowing individual components to be tested in isolation without unintended side effects.\n\n ### Issue: The application uses mutable global variables for managing user session state in a web application, leading to potential race conditions and making it difficult to perform unit tests as the state is shared and can be altered by different parts of the application unpredictably.\n\n ### STA-100\n\n ### Severity: Critical\n\n```python\n# session_manager.py\n\ncurrent_user = {}\n\n\ndef login(user):\n    global current_user\n    current_user = user\n\n\ndef logout():\n    global current_user\n    current_user = {}\n\n\ndef get_current_user():\n    return current_user\n```\n\n ### Solution: Implement a session management class that encapsulates the session state and uses dependency injection to manage the session state within the application components. This approach avoids the use of global variables, making the state predictable and easily mockable for unit testing.\n\n```python\n# session_manager.py\n\nclass SessionManager:\n    def __init__(self):\n        self._current_user = {}\n\n\n    def login(self, user):\n        self._current_user = user\n\n\n    def logout(self):\n        self._current_user = {}\n\n\n    def get_current_user(self):\n        return self._current_user\n\n\n# app.py\n\nclass UserService:\n    def __init__(self, session_manager: SessionManager):\n        self.session_manager = session_manager\n\n\n    def authenticate_user(self, user_credentials):\n        # Authenticate user and retrieve user data\n        user = {"id": 1, "name": "Alice"}  # Example user\n        self.session_manager.login(user)\n\n\n    def end_session(self):\n        self.session_manager.logout()\n\n\n    def get_user(self):\n        return self.session_manager.get_current_user()\n\n\n# test_user_service.py\n\nimport unittest\nfrom session_manager import SessionManager\nfrom app import UserService\n\n\nclass TestUserService(unittest.TestCase):\n    def test_authenticate_user(self):\n        session = SessionManager()\n        service = UserService(session)\n        service.authenticate_user({"username": "alice", "password": "password"})\n        self.assertEqual(session.get_current_user(), {"id": 1, "name": "Alice"})\n\n\n    def test_end_session(self):\n        session = SessionManager()\n        service = UserService(session)\n        service.authenticate_user({"username": "alice", "password": "password"})\n        service.end_session()\n        self.assertEqual(session.get_current_user(), {})\n\n\nif __name__ == \'__main__\':\n    unittest.main()\n```',
    },
    {
        "characteristic": "Input/Output",
        "description": "",
        "abbreviation": "INP-100",
        "example": "## Input/Output refers to the processes of receiving data (input) and producing data (output) in software applications. In the context of software unit testability, Input/Output operations can complicate testing because they often involve external systems or side effects, making it harder to isolate and test individual components.\n\n ### Issue: The function directly reads from a file and writes to a database, making it difficult to unit test without relying on external resources.\n\n ### INP-100\n\n ### Severity: High\n\n```python\ndef process_data():\n    with open('data.txt', 'r') as file:\n        data = file.read()\n    result = perform_complex_calculation(data)\n    db.insert(result)\n```\n\n ### Solution: Refactor the function to separate the Input/Output operations from the business logic by using dependency injection. This allows mocking external dependencies during testing.\n\n```python\ndef process_data(file_reader, db):\n    data = file_reader.read('data.txt')\n    result = perform_complex_calculation(data)\n    db.insert(result)\n```",
    },
    {
        "characteristic": "Tight Coupling",
        "description": "",
        "abbreviation": "TIG-100",
        "example": "## Tight Coupling refers to a scenario where components or classes in a software system are highly dependent on each other, making it difficult to isolate and test them independently. In the context of software `Unit Testability`, tight coupling reduces flexibility and increases the difficulty of mocking dependencies, thereby hindering effective unit testing.\n\n ### Issue: The `OrderService` class is directly instantiating the `PaymentProcessor` class, creating a strong dependency that makes it hard to unit test `OrderService` in isolation.\n\n ### TIG-100\n\n ### Severity: High\n\n```python\nclass PaymentProcessor:\n    def process_payment(self, amount):\n        # Code to process payment\n        pass\n\n\nclass OrderService:\n    def place_order(self, order):\n        payment_processor = PaymentProcessor()\n        payment_processor.process_payment(order.total)\n        # Additional order placement logic\n```\n\n ### Solution: Introduce dependency injection by passing the `PaymentProcessor` as a dependency to the `OrderService`. This allows mocking the `PaymentProcessor` during testing and reduces coupling.\n\n```python\nclass PaymentProcessor:\n    def process_payment(self, amount):\n        # Code to process payment\n        pass\n\n\nclass OrderService:\n    def __init__(self, payment_processor):\n        self.payment_processor = payment_processor\n\n\n    def place_order(self, order):\n        self.payment_processor.process_payment(order.total)\n        # Additional order placement logic\n```",
    },
    {
        "characteristic": "Errors and Exceptions",
        "description": "",
        "abbreviation": "EAE-100",
        "example": '## Errors and Exceptions refer to unexpected conditions or events that occur during the execution of a program, disrupting its normal flow. Proper handling of errors and exceptions is crucial for software `Unit Testability` as it ensures that individual components behave correctly under various failure scenarios, allowing for comprehensive and reliable tests.\n\n ### Issue: The `calculate_interest` function does not handle potential exceptions when processing user input, leading to unhandled errors that can cause the application to crash and complicate unit testing.\n\n ### EAE-100\n\n ### Severity: High\n\n```python\ndef calculate_interest(user_input):\n    amount = float(user_input)\n    interest = amount * 0.05\n    return interest\n```\n\n ### Solution: Implement exception handling to manage invalid inputs gracefully. This allows unit tests to verify that the function correctly handles erroneous data without causing the application to fail.\n\n```python\ndef calculate_interest(user_input):\n    try:\n        amount = float(user_input)\n    except ValueError:\n        raise ValueError("Invalid input: Amount must be a number.")\n    interest = amount * 0.05\n    return interest\n```',
    },
    {
        "characteristic": "Size and Complexity",
        "description": "",
        "abbreviation": "SAC-100",
        "example": "## Size and Complexity refers to the extent and intricacy of a codebase or individual components within it. High size and complexity can make software harder to understand, maintain, and test. In the context of software `Unit Testability`, overly large and complex functions or classes can hinder the ability to write effective and comprehensive unit tests, as it becomes challenging to isolate and target specific functionalities.\n\n ### Issue: The `generate_financial_report` function is excessively long and handles multiple responsibilities, including data retrieval, processing, formatting, and exporting. This high complexity makes it difficult to unit test each part individually and increases the likelihood of bugs.\n\n ### SAC-100\n\n ### Severity: Critical\n\n```python\ndef generate_financial_report(user_id):\n    # Retrieve user data\n    user = get_user_from_db(user_id)\n    transactions = get_transactions(user_id)\n\n    # Process data\n    total = 0\n    for txn in transactions:\n        total += txn.amount\n    average = total / len(transactions) if transactions else 0\n\n    # Format report\n    report = f\"User: {user.name}\\nTotal Transactions: {total}\\nAverage Transaction: {average}\\n\"\n\n    # Export report\n    with open(f'report_{user_id}.txt', 'w') as file:\n        file.write(report)\n\n    return report\n```\n\n ### Solution: Refactor the `generate_financial_report` function by decomposing it into smaller, single-responsibility functions. This modular approach enhances readability, maintainability, and unit testability by allowing each component to be tested in isolation.\n\n```python\ndef retrieve_user_data(user_id):\n    user = get_user_from_db(user_id)\n    transactions = get_transactions(user_id)\n    return user, transactions\n\n\ndef calculate_totals(transactions):\n    total = sum(txn.amount for txn in transactions)\n    average = total / len(transactions) if transactions else 0\n    return total, average\n\n\ndef format_report(user, total, average):\n    return f\"User: {user.name}\\nTotal Transactions: {total}\\nAverage Transaction: {average}\\n\"\n\n\ndef export_report(user_id, report):\n    with open(f'report_{user_id}.txt', 'w') as file:\n        file.write(report)\n\n\ndef generate_financial_report(user_id):\n    user, transactions = retrieve_user_data(user_id)\n    total, average = calculate_totals(transactions)\n    report = format_report(user, total, average)\n    export_report(user_id, report)\n    return report\n```",
    },
    {
        "characteristic": "Modularity and Structure",
        "description": "",
        "abbreviation": "MAS-100",
        "example": "## Modularity and Structure refer to the organization of code into separate, interchangeable components or modules, each responsible for a specific functionality. This design principle enhances code readability, maintainability, and facilitates unit testability by allowing individual modules to be tested in isolation.\n\n ### Issue: The `UserService` class handles multiple responsibilities, including user authentication, data validation, and database interactions, which violates the Single Responsibility Principle.\n\n ### MAS-101\n\n ### Severity: High\n\n```python\nclass UserService:\n\n    def authenticate_user(self, username, password):\n        user = self.get_user_from_db(username)\n        if user and user.password == password:\n            return True\n        return False\n\n    def get_user_from_db(self, username):\n        # Direct database access\n        pass\n\n    def validate_user_data(self, data):\n        # Validation logic\n        pass\n```\n\n ### Solution: Refactor the `UserService` class by separating concerns into distinct classes, such as `AuthenticationService`, `UserRepository`, and `UserValidator`. This enhances modularity and allows each component to be independently tested.\n\n```python\nclass AuthenticationService:\n    def __init__(self, user_repository):\n        self.user_repository = user_repository\n\n    def authenticate_user(self, username, password):\n        user = self.user_repository.get_user(username)\n        if user and user.password == password:\n            return True\n        return False\n\nclass UserRepository:\n    def get_user(self, username):\n        # Direct database access\n        pass\n\nclass UserValidator:\n    def validate_user_data(self, data):\n        # Validation logic\n        pass\n```",
    },
    {
        "characteristic": "Interfaces and Abstractions and Dependency Injection",
        "description": "",
        "abbreviation": "INT-100",
        "example": '## Interfaces and Abstractions and Dependency Injection refer to design principles that promote the separation of concerns by defining clear contracts (interfaces) and abstracting dependencies between components. In the context of software `Unit Testability`, these principles are essential as they allow components to be easily mocked or stubbed, facilitating isolated and effective unit testing.\n\n ### Issue: The `ReportGenerator` class directly instantiates the `PDFExporter` class, creating a strong dependency that makes it difficult to unit test `ReportGenerator` without generating actual PDF files.\n\n ### INT-100\n\n ### Severity: Medium\n\n```python\nclass PDFExporter:\n    def export(self, data):\n        # Code to export data to PDF\n        pass\n\nclass ReportGenerator:\n    def generate_report(self, data):\n        exporter = PDFExporter()\n        exporter.export(data)\n        return "Report generated successfully"\n```\n\n ### Solution: Introduce an abstraction by defining an interface for the exporter and apply dependency injection by passing the exporter as a dependency to the `ReportGenerator` class. This allows for easy mocking of the exporter during unit testing, enhancing testability and reducing coupling.\n\n```python\nfrom abc import ABC, abstractmethod\n\nclass IExporter(ABC):\n    @abstractmethod\n    def export(self, data):\n        pass\n\nclass PDFExporter(IExporter):\n    def export(self, data):\n        # Code to export data to PDF\n        pass\n\nclass ReportGenerator:\n    def __init__(self, exporter: IExporter):\n        self.exporter = exporter\n\n    def generate_report(self, data):\n        self.exporter.export(data)\n        return "Report generated successfully"\n\n# Example of dependency injection\npdf_exporter = PDFExporter()\nreport_generator = ReportGenerator(pdf_exporter)\n```',
    },
    {
        "characteristic": "External Dependencies",
        "description": "",
        "abbreviation": "EXT-100",
        "example": '## External Dependencies refer to external libraries, services, or modules that a software component relies on to function correctly. They are significant in software unit testability because they can introduce complexities such as requiring network access, managing different versions, or handling external states, which can make isolating and testing individual units of code more challenging.\n\n ### Issue: The code directly calls an external API using the `requests` library, making it difficult to unit test without actual API calls.\n\n ### EXT-100\n\n ### Severity: High\n\n```python\nimport requests\n\ndef fetch_user_data(user_id):\n    response = requests.get(f"https://api.example.com/users/{user_id}")\n    return response.json()\n```\n\n ### Solution: Introduce dependency injection by abstracting the external API call, allowing the use of mock objects during testing to simulate API responses without making real HTTP requests.\n\n```python\nimport requests\n\ndef fetch_user_data(user_id, http_client=requests):\n    response = http_client.get(f"https://api.example.com/users/{user_id}")\n    return response.json()\n```',
    },
    {
        "characteristic": "Static Methods and Singletons",
        "description": "",
        "abbreviation": "SMS-100",
        "example": "## Static Methods and Singletons are design patterns used to control object creation and provide global access points to certain functionalities. Static methods belong to the class rather than any instance, making them accessible without creating an object. Singletons ensure a class has only one instance and provide a global point of access to it. In the context of software unit testability, overuse of static methods and singletons can hinder the ability to isolate and mock dependencies, making testing more challenging.\n\n ### Issue: The `DatabaseConnection` class uses a Singleton pattern combined with static methods, which makes it difficult to mock the database connection in unit tests, leading to tightly coupled code and reduced testability.\n\n ### SMS-100\n\n ### Severity: High\n\n```python\nclass DatabaseConnection:\n    _instance = None\n\n    @staticmethod\n    def get_instance():\n        if DatabaseConnection._instance is None:\n            DatabaseConnection._instance = DatabaseConnection()\n        return DatabaseConnection._instance\n\n    @staticmethod\n    def execute_query(query):\n        conn = DatabaseConnection.get_instance()\n        # Execute the query using the connection\n        pass\n```\n\n ### Solution: Refactor the `DatabaseConnection` class to use dependency injection instead of static methods and a Singleton pattern. This allows for easier mocking and isolation of the database connection during unit testing.\n\n```python\nclass DatabaseConnection:\n    def execute_query(self, query):\n        # Execute the query using the connection\n        pass\n\nclass DataService:\n    def __init__(self, db_connection: DatabaseConnection):\n        self.db_connection = db_connection\n\n    def get_data(self, query):\n        return self.db_connection.execute_query(query)\n```",
    },
]

scalability = [
    {
        "characteristic": "Multi-threading",
        "description": "",
        "abbreviation": "MUT-100",
        "example": "## Multi-threading is a programming technique that allows multiple threads to run concurrently within a single process. It is significant for software scalability because it enables applications to handle multiple tasks simultaneously, improving performance and responsiveness. In industries like web services and cloud computing, multi-threading is essential for efficiently managing numerous client requests and processing data in parallel.\n\n ### Issue: In a web server application, the code handles incoming HTTP requests sequentially, processing one request at a time. This approach leads to poor scalability and high latency under heavy traffic, as the server cannot serve multiple clients concurrently.\n\n ### MUT-100\n\n ### Severity: High\n\n```python\ndef handle_requests(server_socket):\n    while True:\n        client_socket, addr = server_socket.accept()\n        handle_client(client_socket)\n```\n\n ### Solution: Introduce multi-threading to handle each client connection in a separate thread. This allows the server to process multiple client requests simultaneously, significantly improving scalability and reducing response times. Using Python's `threading` module, each incoming connection can be assigned to a new thread.\n\n```python\nimport threading\n\ndef handle_requests(server_socket):\n    while True:\n        client_socket, addr = server_socket.accept()\n        client_thread = threading.Thread(target=handle_client, args=(client_socket,))\n        client_thread.start()\n```",
    },
    {
        "characteristic": "Asynchronous programming",
        "description": "",
        "abbreviation": "ASY-100",
        "example": "## Asynchronous programming allows a program to handle multiple tasks concurrently without waiting for each task to complete before starting the next. This is significant for software scalability because it enables efficient utilization of system resources, allowing applications to manage more operations simultaneously and handle higher loads without performance degradation.\n\n ### Issue: The code uses synchronous network calls to fetch data from an external API, causing blocking and reduced scalability in a high-load web application.\n\n ### ASY-100\n\n ### Severity: High\n\n```python\nimport requests\n\ndef fetch_user_data(user_ids):\n    user_data = []\n    for user_id in user_ids:\n        response = requests.get(f'https://api.example.com/users/{user_id}')\n        user_data.append(response.json())\n    return user_data\n```\n\n ### Solution: Use asynchronous HTTP requests to prevent blocking, allowing the application to fetch data for multiple users concurrently and improve scalability.\n\n```python\nimport asyncio\nimport aiohttp\n\nasync def fetch_user(session, user_id):\n    async with session.get(f'https://api.example.com/users/{user_id}') as response:\n        return await response.json()\n\nasync def fetch_user_data(user_ids):\n    user_data = []\n    async with aiohttp.ClientSession() as session:\n        tasks = [fetch_user(session, user_id) for user_id in user_ids]\n        user_data = await asyncio.gather(*tasks)\n    return user_data\n```",
    },
    {
        "characteristic": "Non-blocking I/O",
        "description": "",
        "abbreviation": "NON-100",
        "example": "## Non-blocking I/O is a method in programming where the execution of tasks does not wait for I/O operations to complete before continuing. Instead, the program continues executing while the I/O process is managed in the background, thus allowing more efficient resource utilization and enhancing the application's ability to handle multiple requests simultaneously. Non-blocking I/O is critical in software scalability, particularly in high-performance applications like web servers and real-time systems, as it reduces idle time and improves response rates.\n\n ### Issue: The given code does not implement non-blocking I/O effectively, resulting in potential bottlenecks where I/O operations could delay other tasks from executing. This approach risks reduced performance under heavy load, impacting the application's scalability.\n\n ### NON-100\n\n ### Severity: High\n\n```python\nimport time\n\ndef read_file(file_path):\n    with open(file_path, 'r') as file:\n        data = file.read()  # blocking operation\n    return data\n\ndef process_files(file_paths):\n    results = []\n    for path in file_paths:\n        content = read_file(path)\n        time.sleep(1)  # Simulating additional delay\n        results.append(content)\n    return results\n```\n\n ### Solution: Convert the blocking file reading operation to a non-blocking one using asynchronous I/O. This can be achieved by leveraging `asyncio` and `aiofiles` to allow the file reads to run concurrently without holding up the execution, thereby enhancing scalability under increased I/O loads.\n\n```python\nimport asyncio\nimport aiofiles\n\nasync def read_file(file_path):\n    async with aiofiles.open(file_path, 'r') as file:\n        data = await file.read()  # non-blocking operation\n    return data\n\nasync def process_files(file_paths):\n    tasks = [read_file(path) for path in file_paths]\n    results = await asyncio.gather(*tasks)  # Concurrent processing of all files\n    return results\n```",
    },
    {
        "characteristic": "Efficient data structures",
        "description": "",
        "abbreviation": "DAT-100",
        "example": '## Efficient data structures optimize performance and scalability, especially critical in web development where applications handle high user traffic and large datasets. Efficient structures help ensure fast data access, low latency, and reduced server load.\n\n ### Issue: In a web application managing user sessions, a list is used to store active user sessions, leading to inefficiencies in checking if a session is active, which is frequent in high-traffic applications. Lists are inefficient for such operations due to their linear search time complexity (O(n)).\n\n ### DAT-100\n\n ### Severity: High\n\n```python\n# Inefficient use of list to store active sessions and check for active status\nactive_sessions = ["session1", "session2", "session3"]\nsession_to_check = "session2"\nif session_to_check in active_sessions:\n    # Perform some action when the session is active\n    pass\n```\n\n ### Solution: Replace the list with a `set`, which allows constant time (O(1)) lookups. This change improves scalability, making the session checks much faster, especially when handling thousands of active sessions in a web application.\n\n```python\n# Improved code using a set for efficient session management\nactive_sessions = {"session1", "session2", "session3"}\nsession_to_check = "session2"\nif session_to_check in active_sessions:\n    # Perform some action when the session is active\n    pass\n```',
    },
    {
        "characteristic": "Algorithm optimization",
        "description": "",
        "abbreviation": "ALG-100",
        "example": "## Algorithm optimization involves enhancing the efficiency of code by selecting the most suitable algorithms and data structures. It is significant in software scalability because optimized algorithms can handle increasing workloads and data sizes without a proportional increase in resource usage, ensuring the system remains performant as it grows.\n\n ### Issue: The code uses a nested loop to process user transactions, resulting in O(n^2) time complexity, which is inefficient for large datasets common in financial applications.\n\n ### ALG-100\n\n ### Severity: High\n\n```python\n# Original code\ndef find_fraudulent_transactions(transactions):\n    fraudulent = []\n    for i in range(len(transactions)):\n        for j in range(i+1, len(transactions)):\n            if transactions[i].amount == transactions[j].amount and transactions[i].date == transactions[j].date:\n                fraudulent.append(transactions[i])\n                fraudulent.append(transactions[j])\n    return fraudulent\n```\n\n ### Solution: Optimize the algorithm by using a hash table to reduce the time complexity to O(n), which is more suitable for large-scale financial data processing.\n\n```python\n# Improved code\ndef find_fraudulent_transactions(transactions):\n    transaction_map = {}\n    fraudulent = []\n    for transaction in transactions:\n        key = (transaction.amount, transaction.date)\n        if key in transaction_map:\n            fraudulent.append(transaction)\n            fraudulent.append(transaction_map[key])\n        else:\n            transaction_map[key] = transaction\n    return fraudulent\n```",
    },
    {
        "characteristic": "Memory management",
        "description": "",
        "abbreviation": "MEM-100",
        "example": "## Memory management involves efficiently handling the allocation and deallocation of memory resources in a software application. It is crucial for software scalability because improper memory management can lead to excessive memory usage, memory leaks, and can prevent an application from handling increased loads or larger datasets.\n\n ### Issue: In a data processing application for financial analytics, the code loads an entire large CSV file into memory at once. This approach consumes excessive memory and does not scale well with larger datasets, leading to potential crashes or slow performance.\n\n ### MEM-100\n\n ### Severity: High\n\n```python\nimport pandas as pd\n\ndef process_transactions(file_path):\n    data = pd.read_csv(file_path)\n    for index, row in data.iterrows():\n        process_transaction(row)\n```\n\n ### Solution: Modify the code to read and process the CSV file in chunks or use an iterator to handle large files efficiently, reducing memory usage and improving scalability.\n\n```python\nimport pandas as pd\n\ndef process_transactions(file_path):\n    for chunk in pd.read_csv(file_path, chunksize=1000):\n        for index, row in chunk.iterrows():\n            process_transaction(row)\n```",
    },
    {
        "characteristic": "File handles",
        "description": "",
        "abbreviation": "FIL-100",
        "example": "## A file handle is an operating system abstraction that allows programs to interact with files on disk. In high-frequency trading applications, where market data and transaction logs are rapidly generated, efficient file handle management is crucial for scalability and performance. Failure to manage file handles can result in resource leaks, which impair system responsiveness and the ability to process additional transactions, impacting overall trading performance.\n\n ### Issue: In a high-frequency trading application, file handles for log files are not closed after each transaction, causing a resource leak that could lead to application crashes during peak trading hours.\n\n ### FIL-100\n\n ### Severity: Critical\n\n```python\ndef log_transaction(transaction_data):\n    log_file = open('/var/log/trading_transactions.log', 'a')\n    log_file.write(transaction_data)\n    # File handle is not closed, which can cause memory leaks\n```\n\n ### Solution: Use a context manager to automatically close the file handle after writing, ensuring efficient resource management and scalability.\n\n```python\ndef log_transaction(transaction_data):\n    with open('/var/log/trading_transactions.log', 'a') as log_file:\n        log_file.write(transaction_data)\n```",
    },
    {
        "characteristic": "Invalidation strategy",
        "description": "",
        "abbreviation": "INS-100",
        "example": "## Invalidation strategy refers to the technique used in cache management where cached data is marked as invalid or stale when it becomes outdated or inconsistent with the underlying data source.\n\n ### Issue: In this example, a RESTful API serving product data relies on a caching layer to optimize read performance. However, there is no invalidation mechanism in place, leading to the potential issue of outdated product data being returned after an update in the database. This could cause problems such as showing incorrect prices or inventory levels in an e-commerce system, leading to a poor user experience and operational issues.\n\n ### INS-100\n\n ### Severity: High\n\n\n```python\nproduct_cache = {}\n\ndef get_product(product_id):\n    if product_id in product_cache:\n        return product_cache[product_id]\n    else:\n        product_data = fetch_from_database(product_id)  # Simulated DB call\n        product_cache[product_id] = product_data\n        return product_data\n\ndef update_product(product_id, new_data):\n    update_database(product_id, new_data)  # Updates product data in the database\n    # Missing cache invalidation here\n```\n\n ### Solution: To resolve this issue, we should implement a proper invalidation strategy. This can be done by either removing the product from the cache upon updates or, better yet, updating the cache with the latest product data after successfully updating the database.\n\nImproved \n```python\nproduct_cache = {}\n\ndef get_product(product_id):\n    if product_id in product_cache:\n        return product_cache[product_id]\n    else:\n        product_data = fetch_from_database(product_id)  # Simulated DB call\n        product_cache[product_id] = product_data\n        return product_data\n\ndef update_product(product_id, new_data):\n    update_database(product_id, new_data)  # Updates product data in the database\n    # Invalidate cache or update it with new data\n    if product_id in product_cache:\n        product_cache[product_id] = new_data  # Update the cache with new data\n```",
    },
    {
        "characteristic": "Third-party Services and Libraries",
        "description": "",
        "abbreviation": "TPL-100",
        "example": '## Third-party services and libraries refer to external code, services, or APIs integrated into an application to extend its functionality or reduce development time. These components are significant in software scalability because they allow developers to offload functionality, such as authentication, payment processing, or data handling, to specialized providers without needing to build everything in-house. However, improper use of third-party services or lack of fallback strategies can lead to scalability issues when these services face downtime or performance bottlenecks.\n\n ### Issue: The current code makes a direct call to a third-party payment service without any checks for service failures, timeouts, or fallback mechanisms. If the service is slow or unavailable, users might experience payment failures or indefinite loading screens, leading to frustration and potential business loss.\n\n ### TPL-100\n\n ### Severity: Critical\n\n```python\nimport third_party_payment_gateway as payment_gateway\n\ndef process_payment(order_id, amount):\n    payment_response = payment_gateway.charge(order_id, amount)  # No error handling\n    if payment_response[\'status\'] == \'success\':\n        return "Payment successful"\n    return "Payment failed"\n```\n\n ### Solution: Implement retry logic, error handling, and a fallback mechanism to gracefully handle failures from the third-party service. This ensures that temporary service outages or latency spikes do not entirely disrupt the transaction process. You can also implement a queue system that retries payment processing later if the service is down.\n\n```python\nimport third_party_payment_gateway as payment_gateway\nimport time\n\ndef process_payment(order_id, amount, retries=3):\n    for attempt in range(retries):\n        try:\n            payment_response = payment_gateway.charge(order_id, amount)\n            if payment_response[\'status\'] == \'success\':\n                return "Payment successful"\n            else:\n                return "Payment failed"\n        except Exception as e:\n            if attempt < retries - 1:\n                time.sleep(2 ** attempt)  # Exponential backoff\n            else:\n                # Fallback mechanism or log failure\n                return f"Payment service is currently unavailable. Please try again later."\n```',
    },
    {
        "characteristic": "State Management",
        "description": "",
        "abbreviation": "STA-100",
        "example": "## State management refers to the way software keeps track of and updates the current state of variables, data, or application logic throughout the lifecycle of a program. It plays a crucial role in software scalability as proper management ensures that the application can handle growing demands, maintain consistency, and avoid bottlenecks. Poor state management can lead to performance issues, data inconsistencies, and difficulties in scaling the application to support more users or transactions.\n\n ### Issue: In this code, there is an improper handling of state in a web application's session management. The session data is stored in-memory, leading to a potential memory bottleneck as the number of concurrent users grows. This approach does not scale well, especially in a distributed environment where multiple servers handle requests.\n\n ### STA-100\n\n ### Severity: High\n\n```python\n# Session state stored in memory\nclass SessionManager:\n    sessions = {}  # stores session data in-memory\n\n    @staticmethod\n    def get_session(user_id):\n        return SessionManager.sessions.get(user_id)\n\n    @staticmethod\n    def set_session(user_id, session_data):\n        SessionManager.sessions[user_id] = session_data\n```\n\n ### Solution: The solution is to use a distributed caching mechanism like Redis to store session data. This allows the state to be shared across multiple servers and prevents memory issues as more users connect.\n\n```python\nimport redis\n\nclass SessionManager:\n    # Connect to Redis instance\n    redis_client = redis.StrictRedis(host='localhost', port=6379, db=0)\n\n    @staticmethod\n    def get_session(user_id):\n        return SessionManager.redis_client.get(user_id)\n\n    @staticmethod\n    def set_session(user_id, session_data):\n        SessionManager.redis_client.set(user_id, session_data)\n```",
    },
]


security = [
    {
        "characteristic": "Authentication",
        "description": "The primary objective is to ensure the **Authentication** criteria of security in the code. Examine the following criteria: Proper mechanisms to verify the identity of users, services, or systems. Ensuring secure password policies and storage (e.g., using bcrypt or Argon2 for password hashing). Implementing multi-factor authentication.",
        "abbreviation": "AUT-100",
        "example": "## Authentication is the process of verifying the identity of a user or system, ensuring that only authorized entities can access specific resources. It is crucial in software security to prevent unauthorized access and protect sensitive data.\n\n ### Issue: The application uses hard-coded credentials for user authentication, which can be easily compromised if the source code is exposed.\n\n ### AUT-100\n\n ### Severity: High\n\n```python\ndef authenticate_user(username, password):\n    if username == 'admin' and password == 'password123':\n        return True\n    else:\n        return False\n```\n\n ### Solution: Implement a secure authentication mechanism by storing credentials securely, such as in a database with hashed passwords, and avoid hard-coding sensitive information.\n\n```python\nimport bcrypt\n\ndef authenticate_user(username, password):\n    stored_hash = get_user_password_hash_from_db(username)\n    if stored_hash and bcrypt.checkpw(password.encode('utf-8'), stored_hash):\n        return True\n    else:\n        return False\n```",
    },
    {
        "characteristic": "Authorization",
        "description": "The primary objective is to ensure the **Authorization** criteria of security in the code. Examine the following criteria: Ensuring users and services have appropriate permissions to access resources. Implementing the principle of least privilege: giving only the necessary access rights to perform a function.",
        "abbreviation": "ATH-100",
        "example": "## Authorization is the process of determining whether an authenticated user has permission to access a specific resource or perform a particular action within a system. It is crucial for enforcing security policies and protecting sensitive data from unauthorized access or manipulation.\n\n ### Issue: The code lacks an authorization check, allowing any authenticated user to access and modify any user's data without proper permissions.\n\n ### ATH-100\n\n ### Severity: High\n\n```python\ndef edit_user_profile(request, user_id):\n    if request.method == 'POST':\n        user = User.objects.get(id=user_id)\n        user.profile_data = request.POST['profile_data']\n        user.save()\n        return HttpResponse('Profile updated.')\n```\n\n ### Solution: Implement an authorization check to ensure that only authorized users can edit the profile data. This can be done by verifying that the requesting user matches the profile owner or has administrative privileges.\n\n```python\ndef edit_user_profile(request, user_id):\n    if request.method == 'POST':\n        if request.user.id != user_id and not request.user.is_admin:\n            return HttpResponseForbidden('You are not authorized to edit this profile.')\n        user = User.objects.get(id=user_id)\n        user.profile_data = request.POST['profile_data']\n        user.save()\n        return HttpResponse('Profile updated.')\n```",
    },
    {
        "characteristic": "Session Management",
        "description": "The primary objective is to ensure the **Session Management** criteria of security in the code. Examine the following criteria: Securely handling and storing session tokens. Implementing session timeouts and ensuring session IDs are unpredictable. It is crucial to thoroughly examine the entire code line by line and take your time before presenting any analysis.",
        "abbreviation": "SES-100",
        "example": "## Session Management is the process of securely handling user sessions in a web application to maintain the state and continuity of a user's interaction. It is significant in software security because improper session management can lead to vulnerabilities like session hijacking, unauthorized access, and data breaches.\n\n ### Issue: The application exposes session IDs in URLs and does not invalidate sessions upon logout, making it vulnerable to session fixation and hijacking attacks.\n\n ### SES-100\n\n ### Severity: High\n\n```python\n# Insecure session handling\ndef login(request):\n    user = authenticate(request.form['username'], request.form['password'])\n    if user:\n        session_id = generate_session_id()\n        sessions[session_id] = user\n        return redirect(f\"/dashboard?session_id={session_id}\")\n    else:\n        return \"Login failed\"\n\ndef dashboard(request):\n    session_id = request.args.get('session_id')\n    user = sessions.get(session_id)\n    if user:\n        return f\"Welcome, {user.name}\"\n    else:\n        return redirect('/login')\n\ndef logout(request):\n    # Session not invalidated\n    return redirect('/login')\n```\n\n ### Solution: Use secure cookies to store session IDs (with HttpOnly and Secure flags) and properly invalidate sessions upon logout to prevent unauthorized access.\n\n```python\n# Secure session handling\ndef login(request):\n    user = authenticate(request.form['username'], request.form['password'])\n    if user:\n        session_id = generate_secure_session_id()\n        sessions[session_id] = user\n        response = make_response(redirect(\"/dashboard\"))\n        response.set_cookie(\"session_id\", session_id, httponly=True, secure=True)\n        return response\n    else:\n        return \"Login failed\"\n\ndef dashboard(request):\n    session_id = request.cookies.get('session_id')\n    user = sessions.get(session_id)\n    if user:\n        return f\"Welcome, {user.name}\"\n    else:\n        return redirect('/login')\n\ndef logout(request):\n    session_id = request.cookies.get('session_id')\n    sessions.pop(session_id, None)\n    response = make_response(redirect('/login'))\n    response.set_cookie('session_id', '', expires=0)\n    return response\n```",
    },
    {
        "characteristic": "Cryptography",
        "description": "The primary objective is to ensure the **Cryptography** criteria of security in the code. Examine the following criteria: Using up-to-date libraries and algorithms for encryption and decryption. Safely managing and storing cryptographic keys.",
        "abbreviation": "CRY-100",
        "example": "## Cryptography is the practice of securing information by transforming it to prevent unauthorized access. In web development, it is crucial for protecting sensitive data such as user passwords, ensuring user privacy, and maintaining trust in the application.\n\n ### Issue: In a web application, passwords are being stored in plain text within the database. This poses a severe security risk because if the database is compromised, all user passwords are exposed.\n\n ### CRY-100\n\n ### Severity: Critical\n\n```python\n\ndef register_user(username, password):\n    # Insecure: Storing passwords in plain text\n    database.save({'username': username, 'password': password})\n```\n\n ### Solution: Implement password hashing using a strong algorithm like bcrypt before storing passwords. This ensures that even if the database is compromised, the actual passwords remain protected.\n\n```python\nimport bcrypt\n\ndef register_user(username, password):\n    # Secure: Hashing the password before storing\n    hashed_password = bcrypt.hashpw(password.encode('utf-8'), bcrypt.gensalt())\n    database.save({'username': username, 'password': hashed_password})\n```",
    },
    {
        "characteristic": "Cross-Site Scripting (XSS)",
        "description": "The primary objective is to ensure the **Cross-Site Scripting (XSS)** criteria of security in the code. Examine the following criteria: Validating and escaping output to prevent malicious scripts from running in the context of a web page.",
        "abbreviation": "XSS-100",
        "example": " ## Cross-Site Scripting (XSS) is a security vulnerability that allows attackers to inject malicious scripts into web applications viewed by other users. This can lead to data theft, session hijacking, and compromise of user accounts, making it a critical concern in software security.\n\n ### Issue: The application directly incorporates user input into the HTML response without proper sanitization, making it susceptible to XSS attacks.\n\n ### XSS-100\n\n ### Severity: High\n\n```python\nfrom flask import Flask, request\n\napp = Flask(__name__)\n\n@app.route('/search')\ndef search():\n    query = request.args.get('query', '')\n    return f\"<div>Results for: {query}</div>\"\n```\n\n ### Solution: Sanitize or escape user input before rendering it in the HTML response to prevent the execution of malicious scripts.\n\n```python\nfrom flask import Flask, request\nimport html\n\napp = Flask(__name__)\n\n@app.route('/search')\ndef search():\n    query = request.args.get('query', '')\n    safe_query = html.escape(query)\n    return f\"<div>Results for: {safe_query}</div>\"\n```",
    },
    {
        "characteristic": "Cross-Site Request Forgery (CSRF)",
        "description": "The primary objective is to ensure the **Cross-Site Request Forgery (CSRF)** criteria of security in the code. Examine the following criteria: Implementing anti-CSRF tokens in web applications to prevent unauthorized actions on behalf of an authenticated user.",
        "abbreviation": "CSR-100",
        "example": "## Cross-Site Request Forgery (CSRF) is an attack that tricks a user into executing unwanted actions on a web application in which they are authenticated. It exploits the trust a web application has in a user's browser, allowing malicious commands to be transmitted without the user's consent, potentially leading to unauthorized transactions or data compromise.\n\n ### Issue: The application lacks CSRF protection on sensitive endpoints, allowing attackers to perform unauthorized actions on behalf of authenticated users.\n\n ### CSR-100\n\n ### Severity: High\n\n```python\nfrom flask import Flask, request\n\napp = Flask(__name__)\n\n@app.route('/transfer_funds', methods=['POST'])\ndef transfer_funds():\n    amount = request.form['amount']\n    recipient = request.form['recipient']\n    # Code to transfer funds\n    return 'Funds transferred successfully'\n```\n\n ### Solution: Implement CSRF protection by incorporating a unique token in forms that is validated on the server side. This ensures that requests are intentional and originated from the authenticated user.\n\n```python\nfrom flask import Flask, request, session, render_template_string\nimport secrets\n\napp = Flask(__name__)\napp.secret_key = 'your_secret_key'\n\n@app.route('/transfer_funds', methods=['GET', 'POST'])\ndef transfer_funds():\n    if request.method == 'GET':\n        csrf_token = secrets.token_hex(16)\n        session['csrf_token'] = csrf_token\n        return render_template_string('''\n            <form method=\"POST\" action=\"/transfer_funds\">\n                <input type=\"number\" name=\"amount\" required>\n                <input type=\"text\" name=\"recipient\" required>\n                <input type=\"hidden\" name=\"csrf_token\" value=\"{{ csrf_token }}\">\n                <input type=\"submit\" value=\"Transfer Funds\">\n            </form>\n        ''', csrf_token=csrf_token)\n    else:\n        submitted_token = request.form.get('csrf_token')\n        if not submitted_token or submitted_token != session.get('csrf_token'):\n            return 'Invalid CSRF token', 400\n        amount = request.form['amount']\n        recipient = request.form['recipient']\n        # Code to transfer funds\n        return 'Funds transferred successfully'\n```",
    },
    {
        "characteristic": "File and Resource Access",
        "description": "Setting and handling timeouts for operations, especially external calls, to ensure the system doesn't hang indefinitely.",
        "abbreviation": "FIL-100",
        "example": "## File and Resource Access refers to how software interacts with files and system resources, which is critical for maintaining data security and preventing unauthorized access or manipulation of sensitive information.\n\n ### Issue: The code accepts user input to determine which file to open without proper validation, leading to a path traversal vulnerability. An attacker can exploit this to access unauthorized files on the system.\n\n ### FIL-100\n\n ### Severity: High\n\n```python\nfilename = input(\"Enter the filename: \")\nwith open(filename, 'r') as f:\n    data = f.read()\n    print(data)\n```\n\n ### Solution: Implement input validation to restrict file access to a specific directory and sanitize the filename to prevent path traversal.\n\n```python\nimport os\n\nfilename = input(\"Enter the filename: \")\nsafe_dir = '/safe_directory/'\nfile_path = os.path.join(safe_dir, os.path.basename(filename))\n\nwith open(file_path, 'r') as f:\n    data = f.read()\n    print(data)\n```",
    },
    {
        "characteristic": "Code Injection",
        "description": "Checks should only be applied when **raw queries**, **dynamic code execution**, or direct **string concatenation** are used for command or query construction—not for ORM-based. if no raw query attacks or script attacks are present, then this check can be skipped.",
        "abbreviation": "COD-100",
        "example": "## Code Injection is a security vulnerability where an attacker injects malicious code into an application, altering its execution flow. This can lead to unauthorized access, data leakage, or complete system compromise, making it a critical concern in software security.\n\n ### Issue: The application constructs SQL queries using unsanitized user input in a financial services system, allowing attackers to execute arbitrary SQL commands on the database.\n\n ### COD-100\n\n ### Severity: Critical\n\n```python\nimport sqlite3\n\ndef get_account_balance(account_id):\n    conn = sqlite3.connect('bank.db')\n    cursor = conn.cursor()\n    query = \"SELECT balance FROM accounts WHERE account_id = '%s'\" % account_id\n    cursor.execute(query)\n    result = cursor.fetchone()\n    conn.close()\n    return result[0]\n```\n\n ### Solution: Use parameterized queries to prevent code injection by properly sanitizing user input.\n\n```python\nimport sqlite3\n\ndef get_account_balance(account_id):\n    conn = sqlite3.connect('bank.db')\n    cursor = conn.cursor()\n    query = \"SELECT balance FROM accounts WHERE account_id = ?\"\n    cursor.execute(query, (account_id,))\n    result = cursor.fetchone()\n    conn.close()\n    return result[0]\n```",
    },
    {
        "characteristic": "Input Validation",
        "description": "Do not raise issues for basic input checks like **format**, **primitive type**, or **length** unless missing them causes crashes, buffer overflows, or memory corruption. if basic checks are already presents, then this check can be skipped.",
        "abbreviation": "INP-100",
        "example": '## Input Validation is the process of ensuring that user-provided data is correct, safe, and appropriate before processing it. It is significant in software security because improper input validation can lead to vulnerabilities like injection attacks, buffer overflows, and unauthorized access.\n\n ### Issue: The code accepts user input without validation, allowing potential execution of malicious code or access to unintended resources.\n\n ### INP-100\n\n ### Severity: High\n\n```python\nuser_input = input("Enter your username: ")\nprint("Welcome, " + user_input)\n```\n\n ### Solution: Implement input validation to ensure the input meets expected criteria, such as length and allowed characters, preventing injection attacks.\n\n```python\nimport re\n\nuser_input = input("Enter your username: ")\nif re.match("^[A-Za-z0-9_]{3,20}$", user_input):\n    print("Welcome, " + user_input)\nelse:\n    print("Invalid username. Please use 3-20 alphanumeric characters and underscores only.")\n```',
    },
]

power_analysis_small = [
    {
        "characteristic": "Comprehensive Power Analysis",
        "description": "This analysis investigates the maintainability, readability, and critical runtime errors in the code. It aims to identify areas where code structure, logic, and design can be improved for better performance, and long-term maintainability.",
        "abbreviation": "ANA-100",
        "weight": 1,
        "example": '## Comprehensive Power analysis involves identifying critical runtime issues, improving code structure, and ensuring stability for better performance and maintainability.\n\n ### Issue: The code responsible for calculating the average may face a division by zero error if the count variable is either zero or undefined. This situation can arise due to several factors, such as improper assignment of the count variable, failure to initialize it correctly, or unexpected data conditions that result in a zero or undefined value. Consequently, this issue can lead to a runtime crash, potentially terminating the program, or produce inaccurate results, compromising the integrity of the calculation. Implementing appropriate validation checks for the count variable before performing the division can help mitigate this risk.\n\n ### ANA-100\n\n ### Severity: Critical\n\n```python\ndef calculate_average(total, count):\n    if count == 0:\n        print("Warning: Count is zero. Returning zero to avoid division error.")\n        return 0\n    return total / count\n```\n\n ### Solution: To ensure the code handles zero values safely, it is essential to implement a conditional check before performing the division operation. By verifying that the count variable is neither zero nor undefined, the code can gracefully manage potential edge cases.\n\n```python\ndef calculate_average_safe(total, count):\n    return total / count if count else 0\n```',
    },
]

power_analysis = [
    {
        "characteristic": "Modularity",
        "description": "",
        "abbreviation": "MOD-100",
        "weight": 1,
        "example": "## Modularity refers to the practice of dividing a software system into distinct, independent modules that each handle a specific functionality. This allows for better maintainability, as modular code is easier to read, debug, and test. Additionally, changes to one module do not heavily impact other parts of the system, promoting flexibility and scalability.\n\n ### Issue: The given function handles too many responsibilities, violating the Single Responsibility Principle (SRP), which is a key aspect of modularity. The function performs data fetching, data validation, and data transformation all within the same block of code.\n\n ### MOD-100\n\n ### Severity: High\n\n\n```python\ndef process_data_and_save(url, filename):\n    # Fetch data from URL\n    response = requests.get(url)\n    data = response.json()\n\n    # Validate data\n    if not isinstance(data, list):\n        raise ValueError(\"Data should be a list\")\n\n    # Process data\n    processed_data = [item['value'] for item in data if 'value' in item]\n\n    # Save processed data\n    with open(filename, 'w') as file:\n        json.dump(processed_data, file)\n```\n\n ### Solution: Break down the function into smaller, modular functions where each function focuses on a single responsibility: fetching data, validating data, processing data, and saving data. This will improve readability, testability, and overall maintainability.\n\n:\n```python\ndef fetch_data(url):\n    response = requests.get(url)\n    return response.json()\n\ndef validate_data(data):\n    if not isinstance(data, list):\n        raise ValueError(\"Data should be a list\")\n    return data\n\ndef process_data(data):\n    return [item['value'] for item in data if 'value' in item]\n\ndef save_data(filename, data):\n    with open(filename, 'w') as file:\n        json.dump(data, file)\n\ndef process_data_and_save(url, filename):\n    data = fetch_data(url)\n    validated_data = validate_data(data)\n    processed_data = process_data(validated_data)\n    save_data(filename, processed_data)\n```\n",
    },
    {
        "characteristic": "Resource Utilization",
        "description": "Evaluate how efficiently the code uses memory, CPU, and other system resources. Flag issues like unbounded loops, large data allocations, redundant computations, or failure to release resources. Do not flag input validation issues unless they directly lead to excessive or uncontrolled resource usage.",
        "abbreviation": "RES-100",
        "weight": 1,
        "example": "## Resource Utilization refers to the efficient and effective handling of system resources such as memory, files, and network connections during software execution. Proper resource utilization is crucial for software reliability because it prevents resource leaks, ensures optimal performance, and avoids system crashes or unexpected behavior.\n\n ### Issue: The code opens a file but does not close it after reading, leading to a potential file descriptor leak.\n\n ### RES-100\n\n ### Severity: High\n\n\n```python\ndef read_data(file_path):\n    file = open(file_path, 'r')\n    data = file.read()\n    return data\n```\n\n ### Solution: Use a context manager to automatically handle the opening and closing of the file.\n\n :\n```python\ndef read_data(file_path):\n    with open(file_path, 'r') as file:\n        data = file.read()\n    return data\n```",
    },
    {
        "characteristic": "Exception Handling",
        "description": "",
        "abbreviation": "EXC-100",
        "weight": 1,
        "example": '## Exception Handling is a programming practice that allows developers to manage runtime errors gracefully, enhancing software resilience by preventing crashes and maintaining consistent application behavior.\n\n ### Issue: The code does not handle exceptions that may occur during a database transaction in a financial application, potentially leading to data inconsistency or system crashes.\n\n ### EXC-100\n\n ### Severity: Critical\n\n```python\ndef process_transaction(account_id, amount):\n    balance = get_account_balance(account_id)\n    new_balance = balance - amount\n    update_account_balance(account_id, new_balance)\n    log_transaction(account_id, amount)\n```\n\n ### Solution: Introduce exception handling to ensure any errors lead to a rollback, maintaining data integrity.\n\n```python\ndef process_transaction(account_id, amount):\n    try:\n        balance = get_account_balance(account_id)\n        new_balance = balance - amount\n        update_account_balance(account_id, new_balance)\n        log_transaction(account_id, amount)\n    except Exception as e:\n        rollback_transaction()\n        log_error(f"Transaction failed for account {account_id}: {e}")\n        raise\n```',
    },
    {
        "characteristic": "Monitoring and Logging",
        "description": "",
        "abbreviation": "MOL-100",
        "weight": 1,
        "example": "## Monitoring and Logging are practices that involve tracking an application's performance and recording significant events or errors. They are significant in software resilience because they enable early detection of issues, facilitate troubleshooting, and ensure the system remains reliable and available.\n\n ### Issue: In an industrial automation system, the code responsible for controlling machinery operations lacks proper logging of errors and events. This absence makes it difficult to monitor system health or diagnose problems, potentially leading to unnoticed failures and costly production downtime.\n\n ### MON-100\n\n ### Severity: High\n\n```python\ndef control_machine(operation):\n    result = machine.execute(operation)\n    if result:\n        return True\n    else:\n        # No logging implemented\n        return False\n```\n\n ### Solution: Implement comprehensive logging to record both successful operations and failures. This enhances software resilience by allowing real-time monitoring and quicker diagnosis of issues, thereby minimizing downtime and maintaining production efficiency.\n\n```python\nimport logging\n\ndef control_machine(operation):\n    result = machine.execute(operation)\n    if result:\n        logging.info(f\"Operation '{operation}' executed successfully.\")\n        return True\n    else:\n        logging.error(f\"Failed to execute operation '{operation}'.\")\n        return False\n```",
    },
    {
        "characteristic": "Dependency Injection",
        "description": "Dependency Injection involves supplying dependencies from outside to a class or function rather than creating them internally. Look for adherence to design best practices such as inversion of control, depending on abstractions, injecting dependencies through parameters, constructors or framework mechanisms, and avoiding hardcoded or tightly coupled implementations. This promotes flexibility, testability, and separation of concerns across various programming languages and frameworks.",
        "abbreviation": "DEP-200",
        "weight": 1,
        "example": "## Dependency Injection is a design principle where an object receives its dependencies from an external source rather than creating them internally. This promotes loose coupling, testability, and adherence to the SOLID principles, especially the Dependency Inversion Principle.\n\n ### Issue: A service class directly instantiates a database client within its methods, making it tightly coupled and difficult to mock or replace during testing.\n\n ### DEP-200\n\n ### Severity: Moderate\n\n```python\nclass UserService:\n    def __init__(self):\n        pass\n\n    def get_user_data(self, user_id):\n        db_client = DatabaseClient()\n        return db_client.fetch_user(user_id)\n```\n\n ### Solution: Inject the `DatabaseClient` dependency via the constructor to allow for better testability and decoupling.\n\n```python\nclass UserService:\n    def __init__(self, db_client):\n        self.db_client = db_client\n\n    def get_user_data(self, user_id):\n        return self.db_client.fetch_user(user_id)\n```"
    },
    {
        "characteristic": "Code Injection",
        "description": "Checks should only be applied when **raw queries**, **dynamic code execution**, or direct **string concatenation** are used for command or query construction—not for ORM-based. if no raw query attacks or script attacks are present, then this check can be skipped.",
        "abbreviation": "COD-100",
        "weight": 1,
        "example": "## Code Injection is a security vulnerability where an attacker injects malicious code into an application, altering its execution flow. This can lead to unauthorized access, data leakage, or complete system compromise, making it a critical concern in software security.\n\n ### Issue: The application constructs SQL queries using unsanitized user input in a financial services system, allowing attackers to execute arbitrary SQL commands on the database.\n\n ### COD-100\n\n ### Severity: Critical\n\n```python\nimport sqlite3\n\ndef get_account_balance(account_id):\n    conn = sqlite3.connect('bank.db')\n    cursor = conn.cursor()\n    query = \"SELECT balance FROM accounts WHERE account_id = '%s'\" % account_id\n    cursor.execute(query)\n    result = cursor.fetchone()\n    conn.close()\n    return result[0]\n```\n\n ### Solution: Use parameterized queries to prevent code injection by properly sanitizing user input.\n\n```python\nimport sqlite3\n\ndef get_account_balance(account_id):\n    conn = sqlite3.connect('bank.db')\n    cursor = conn.cursor()\n    query = \"SELECT balance FROM accounts WHERE account_id = ?\"\n    cursor.execute(query, (account_id,))\n    result = cursor.fetchone()\n    conn.close()\n    return result[0]\n```",
    },
    {
        "characteristic": "Input Validation",
        "description": "Do not raise issues for basic input checks like **format**, **primitive type**, or **length** unless missing them causes crashes, buffer overflows, or memory corruption. if basic checks are already presents, then this check can be skipped.",
        "abbreviation": "INP-100",
        "weight": 1,
        "example": '## It is significant in software security because improper input validation can lead to vulnerabilities like buffer overflows, null pointer dereferences, and unauthorized access.\n\n### Issue: The code accepts unchecked input, which may lead to a null pointer dereference or buffer overflow in systems-level programming. For example, assuming non-null strings or exceeding buffer limits can crash the application or lead to arbitrary code execution.\n\n### INP-100\n\n### Severity: High\n\n```c\n#include <stdio.h>\n#include <string.h>\n\nvoid greet(char *name) {\n    char buffer[20];\n    strcpy(buffer, name); // Unsafe: no length check\n    printf("Welcome, %s\\n", buffer);\n}\n\nint main() {\n    char *user_input = NULL;\n    // Simulating a potential null pointer scenario\n    greet(user_input); // Leads to crash (null dereference)\n    return 0;\n}\n```\n\n### Solution: Validate input pointers and enforce buffer size limits to prevent memory issues.\n\n```c\n#include <stdio.h>\n#include <string.h>\n\nvoid greet(const char *name) {\n    if (name == NULL) {\n        printf("Error: null input provided.\\n");\n        return;\n    }\n    if (strlen(name) >= 20) {\n        printf("Error: name too long.\\n");\n        return;\n    }\n    char buffer[20];\n    strncpy(buffer, name, sizeof(buffer) - 1);\n    buffer[19] = \'\\0\';\n    printf("Welcome, %s\\n", buffer);\n}\n\nint main() {\n    char input[100];\n    printf("Enter your name: ");\n    if (fgets(input, sizeof(input), stdin)) {\n        input[strcspn(input, "\\n")] = 0; // Remove newline\n        greet(input);\n    }\n    return 0;\n}\n```',
    },
]


owasp = [
    {
        "characteristic": "OWASP A01:2021 Broken Access Control",
        "description": "",
        "abbreviation": "BAC-100",
        "example": '# OWASP A01:2021 Broken Access Control\n ## Broken Access Control occurs when an application fails to enforce proper restrictions on what authenticated users can do. Attackers exploit these flaws to access unauthorized functionality or data, such as viewing sensitive files, modifying others’ data, or escalating privileges.\n\n ### Issue: The function `is_admin_user` does not properly enforce access control, allowing unauthorized users to perform admin-only actions.\n\n ### BAC-100\n\n ### Severity: High\n\n```python\ndef is_admin_user(user_id):\n    user = get_user_by_id(user_id)\n    if user:\n        return True  # Incorrect: This grants admin access to any valid user\n    return False\n\ndef delete_user(target_user_id, user_id):\n    if is_admin_user(user_id):\n        delete_user_from_db(target_user_id)\n        print("User deleted successfully.")\n    else:\n        print("Access denied: Admin privileges required.")\n```\n\n### Solution: Implement proper role-based access control (RBAC) by checking the user’s role explicitly.\n\n```python\ndef is_admin_user(user_id):\n    user = get_user_by_id(user_id)\n    return user and user.get(\'role\') == \'admin\'\n\ndef delete_user(target_user_id, user_id):\n    if is_admin_user(user_id):\n        delete_user_from_db(target_user_id)\n        print("User deleted successfully.")\n    else:\n        print("Access denied: Admin privileges required.")\n```\n',
    },
    {
        "characteristic": "OWASP A02:2021 Cryptographic Failures",
        "description": "",
        "abbreviation": "CRY-100",
        "example": '# OWASP A02:2021 Cryptographic Failures\n ## Cryptographic failures occur when sensitive data is inadequately protected due to flaws in encryption, hashing, or key management.\n\n ### Issue: The function `store_password` uses an insecure hashing algorithm (MD5) for password storage, making it vulnerable to brute-force attacks.\n\n ### CRY-100\n\n ### Severity: High\n\n```python\nimport hashlib\n\ndef store_password(password):\n    hashed_password = hashlib.md5(password.encode()).hexdigest()  # Insecure: MD5 is weak and easily cracked\n    save_to_database(hashed_password)\n    print("Password stored successfully.")\n```\n\n### Solution: Use a strong password hashing algorithm like bcrypt or Argon2 to securely store passwords.\n\n```python\nfrom bcrypt import hashpw, gensalt\n\ndef store_password(password):\n    salt = gensalt()\n    hashed_password = hashpw(password.encode(), salt)\n    save_to_database(hashed_password)\n    print("Password stored securely.")\n```\n',
    },
    {
        "characteristic": "OWASP A03:2021 Injection",
        "description": "",
        "abbreviation": "INJ-100",
        "example": "# OWASP A03:2021 Injection\n ## Injection vulnerabilities occur when an attacker can send untrusted data to an interpreter, such as a SQL query, command, or other types of queries, which can result in the execution of malicious commands.\n\n ### Issue: The function `execute_query` allows unsanitized user input to be directly passed to a SQL query, making it vulnerable to SQL injection attacks.\n\n ### INJ-100\n\n ### Severity: High\n\n```python\nimport sqlite3\n\ndef execute_query(user_input):\n    conn = sqlite3.connect('example.db')\n    cursor = conn.cursor()\n    query = f\"SELECT * FROM users WHERE username = '{user_input}'\"  # Insecure: User input is directly inserted into the query\n    cursor.execute(query)\n    result = cursor.fetchall()\n    conn.close()\n    print(result)\n```\n\n### Solution: Use parameterized queries to safely handle user input and avoid SQL injection attacks.\n\n```python\nimport sqlite3\n\ndef execute_query(user_input):\n    conn = sqlite3.connect('example.db')\n    cursor = conn.cursor()\n    query = \"SELECT * FROM users WHERE username = ?\"  # Secure: Using parameterized queries\n    cursor.execute(query, (user_input,))\n    result = cursor.fetchall()\n    conn.close()\n    print(result)\n```",
    },
    {
        "characteristic": "OWASP A04:2021 Insecure Design",
        "description": "",
        "abbreviation": "IND-100",
        "example": "# OWASP A04:2021 Insecure Design\n ## Insecure Design refers to weaknesses in the design phase that can lead to security vulnerabilities in the application or system. This includes issues like poor threat modeling, insecure architecture, and lack of security controls.\n\n ### Issue: The web application uses a simplistic and inadequate authentication mechanism, which doesn't include multi-factor authentication (MFA), making it easier for attackers to gain unauthorized access.\n\n ### IND-102\n\n ### Severity: High\n\n```python\n# Insecure design: Simple username and password authentication\n\ndef authenticate_user(username, password):\n    if username == 'admin' and password == 'password123':\n        print('Authentication successful')\n    else:\n        print('Authentication failed')\n```\n\n### Solution: Implement multi-factor authentication (MFA) to enhance security and reduce the risk of unauthorized access.\n\n```python\n# Secure design: Implementing multi-factor authentication (MFA)\n\ndef authenticate_user(username, password, otp):\n    if username == 'admin' and password == 'password123' and verify_otp(otp):\n        print('Authentication successful')\n    else:\n        print('Authentication failed')\n\ndef verify_otp(otp):\n    # Implement OTP verification logic\n    return otp == '123456'  # Placeholder verification\n```",
    },
    {
        "characteristic": "OWASP A05:2021 Security Misconfiguration",
        "description": "",
        "abbreviation": "SEM-100",
        "example": "# OWASP A05:2021 Security Misconfiguration\n ## Security Misconfiguration occurs when security settings are improperly configured or not securely applied in an application, system, or server. This can lead to vulnerabilities such as excessive permissions, unnecessary services running, or exposing sensitive information.\n\n ### Issue: The web application is running in production with debugging enabled, exposing sensitive information such as stack traces and configuration details to users.\n\n ### SEM-100\n\n ### Severity: High\n\n```python\n# Insecure configuration: Debugging enabled in production\n\nimport os\n\ndef check_debug_mode():\n    if os.environ.get('DEBUG') == 'True':\n        print('Debugging enabled - This should never be in production!')\n    else:\n        print('Production environment - Debugging disabled')\n```\n\n### Solution: Ensure debugging is disabled in production and sensitive information like stack traces is not exposed.\n\n```python\n# Secure configuration: Disabling debugging in production\n\nimport os\n\ndef check_debug_mode():\n    if os.environ.get('DEBUG') == 'True':\n        print('Debugging enabled - Should only be for development environment')\n    else:\n        print('Production environment - Debugging disabled')\n\n# Properly configure production environment to disable debugging\n# and ensure no sensitive information is exposed.\n```\n",
    },
    {
        "characteristic": "OWASP A07:2021 Identification and Authentication Failures",
        "description": "",
        "abbreviation": "IAF-100",
        "example": "# OWASP A07:2021 Identification and Authentication Failures\n ## Identification and Authentication Failures occur when there are flaws in the implementation of authentication or session management mechanisms, leading to unauthorized access or session hijacking.\n\n ### Issue: The web application does not implement account lockout or throttling after multiple failed login attempts, making it vulnerable to brute-force attacks.\n\n ### IAF-100\n\n ### Severity: High\n\n```python\n# Insecure authentication: No account lockout after multiple failed login attempts\n\ndef login(username, password):\n    stored_password = get_stored_password(username)\n    if stored_password == password:\n        print('Login successful')\n    else:\n        print('Login failed')\n\n# This implementation is vulnerable to brute-force attacks, where an attacker can repeatedly attempt to guess the password without any consequences.\n``` \n\n### Solution: Implement account lockout or throttling after a certain number of failed login attempts to prevent brute-force attacks.\n\n```python\n# Secure authentication: Implementing account lockout after multiple failed login attempts\n\nimport time\n\ndef login(username, password):\n    failed_attempts = get_failed_attempts(username)\n    if failed_attempts >= 5:\n        print('Account locked due to multiple failed login attempts. Please try again later.')\n        return\n    stored_password = get_stored_password(username)\n    if stored_password == password:\n        reset_failed_attempts(username)\n        print('Login successful')\n    else:\n        increment_failed_attempts(username)\n        print('Login failed')\n\n# This approach prevents brute-force attacks by limiting the number of login attempts and locking the account after repeated failures.\n```\n",
    },
    {
        "characteristic": "OWASP A08:2021 Software and Data Integrity Failures",
        "description": "",
        "abbreviation": "SDI-100",
        "example": "# OWASP A08:2021 Software and Data Integrity Failures\n ## Software and Data Integrity Failures occur when software or data is tampered with, or when software components fail to ensure the integrity and authenticity of critical systems, leading to issues like unauthorized access, data corruption, or compromise of integrity.\n\n ### Issue: The application relies on an insecure method of validating code updates, making it possible for an attacker to inject malicious code into a software update, compromising the system.\n\n ### SDI-100\n\n ### Severity: High\n\n```python\n# Insecure update mechanism: Using an insecure method to download and validate code updates\n\nimport requests\n\ndef download_update(update_url):\n    response = requests.get(update_url)\n    # Vulnerable: No validation of the integrity of the downloaded update\n    if response.status_code == 200:\n        exec(response.text)  # Directly executing the downloaded code\n        print('Update applied successfully!')\n    else:\n        print('Failed to download update')\n```\n\n### Solution: Use secure methods for validating the integrity and authenticity of updates, such as cryptographic signatures and secure download protocols.\n\n```python\n# Secure update mechanism: Using a cryptographic signature to validate the authenticity of the update\n\nimport hashlib\nimport requests\n\ndef download_update(update_url, expected_signature):\n    response = requests.get(update_url)\n    # Secure: Verify the integrity of the update using its cryptographic signature\n    update_signature = hashlib.sha256(response.content).hexdigest()\n    if update_signature == expected_signature:\n        exec(response.text)  # Safe to execute\n        print('Update applied securely!')\n    else:\n        print('Update integrity check failed')\n```",
    },
    {
        "characteristic": "OWASP A09:2021 Security Logging and Monitoring Failures",
        "description": "",
        "abbreviation": "SLM-100",
        "example": "# OWASP A09:2021 Security Logging and Monitoring Failures\n ## Security Logging and Monitoring Failures occur when insufficient or ineffective logging, monitoring, or alerting is implemented in an application, leading to delayed detection of security incidents or inability to trace malicious activity.\n\n ### Issue: The application does not log important security events, such as failed login attempts or unusual user behavior, making it difficult to detect and respond to attacks.\n\n ### SLM-100\n\n ### Severity: High\n\n```python\n# Insecure logging: No logging of failed login attempts or suspicious activity\n\nimport time\n\n# No logs for failed login attempts\n\ndef login(username, password):\n    stored_password = get_stored_password(username)\n    if stored_password == password:\n        print('Login successful')\n    else:\n        print('Login failed')  # No logging of the failed attempt\n``` \n\n### Solution: Implement comprehensive logging and monitoring to track security events such as failed logins, suspicious behavior, and other potentially malicious activities.\n\n```python\n# Secure logging: Logging failed login attempts and other security events\n\nimport logging\n\n# Set up logging configuration\nlogging.basicConfig(filename='security_log.txt', level=logging.INFO)\n\ndef login(username, password):\n    stored_password = get_stored_password(username)\n    if stored_password == password:\n        print('Login successful')\n    else:\n        logging.warning(f'Failed login attempt for user: {username} at {time.ctime()}')  # Log failed login attempt\n        print('Login failed')\n\n# This approach helps track suspicious activities and allows for better detection of attacks.\n```\n",
    },
    {
        "characteristic": "OWASP A10:2021 Server-Side Request Forgery (SSRF)",
        "description": "",
        "abbreviation": "SSR-100",
        "example": "# OWASP A10:2021 Server-Side Request Forgery (SSRF)\n ## Server-Side Request Forgery (SSRF) occurs when an attacker can send crafted requests from the server to internal or external resources, potentially accessing sensitive information, internal services, or bypassing security controls.\n\n ### Issue: The application accepts a URL as user input and sends a request to that URL without proper validation, allowing an attacker to initiate requests to internal services or unauthorized external resources.\n\n ### SSR-100\n\n ### Severity: High\n\n```python\n# Insecure code: Vulnerable to SSRF due to unvalidated user input\n\nimport requests\n\ndef fetch_data_from_url(user_url):\n    response = requests.get(user_url)\n    return response.text\n\n# This code does not validate or sanitize the user-provided URL, allowing attackers to make requests to internal services or unauthorized resources.\n``` \n\n### Solution: Validate and sanitize user-provided URLs to ensure that they are not malicious or pointing to internal services or resources.\n\n```python\n# Secure code: Validate and sanitize URLs to prevent SSRF attacks\n\nimport requests\nfrom urllib.parse import urlparse\n\ndef fetch_data_from_url(user_url):\n    # Validate the URL to ensure it points to a safe external resource\n    parsed_url = urlparse(user_url)\n    if parsed_url.scheme not in ['http', 'https']:\n        raise ValueError('Invalid URL scheme, only HTTP/HTTPS allowed.')\n    if 'localhost' in parsed_url.hostname or '127.0.0.1' in parsed_url.hostname:\n        raise ValueError('Access to internal resources is forbidden.')\n    response = requests.get(user_url)\n    return response.text\n\n# This approach ensures that only safe external URLs are allowed, mitigating the risk of SSRF.\n```",
    },
]

soc2 = [
{
  "characteristic": "Input Validation",
  "description": "Ensures user-supplied data is properly validated, sanitized, and escaped to prevent injection attacks or unauthorized data manipulation.",
  "abbreviation": "INP-100",
  "example": "## SOC 2 requires controls to protect systems from unauthorized access and data manipulation. Failing to validate input properly can result in injection attacks, allowing attackers to manipulate queries, execute commands, or corrupt sensitive data.\n\n ### Issue: The function `get_user_info` directly interpolates user input into an SQL query without validation or parameterization, making it vulnerable to SQL injection.\n\n ### INP-100\n\n ### Severity: High\n\n```python\nimport sqlite3\n\ndef get_user_info(username):\n    conn = sqlite3.connect('users.db')\n    cursor = conn.cursor()\n    # Vulnerable: Directly concatenates input into SQL query\n    query = f\"SELECT * FROM users WHERE username = '{username}'\"\n    cursor.execute(query)\n    return cursor.fetchall()\n```\n\n### Solution: Use parameterized queries or input sanitization to prevent SQL injection.\n\n```python\nimport sqlite3\n\ndef get_user_info(username):\n    conn = sqlite3.connect('users.db')\n    cursor = conn.cursor()\n    # Safe: Parameterized query prevents injection\n    query = \"SELECT * FROM users WHERE username = ?\"\n    cursor.execute(query, (username,))\n    return cursor.fetchall()\n```\n"
},
{
  "characteristic": "Access Control(Least Privilege)",
  "description": "Implements least privilege by ensuring users only have the minimum access necessary for their roles. Enforced through RBAC (role-based access control) and MFA where required.",
  "abbreviation": "ACC-400",
  "example": "## SOC 2 requires logical access controls to enforce least privilege. Granting excessive permissions increases the risk of data breaches, insider threats, and unauthorized actions. Alignment with NIST 800-171 (3.1.1) ensures only necessary privileges are assigned.\n\n ### Issue: The function `delete_user` checks only if the user is logged in, but does not enforce role-based restrictions. This violates least privilege.\n\n ### ACC-100\n\n ### Severity: High\n\n```python\n# Vulnerable Example\n\ndef delete_user(target_user_id, current_user):\n    if current_user:  # Any logged-in user can delete accounts\n        delete_user_from_db(target_user_id)\n        print(\"User deleted successfully.\")\n    else:\n        print(\"Access denied.\")\n```\n\n### Solution: Enforce RBAC and check roles explicitly. Ensure admins only perform admin-level actions.\n\n```python\n# Production-Ready Example\n\nclass User:\n    def __init__(self, user_id, role):\n        self.user_id = user_id\n        self.role = role\n\n# RBAC mapping: defines allowed actions per role\nROLE_PERMISSIONS = {\n    \"admin\": {\"delete_user\", \"view_reports\", \"manage_roles\"},\n    \"manager\": {\"view_reports\"},\n    \"user\": set(),\n}\n\ndef has_permission(user: User, action: str) -> bool:\n    return action in ROLE_PERMISSIONS.get(user.role, set())\n\ndef delete_user(target_user_id: int, current_user: User):\n    if not has_permission(current_user, \"delete_user\"):\n        raise PermissionError(\"Access denied: Admin privileges required.\")\n    delete_user_from_db(target_user_id)\n    print(\"User deleted successfully.\")\n\n# Example usage\nadmin_user = User(user_id=1, role=\"admin\")\nnormal_user = User(user_id=2, role=\"user\")\n\n# Allowed\ndelete_user(target_user_id=3, current_user=admin_user)\n\n#  Raises PermissionError\n# delete_user(target_user_id=3, current_user=normal_user)\n```\n\n### Notes:\n- MFA (Multi-Factor Authentication) should be enforced at login.\n- Cloud environments (AWS IAM, Azure AD) should map to least privilege RBAC.\n"
},
{
  "characteristic": "Error Handling",
  "description": "Implements secure error handling that prevents leakage of sensitive system details while ensuring sufficient logging for monitoring and audit purposes.",
  "abbreviation": "ERR-500",
  "example": "## SOC 2 requires that errors be properly managed without exposing sensitive information to end users. Detailed error messages can leak database schema, credentials, or stack traces that attackers can exploit. Instead, errors should be logged securely for administrators while users receive only generic, non-sensitive messages.\n\n ### Issue: The API endpoint exposes stack traces and raw database errors directly to the client.\n\n ### ERR-100\n\n ### Severity: High\n\n```python\n# Vulnerable Example\nfrom flask import Flask, request\nimport sqlite3\n\napp = Flask(__name__)\n\n@app.route('/user')\ndef get_user():\n    username = request.args.get('username')\n    conn = sqlite3.connect('users.db')\n    cursor = conn.cursor()\n    try:\n        cursor.execute(\"SELECT * FROM users WHERE username=?\", (username,))\n        return {\"data\": cursor.fetchall()}\n    except Exception as e:\n        #  Bad: returning full error to client\n        return {\"error\": str(e)}, 500\n```\n\n### Solution: Provide generic error responses to users while logging details securely for system monitoring.\n\n```python\n# Production-Ready Example\nfrom flask import Flask, request, jsonify\nimport sqlite3, logging\n\napp = Flask(__name__)\n\n# Configure secure logging\nlogging.basicConfig(\n    filename=\"app_errors.log\",\n    level=logging.ERROR,\n    format=\"%(asctime)s %(levelname)s %(message)s\"\n)\n\n@app.route('/user')\ndef get_user():\n    username = request.args.get('username')\n    conn = sqlite3.connect('users.db')\n    cursor = conn.cursor()\n    try:\n        cursor.execute(\"SELECT * FROM users WHERE username=?\", (username,))\n        return {\"data\": cursor.fetchall()}\n    except Exception as e:\n        #  Log detailed error for admins\n        logging.error(\"Database error while fetching user '%s': %s\", username, e)\n        #  Return generic error message to client\n        return jsonify({\"error\": \"An internal error occurred. Please try again later.\"}), 500\n```\n\n### Notes:\n- End users only see a generic, non-sensitive error.\n- Detailed logs are securely stored for monitoring (aligns with SOC 2, PCI DSS logging).\n- OWASP A09 stresses avoiding sensitive information exposure in logs while ensuring auditability.\n"
},
{
  "characteristic": "Secure Authentication",
  "description": "Ensures strong authentication controls, including MFA, secure password storage, and compliance with NIST SP 800-63B. Protects against credential theft, brute force, and unauthorized access.",
  "abbreviation": "SEC-100",
  "example": "## SOC 2 requires strong identity verification mechanisms. Weak authentication (e.g., plaintext passwords, no MFA) can allow unauthorized access. AICPA TSC and NIST SP 800-63B emphasize secure password hashing, session management, and multi-factor authentication.\n\n ### Issue: The function `authenticate_user` compares passwords in plaintext and does not enforce MFA.\n\n ### SEC-100\n\n ### Severity: Critical\n\n```python\n#  Vulnerable Example: Plaintext password check, no MFA\ndef authenticate_user(username, password):\n    user = get_user_from_db(username)\n    if user and user['password'] == password:  # Insecure\n        return True\n    return False\n```\n\n### Solution: Use strong password hashing (bcrypt/argon2) and require MFA (e.g., TOTP-based codes).\n\n```python\n#  Production-Ready Example\nimport bcrypt, pyotp\n\n# Simulated DB user record\nuser_record = {\n    \"username\": \"alice\",\n    \"password_hash\": bcrypt.hashpw(b\"secure_password123\", bcrypt.gensalt()),\n    \"mfa_secret\": pyotp.random_base32()  # Assigned during user onboarding\n}\n\ndef authenticate_user(username: str, password: str, otp_code: str) -> bool:\n    # Fetch user from database\n    if username != user_record[\"username\"]:\n        return False\n\n    # Check password securely\n    if not bcrypt.checkpw(password.encode(), user_record[\"password_hash\"]):\n        return False\n\n    # Enforce MFA (TOTP)\n    totp = pyotp.TOTP(user_record[\"mfa_secret\"])\n    if not totp.verify(otp_code):\n        return False\n\n    return True\n\n# Example usage\npassword_attempt = \"secure_password123\"\notp_attempt = pyotp.TOTP(user_record[\"mfa_secret\"]).now()\n\nif authenticate_user(\"alice\", password_attempt, otp_attempt):\n    print(\" Authentication successful\")\nelse:\n    print(\"❌ Authentication failed\")\n```\n\n### Notes:\n- Passwords are hashed using bcrypt (SOC 2 + NIST SP 800-63B compliant).\n- MFA (TOTP/OTP apps like Google Authenticator) provides a second factor.\n- Session management & account lockout should also be enforced.\n- Cloud providers (AWS/Azure) require MFA for privileged accounts to meet SOC 2.\n"
},
{
  "characteristic": "Encryption (Data at Rest/Transit)",
  "description": "Ensures that sensitive data is encrypted at rest using strong algorithms (e.g., AES-256) and transmitted securely over TLS 1.2+ to maintain confidentiality and integrity.",
  "abbreviation": "ENC-100",
  "example": "## SOC 2 requires encryption to protect confidentiality and integrity of sensitive data. Weak or missing encryption can result in unauthorized disclosure or tampering. Auditors commonly reference FIPS 140-2 standards, recommending AES-256 for storage and TLS 1.2+ for transmission.\n\n ### Issue: Sensitive user data is stored in plaintext and sent over HTTP without encryption.\n\n ### ENC-100\n\n ### Severity: Critical\n\n```python\n#  Vulnerable Example\nimport sqlite3\nimport requests\n\n# Storing password in plaintext (insecure)\nconn = sqlite3.connect('users.db')\ncursor = conn.cursor()\ncursor.execute(\"INSERT INTO users (username, password) VALUES (?, ?)\", (\"alice\", \"plaintextpassword\"))\nconn.commit()\n\n# Sending sensitive data over HTTP (insecure)\nresponse = requests.post(\"http://example.com/api/login\", json={\"username\": \"alice\", \"password\": \"plaintextpassword\"})\nprint(response.text)\n```\n\n### Solution: Encrypt data at rest (AES-256) and enforce TLS 1.2+ for all communications.\n\n```python\n#  Production-Ready Example\nimport sqlite3, base64\nfrom cryptography.hazmat.primitives.ciphers import Cipher, algorithms, modes\nfrom cryptography.hazmat.backends import default_backend\nimport os, requests\n\n# AES-256 encryption for data at rest\nkey = os.urandom(32)  # 256-bit key\niv = os.urandom(16)\ncipher = Cipher(algorithms.AES(key), modes.CFB(iv), backend=default_backend())\n\n# Encrypt password before storing\nencryptor = cipher.encryptor()\npassword = b\"StrongPassword123!\"\nciphertext = encryptor.update(password) + encryptor.finalize()\nencoded_password = base64.b64encode(iv + ciphertext).decode()\n\n# Store encrypted password\nconn = sqlite3.connect('users_secure.db')\ncursor = conn.cursor()\ncursor.execute(\"CREATE TABLE IF NOT EXISTS users (username TEXT, password TEXT)\")\ncursor.execute(\"INSERT INTO users (username, password) VALUES (?, ?)\", (\"alice\", encoded_password))\nconn.commit()\n\n# Secure transmission using HTTPS (TLS 1.2+)\nresponse = requests.post(\n    \"https://example.com/api/login\",  #  HTTPS ensures TLS\n    json={\"username\": \"alice\", \"password\": encoded_password},\n    verify=True  # Verifies SSL certificates\n)\nprint(response.status_code)\n```\n\n### Notes:\n- Data at rest: AES-256 encryption (FIPS 140-2 validated algorithms).\n- Data in transit: TLS 1.2+ required for all endpoints.\n- Keys must be managed securely (AWS KMS, Azure Key Vault, or GCP KMS recommended).\n- Integrity ensured by avoiding plaintext and enforcing certificate validation.\n"
},
{
  "characteristic": "Audit Logging",
  "description": "Implements audit logging for security-relevant events (e.g., logins, privilege changes, data access). Ensures logs are captured, timestamped, and stored securely for audits and incident response.",
  "abbreviation": "AUD-100",
  "example": "## SOC 2 requires monitoring activities with comprehensive audit logs. Without logs, organizations cannot detect security incidents or support investigations. NIST SP 800-53 (AU-2, AU-3) and AICPA TSC emphasize logging login attempts, privilege changes, and sensitive data access.\n\n ### Issue: Application only prints actions to console and does not persist logs, making monitoring impossible.\n\n ### AUD-100\n\n ### Severity: High\n\n```python\n#  Vulnerable Example: No audit logging, just console output\n\ndef login(username):\n    print(f\"{username} logged in\")  # Not captured by monitoring systems\n    return True\n\nlogin(\"alice\")\n```\n\n### Solution: Use centralized audit logging with structured log entries.\n\n```python\n#  Production-Ready Example: Audit logging with standard logging module\nimport logging\nfrom logging.handlers import TimedRotatingFileHandler\n\n# Configure audit logger\nlogger = logging.getLogger(\"audit\")\nlogger.setLevel(logging.INFO)\n\n# Rotate logs daily, keep 30 days\nhandler = TimedRotatingFileHandler(\"audit.log\", when=\"midnight\", backupCount=30)\nformatter = logging.Formatter(\"%(asctime)s | user=%(user)s | action=%(action)s\")\nhandler.setFormatter(formatter)\nlogger.addHandler(handler)\n\n# Utility to log audit events\ndef log_event(user, action):\n    extra = {\"user\": user, \"action\": action}\n    logger.info(\"SECURITY_EVENT\", extra=extra)\n\n# Example usage\nlog_event(\"alice\", \"LOGIN_SUCCESS\")\nlog_event(\"alice\", \"VIEWED_CUSTOMER_RECORD:123\")\nlog_event(\"bob\", \"FAILED_LOGIN_ATTEMPT\")\n```\n\n### Notes:\n- Logs include **timestamp, user, and action** for auditability.\n- Stored in rotating files to prevent uncontrolled growth.\n- Can be shipped to SIEM / centralized monitoring (Splunk, ELK, CloudWatch, Azure Monitor).\n- Protect log file directory with strict permissions to prevent tampering.\n"
}
]

cwe= [
{
  "characteristic": "CWE-79: Cross-Site Scripting (XSS)",
  "description": "Occurs when user-controlled input is not properly neutralized before being included in web page content. Attackers can inject malicious scripts into web pages viewed by other users, leading to theft of cookies, session hijacking, redirection, or execution of arbitrary code in the user's browser.",
  "abbreviation": "XSS-100",
  "example": "## Cross-site Scripting (XSS) occurs when applications fail to sanitize or escape user input before rendering it in the browser.\n\n ### Issue: The application directly reflects user-supplied input into HTML output, allowing attackers to execute malicious scripts in the victim’s browser.\n\n ### XSS-100\n\n ### Severity: High\n\n```python\n# Insecure: Directly embedding user input into response\n@app.route('/search')\ndef search():\n    query = request.args.get('q')\n    return f\"<h1>Results for {query}</h1>\"  # Vulnerable to XSS\n```\n\n ### Solution: Properly escape/encode user input before rendering it in HTML, and use frameworks or libraries that handle escaping automatically.\n\n```python\n# Secure: Escape user input before rendering\nfrom markupsafe import escape\n\n@app.route('/search')\ndef search():\n    query = request.args.get('q')\n    return f\"<h1>Results for {escape(query)}</h1>\"\n\n# Alternatively, use templating engines (like Jinja2) which escape input by default.\n```"
},
{
  "characteristic": "CWE-787: Out-of-bounds Write",
  "description": "Occurs when a program writes data outside the bounds of allocated memory, either before the beginning or past the end of a buffer. This can corrupt memory, crash the application, or enable attackers to execute arbitrary code. Out-of-bounds writes are a critical memory corruption vulnerability often leading to severe security risks.",
  "abbreviation": "OOB-100",
  "example": "## Out-of-bounds Write occurs when data is written outside the allocated memory buffer.\n\n ### Issue: The program allocates a fixed-size buffer but writes more data than it can hold, overwriting adjacent memory.\n\n ### OOB-100\n\n ### Severity: Critical\n\n```c\n// Insecure: Buffer overflow due to unchecked write\nvoid copy_input(char *input) {\n    char buffer[10];\n    strcpy(buffer, input);  // No bounds check -> out-of-bounds write\n}\n```\n\n ### Solution: Validate buffer sizes and enforce bounds checking to prevent writes beyond allocated memory.\n\n```c\n// Secure: Use bounded copy to prevent overflow\nvoid copy_input(char *input) {\n    char buffer[10];\n    strncpy(buffer, input, sizeof(buffer) - 1);\n    buffer[sizeof(buffer) - 1] = '\\0';  // Ensure null termination\n}\n```"
},
    {
    "characteristic": "CWE-89: SQL Injection",
    "description": "Occurs when an application constructs SQL queries by directly concatenating user-controlled input without proper sanitization or parameterization. Attackers can manipulate the SQL query to alter its logic, retrieve unauthorized data, bypass authentication, or modify the database. SQL Injection is one of the most severe and common web vulnerabilities.",
    "abbreviation": "INJ-100",
    "example": "## SQL Injection occurs when user input is embedded directly into an SQL query without proper neutralization.\n\n ### Issue: The application concatenates user-supplied input into the SQL statement, allowing attackers to inject malicious SQL payloads.\n\n ### INJ-100\n\n ### Severity: Critical\n\n```python\n# Insecure: SQL query built using string concatenation\n@app.route('/login')\ndef login():\n    username = request.args.get('username')\n    password = request.args.get('password')\n    query = f\"SELECT * FROM users WHERE username='{username}' AND password='{password}'\"\n    result = db.execute(query)  # Vulnerable to SQL injection\n    return result\n```\n\n ### Solution: Use parameterized queries (prepared statements) to separate SQL logic from user input.\n\n```python\n# Secure: Use parameterized query to prevent SQL injection\n@app.route('/login')\ndef login():\n    username = request.args.get('username')\n    password = request.args.get('password')\n    query = \"SELECT * FROM users WHERE username = %s AND password = %s\"\n    result = db.execute(query, (username, password))\n    return result\n```"
    },
{
    "characteristic": "CWE-352: Cross-Site Request Forgery (CSRF)",
    "description": "The primary objective is to ensure the **Cross-Site Request Forgery (CSRF)** criteria of security in the code. Examine the following criteria: Implementing anti-CSRF tokens in web applications to prevent unauthorized actions on behalf of an authenticated user.",
    "abbreviation": "CSR-100",
    "example": "## Cross-Site Request Forgery (CSRF) is an attack that tricks a user into executing unwanted actions on a web application in which they are authenticated. It exploits the trust a web application has in a user's browser, allowing malicious commands to be transmitted without the user's consent, potentially leading to unauthorized transactions or data compromise.\n\n ### Issue: The application lacks CSRF protection on sensitive endpoints, allowing attackers to perform unauthorized actions on behalf of authenticated users.\n\n ### CSR-100\n\n ### Severity: High\n\n```python\nfrom flask import Flask, request\n\napp = Flask(__name__)\n\n@app.route('/transfer_funds', methods=['POST'])\ndef transfer_funds():\n    amount = request.form['amount']\n    recipient = request.form['recipient']\n    # Code to transfer funds\n    return 'Funds transferred successfully'\n```\n\n ### Solution: Implement CSRF protection by incorporating a unique token in forms that is validated on the server side. This ensures that requests are intentional and originated from the authenticated user.\n\n```python\nfrom flask import Flask, request, session, render_template_string\nimport secrets\n\napp = Flask(__name__)\napp.secret_key = 'your_secret_key'\n\n@app.route('/transfer_funds', methods=['GET', 'POST'])\ndef transfer_funds():\n    if request.method == 'GET':\n        csrf_token = secrets.token_hex(16)\n        session['csrf_token'] = csrf_token\n        return render_template_string('''\n            <form method=\"POST\" action=\"/transfer_funds\">\n                <input type=\"number\" name=\"amount\" required>\n                <input type=\"text\" name=\"recipient\" required>\n                <input type=\"hidden\" name=\"csrf_token\" value=\"{{ csrf_token }}\">\n                <input type=\"submit\" value=\"Transfer Funds\">\n            </form>\n        ''', csrf_token=csrf_token)\n    else:\n        submitted_token = request.form.get('csrf_token')\n        if not submitted_token or submitted_token != session.get('csrf_token'):\n            return 'Invalid CSRF token', 400\n        amount = request.form['amount']\n        recipient = request.form['recipient']\n        # Code to transfer funds\n        return 'Funds transferred successfully'\n```",
},
{
  "characteristic": "CWE-22: Path Traversal",
  "description": "Occurs when external input is used to construct file or directory paths without proper validation or neutralization of special elements (e.g., '../'). Attackers can exploit this to traverse directories and access files outside the intended restricted location, potentially exposing sensitive system files or application data.",
  "abbreviation": "PAT-100",
  "example": "## Path Traversal occurs when applications fail to properly sanitize user-supplied file paths.\n\n ### Issue: The application directly appends user input to a file path, allowing attackers to escape the restricted directory.\n\n ### PAT-100\n\n ### Severity: High\n\n```python\n# Insecure: User input directly used in file path\n@app.route('/read')\ndef read_file():\n    filename = request.args.get('file')\n    with open('/app/data/' + filename, 'r') as f:  # Vulnerable to ../ traversal\n        return f.read()\n```\n\n ### Solution: Validate and restrict file paths to allowed directories, and use safe path-joining functions.\n\n```python\n# Secure: Normalize and validate path before access\nimport os\nfrom werkzeug.utils import secure_filename\n\n@app.route('/read')\ndef read_file():\n    filename = secure_filename(request.args.get('file'))\n    base_path = '/app/data/'\n    full_path = os.path.abspath(os.path.join(base_path, filename))\n\n    if not full_path.startswith(os.path.abspath(base_path)):\n        return \"Unauthorized\", 403\n\n    with open(full_path, 'r') as f:\n        return f.read()\n```"
},
{
  "characteristic": "CWE-125: Out-of-bounds Read",
  "description": "Occurs when a program reads data outside the bounds of allocated memory, either before the beginning or past the end of a buffer. This can disclose sensitive information stored in adjacent memory, cause crashes, or lead to undefined behavior. Attackers may exploit out-of-bounds reads to leak secrets or bypass security mechanisms.",
  "abbreviation": "OOR-100",
  "example": "## Out-of-bounds Read occurs when memory is accessed outside the allocated range.\n\n ### Issue: The program reads more data than the buffer holds, allowing access to unintended memory.\n\n ### OOR-100\n\n ### Severity: High\n\n```c\n// Insecure: Reads beyond buffer boundary\nvoid print_data(char *input) {\n    char buffer[10];\n    strncpy(buffer, input, sizeof(buffer));\n    // No null terminator enforced, may read past buffer\n    printf(\"%s\", buffer);  // Potential out-of-bounds read\n}\n```\n\n ### Solution: Enforce strict bounds checking and ensure proper string termination.\n\n```c\n// Secure: Enforce safe copy with explicit termination\nvoid print_data(char *input) {\n    char buffer[10];\n    strncpy(buffer, input, sizeof(buffer) - 1);\n    buffer[sizeof(buffer) - 1] = '\\0';  // Ensure null termination\n    printf(\"%s\", buffer);\n}\n```"
},
{
  "characteristic": "CWE-78: Improper Neutralization of Special Elements used in an OS Command (OS Command Injection)",
  "description": "Occurs when an application builds operating system commands by concatenating user-controlled input without proper validation or sanitization. Attackers can inject malicious OS commands, enabling arbitrary command execution on the server, leading to data theft, privilege escalation, or full system compromise.",
  "abbreviation": "CMD-100",
  "example": "## OS Command Injection occurs when user input is embedded directly into system command execution.\n\n ### Issue: The application directly appends user input to an OS command string, allowing attackers to execute arbitrary system commands.\n\n ### CMD-100\n\n ### Severity: Critical\n\n```python\n# Insecure: Directly embedding user input into shell command\n@app.route('/ping')\ndef ping():\n    host = request.args.get('host')\n    os.system(f\"ping -c 1 {host}\")  # Vulnerable to command injection\n    return \"Ping executed\"\n```\n\n ### Solution: Avoid direct command execution with user input. Use parameterized APIs or sanitize inputs strictly.\n\n```python\n# Secure: Use subprocess with argument list (avoids shell interpretation)\nimport subprocess\n\n@app.route('/ping')\ndef ping():\n    host = request.args.get('host')\n    # Allowlist validation of host input (only alphanumeric and dots)\n    if not re.match(r'^[a-zA-Z0-9.]+$', host):\n        return \"Invalid input\", 400\n\n    result = subprocess.run([\"ping\", \"-c\", \"1\", host], capture_output=True, text=True)\n    return result.stdout\n```"
},
{
  "characteristic": "CWE-416: Use After Free",
  "description": "Occurs when a program continues to use a pointer after the memory it references has been freed. Since the memory may be reallocated for other purposes, dereferencing the freed pointer can cause crashes, data corruption, or enable attackers to execute arbitrary code. Use After Free is a critical memory corruption issue frequently exploited in real-world attacks.",
  "abbreviation": "UAF-100",
  "example": "## Use After Free occurs when code accesses memory that has already been freed.\n\n ### Issue: The program frees memory but later continues to use the dangling pointer, leading to undefined behavior.\n\n ### UAF-100\n\n ### Severity: Critical\n\n```c\n// Insecure: Using pointer after freeing memory\nvoid example() {\n    char *data = (char *)malloc(10);\n    strcpy(data, \"test\");\n    free(data);\n    printf(\"%s\", data);  // Use after free\n}\n```\n\n ### Solution: Set pointers to NULL after freeing and avoid dereferencing freed memory.\n\n```c\n// Secure: Prevent use after free by nullifying pointer\nvoid example() {\n    char *data = (char *)malloc(10);\n    strcpy(data, \"test\");\n    free(data);\n    data = NULL;  // Reset pointer to avoid dangling reference\n    if (data != NULL) {\n        printf(\"%s\", data);\n    }\n}\n```"
},
{
    "characteristic": "CWE-862: Missing Authorization",
    "description": "Occurs when the application completely omits authorization checks for an action or resource. This means that as long as a user is authenticated (or sometimes not even authenticated), they can directly access sensitive functionality without verifying if they have the required privileges. Do not raise issues for public resources unless their exposure clearly allows privilege escalation or access to sensitive operations.",
    "abbreviation": "AUT-100",
    "example": "## Missing Authorization happens when a system does not check if an authenticated user is allowed to perform an action, leading to unauthorized access to sensitive functions.\n\n ### Issue: Any authenticated user, regardless of role, can access the endpoint for deleting other users without a privilege check.\n\n ### AUT-100\n\n ### Severity: Critical\n\n```python\n# Insecure: No authorization check applied\n@app.route('/delete_user/<user_id>', methods=['POST'])\ndef delete_user(user_id):\n    # Directly executes critical action with no role/privilege validation\n    delete_user_from_db(user_id)\n    return \"User deleted!\"\n```\n\n ### Solution: Enforce explicit authorization checks for all sensitive actions. Only authorized roles (e.g., admin) should be allowed to perform critical operations.\n\n```python\n# Secure: Explicit authorization required\n@app.route('/delete_user/<user_id>', methods=['POST'])\ndef delete_user(user_id):\n    current_user = get_current_user()\n    if not current_user or not current_user.is_admin:\n        return \"Unauthorized\", 403\n\n    delete_user_from_db(user_id)\n    return \"User deleted!\"\n```"
    },
{
  "characteristic": "CWE-434: Unrestricted Upload of File with Dangerous Type",
  "description": "Occurs when an application allows users to upload files without proper validation of type, content, or handling. Attackers can exploit this to upload executable scripts, malware, or other dangerous files that can be executed or processed by the server or client, leading to remote code execution, data compromise, or system takeover.",
  "abbreviation": "UNF-100",
  "example": "## Unrestricted File Upload occurs when applications accept dangerous file types without validation.\n\n ### Issue: The application allows users to upload any file type, including executable scripts, which may later be executed on the server.\n\n ### UNF-100\n\n ### Severity: Critical\n\n```python\n# Insecure: Directly saving uploaded file without validation\n@app.route('/upload', methods=['POST'])\ndef upload():\n    file = request.files['file']\n    file.save('/uploads/' + file.filename)  # Dangerous: may allow .php/.exe upload\n    return \"File uploaded\"\n```\n\n ### Solution: Validate file types, enforce allowlists, and store uploads securely outside the web root.\n\n```python\n# Secure: Restrict upload types and sanitize filenames\nimport os\nfrom werkzeug.utils import secure_filename\n\nALLOWED_EXTENSIONS = {\"txt\", \"jpg\", \"png\", \"pdf\"}\n\n@app.route('/upload', methods=['POST'])\ndef upload():\n    file = request.files['file']\n    filename = secure_filename(file.filename)\n    ext = filename.rsplit('.', 1)[1].lower() if '.' in filename else ''\n\n    if ext not in ALLOWED_EXTENSIONS:\n        return \"File type not allowed\", 400\n\n    safe_path = os.path.join('/uploads', filename)\n    file.save(safe_path)\n    return \"File uploaded securely\"\n```"
},
{
  "characteristic": "CWE-94: Code Injection",
  "description": "Occurs when an application generates or executes code based on user-controlled input without proper neutralization. Attackers can manipulate the input to alter the logic, inject arbitrary code, or execute malicious payloads within the application’s runtime environment. This can lead to full system compromise, data theft, or remote code execution.",
  "abbreviation": "COD-100",
  "example": "## Code Injection occurs when applications build or execute code directly from external input.\n\n ### Issue: The application uses user-supplied input in dynamic code execution without validation, allowing attackers to run arbitrary commands.\n\n ### COD-100\n\n ### Severity: Critical\n\n```python\n# Insecure: Directly evaluating user input as code\n@app.route('/eval')\ndef eval_code():\n    user_input = request.args.get('code')\n    return str(eval(user_input))  # Vulnerable to code injection\n```\n\n ### Solution: Avoid dynamic code execution on untrusted input. Use safe alternatives such as controlled interpreters, allowlists, or restricted parsers.\n\n```python\n# Secure: Implement restricted evaluation or safe handling\nimport ast\n\n@app.route('/eval')\ndef eval_code():\n    user_input = request.args.get('code')\n    try:\n        # Safely parse only expressions, not arbitrary code\n        parsed = ast.literal_eval(user_input)\n        return str(parsed)\n    except Exception:\n        return \"Invalid input\", 400\n```"
},
{
    "characteristic": "CWE-20: Input Validation",
    "description": "Do not raise issues for basic input checks like **format**, **primitive type**, or **length** unless missing them causes crashes, buffer overflows, or memory corruption. if basic checks are already presents, then this check can be skipped.",
    "abbreviation": "INP-100",
    "example": '## Input Validation is the process of ensuring that user-provided data is correct, safe, and appropriate before processing it. It is significant in software security because improper input validation can lead to vulnerabilities like injection attacks, buffer overflows, and unauthorized access.\n\n ### Issue: The code accepts user input without validation, allowing potential execution of malicious code or access to unintended resources.\n\n ### INP-100\n\n ### Severity: High\n\n```python\nuser_input = input("Enter your username: ")\nprint("Welcome, " + user_input)\n```\n\n ### Solution: Implement input validation to ensure the input meets expected criteria, such as length and allowed characters, preventing injection attacks.\n\n```python\nimport re\n\nuser_input = input("Enter your username: ")\nif re.match("^[A-Za-z0-9_]{3,20}$", user_input):\n    print("Welcome, " + user_input)\nelse:\n    print("Invalid username. Please use 3-20 alphanumeric characters and underscores only.")\n```',
},
{
  "characteristic": "CWE-77: Command Injection",
  "description": "Occurs when an application constructs system commands using externally-influenced input without proper neutralization. Attackers can inject malicious command-line arguments or chain additional commands, leading to arbitrary command execution, data exposure, or full system compromise.",
  "abbreviation": "CMD-100",
  "example": "## Command Injection occurs when applications build operating system commands directly from untrusted input.\n\n ### Issue: The application concatenates user input into a shell command without sanitization, allowing attackers to execute arbitrary system commands.\n\n ### CMD-100\n\n ### Severity: Critical\n\n```python\n# Insecure: Using user input directly in a shell command\n@app.route('/ping')\ndef ping():\n    host = request.args.get('host')\n    result = os.system(f\"ping -c 1 {host}\")  # Vulnerable to command injection\n    return str(result)\n```\n\n ### Solution: Use safe APIs that avoid shell invocation. Validate and sanitize inputs with strict allowlists.\n\n```python\n# Secure: Use subprocess with controlled arguments\nimport subprocess\n\n@app.route('/ping')\ndef ping():\n    host = request.args.get('host')\n    if not host.isalnum():\n        return \"Invalid host\", 400\n    try:\n        result = subprocess.run([\"ping\", \"-c\", \"1\", host], capture_output=True, text=True, check=True)\n        return result.stdout\n    except subprocess.CalledProcessError:\n        return \"Ping failed\", 400\n```"
},
{
  "characteristic": "CWE-287: Improper Authentication",
  "description": "Occurs when an application does not sufficiently verify the identity of a user or process before granting access. Attackers can bypass authentication mechanisms, impersonate legitimate users, or gain unauthorized access to sensitive functions and data.",
  "abbreviation": "IMA-100",
  "example": "## Improper Authentication occurs when identity claims are not correctly verified.\n\n ### Issue: The application only checks for the presence of a username but does not verify the associated password, allowing attackers to impersonate any user.\n\n ### IMA-100\n\n ### Severity: High\n\n```python\n# Insecure: Authentication bypass due to missing password verification\n@app.route('/login', methods=['POST'])\ndef login():\n    username = request.form['username']\n    # Password check is missing!\n    session['user'] = username\n    return \"Logged in as \" + username\n```\n\n ### Solution: Always validate both username and password using secure, salted password hashing and enforce strong authentication mechanisms.\n\n```python\n# Secure: Proper authentication with password verification\nfrom werkzeug.security import check_password_hash\n\n@app.route('/login', methods=['POST'])\ndef login():\n    username = request.form['username']\n    password = request.form['password']\n    user = get_user_from_db(username)\n    if user and check_password_hash(user['password_hash'], password):\n        session['user'] = username\n        return \"Logged in as \" + username\n    return \"Invalid credentials\", 401\n```"
},
{
  "characteristic": "CWE-269: Improper Privilege Management",
  "description": "Occurs when an application fails to correctly assign, enforce, or restrict user privileges. Attackers may escalate their privileges, retain higher permissions longer than intended, or gain unauthorized control over restricted resources, leading to data compromise or full system takeover.",
  "abbreviation": "PRV-100",
  "example": "## Improper Privilege Management occurs when privilege levels are not securely enforced.\n\n ### Issue: The application allows privilege escalation by not validating role changes, enabling a regular user to grant themselves administrator rights.\n\n ### PRV-100\n\n ### Severity: Critical\n\n```python\n# Insecure: User can escalate privileges by manipulating input\n@app.route('/update_role', methods=['POST'])\ndef update_role():\n    username = request.form['username']\n    new_role = request.form['role']  # User-controlled input\n    db.execute(\"UPDATE users SET role=? WHERE username=?\", (new_role, username))\n    return \"Role updated\"\n```\n\n ### Solution: Enforce strict privilege boundaries and validate role assignments on the server side. Never allow privilege escalation based solely on user input.\n\n```python\n# Secure: Validate role changes with proper authorization checks\n@app.route('/update_role', methods=['POST'])\ndef update_role():\n    if 'admin' not in session.get('roles', []):\n        return \"Unauthorized\", 403\n    username = request.form['username']\n    new_role = request.form['role']\n    if new_role not in [\"user\", \"moderator\", \"admin\"]:\n        return \"Invalid role\", 400\n    db.execute(\"UPDATE users SET role=? WHERE username=?\", (new_role, username))\n    return \"Role updated\"\n```"
},
{
  "characteristic": "CWE-502: Deserialization of Untrusted Data",
  "description": "Occurs when an application deserializes data from untrusted sources without validation. Attackers can craft malicious payloads that execute arbitrary code, escalate privileges, or tamper with application logic during the deserialization process.",
  "abbreviation": "DES-100",
  "example": "## Deserialization of Untrusted Data occurs when unsafe deserialization allows malicious objects to be executed.\n\n ### Issue: The application directly deserializes user-provided data, enabling attackers to inject harmful payloads.\n\n ### DES-100\n\n ### Severity: Critical\n\n```python\n# Insecure: Directly deserializing untrusted input\nimport pickle\n@app.route('/load', methods=['POST'])\ndef load_data():\n    data = request.data\n    obj = pickle.loads(data)  # Vulnerable to code execution\n    return str(obj)\n```\n\n ### Solution: Avoid unsafe deserialization. Use safe formats like JSON and enforce strict validation.\n\n```python\n# Secure: Use safe serialization format\nimport json\n@app.route('/load', methods=['POST'])\ndef load_data():\n    try:\n        data = json.loads(request.data)\n        return str(data)\n    except Exception:\n        return \"Invalid input\", 400\n```"
},
{
  "characteristic": "CWE-200: Exposure of Sensitive Information to an Unauthorized Actor",
  "description": "Occurs when an application unintentionally exposes sensitive information (such as credentials, tokens, system details, or personal data) to actors who are not authorized to access it. This can lead to privacy violations, identity theft, or system compromise.",
  "abbreviation": "INF-100",
  "example": "## Exposure of Sensitive Information occurs when unauthorized actors gain access to confidential data.\n\n ### Issue: The application returns detailed error messages with stack traces, exposing sensitive system information to the client.\n\n ### INF-100\n\n ### Severity: High\n\n```python\n# Insecure: Exposing internal error details\n@app.route('/profile')\ndef profile():\n    try:\n        user_id = int(request.args.get('id'))\n        return get_user_profile(user_id)\n    except Exception as e:\n        return str(e)  # Sensitive error details leaked\n```\n\n ### Solution: Return generic error messages to users, log details securely on the server, and avoid exposing sensitive data through APIs, logs, or responses.\n\n```python\n# Secure: Provide generic error message, log sensitive details internally\nimport logging\n\n@app.route('/profile')\ndef profile():\n    try:\n        user_id = int(request.args.get('id'))\n        return get_user_profile(user_id)\n    except Exception as e:\n        logging.error(f\"Internal error: {e}\")\n        return \"An error occurred. Please try again later.\", 500\n```"
},
{
  "characteristic": "CWE-863: Incorrect Authorization",
  "description": "Occurs when an application implements an authorization check, but the logic is flawed or incomplete. Unlike missing authorization, the check exists but is incorrectly enforced — for example, validating the wrong attribute, trusting user-supplied input, or misapplying role logic. This can allow attackers to perform unauthorized actions or access restricted resources.",
  "abbreviation": "INC-100",
  "example": "## Incorrect Authorization occurs when an application enforces access control but with flawed logic.\n\n ### Issue: The application checks if a 'role' field in the request equals 'admin', but trusts user input instead of validating against server-side data.\n\n ### INC-100\n\n ### Severity: High\n\n```python\n# Insecure: Trusting user input for authorization\n@app.route('/admin/delete', methods=['POST'])\ndef delete_user():\n    if request.form.get('role') == 'admin':  # User-controlled field\n        user_id = request.form['user_id']\n        db.execute(\"DELETE FROM users WHERE id=?\", (user_id,))\n        return \"User deleted\"\n    return \"Unauthorized\", 403\n```\n\n ### Solution: Always enforce authorization based on trusted server-side session or identity data, not user-supplied fields.\n\n```python\n# Secure: Use server-side session roles for authorization\n@app.route('/admin/delete', methods=['POST'])\ndef delete_user():\n    if 'admin' not in session.get('roles', []):\n        return \"Unauthorized\", 403\n    user_id = request.form['user_id']\n    db.execute(\"DELETE FROM users WHERE id=?\", (user_id,))\n    return \"User deleted\"\n```"
},
{
  "characteristic": "CWE-918: Server-Side Request Forgery (SSRF)",
  "description": "Occurs when an application fetches remote resources based on user-supplied input without validating the destination. Attackers can exploit this to make the server send malicious requests to internal systems, cloud metadata endpoints, or other protected resources, potentially leading to data exposure or remote compromise.",
  "abbreviation": "SSR-100",
  "example": "## Server-Side Request Forgery occurs when user input controls outbound requests without proper validation.\n\n ### Issue: The application accepts a user-provided URL and fetches it directly, allowing attackers to reach internal services or sensitive endpoints.\n\n ### SSR-100\n\n ### Severity: Critical\n\n```python\n# Insecure: Fetching arbitrary URLs from user input\nimport requests\n\n@app.route('/fetch')\ndef fetch():\n    url = request.args.get('url')\n    response = requests.get(url)  # Vulnerable to SSRF\n    return response.text\n```\n\n ### Solution: Enforce strict allowlists of permitted domains or endpoints, block access to internal/private networks, and sanitize user inputs.\n\n```python\n# Secure: Validate destination before making requests\nimport requests\nfrom urllib.parse import urlparse\n\nALLOWED_DOMAINS = [\"example.com\", \"api.safe-service.com\"]\n\n@app.route('/fetch')\ndef fetch():\n    url = request.args.get('url')\n    parsed = urlparse(url)\n    if parsed.hostname not in ALLOWED_DOMAINS:\n        return \"Unauthorized request\", 403\n    response = requests.get(url)\n    return response.text\n```"
},
{
  "characteristic": "CWE-119: Improper Restriction of Operations within the Bounds of a Memory Buffer",
  "description": "Occurs when an application reads from or writes to memory outside the boundaries of a buffer. This can lead to memory corruption, crashes, exposure of sensitive information, or execution of arbitrary code.",
  "abbreviation": "BUF-100",
  "example": "## Buffer Overflow occurs when operations exceed the allocated memory boundaries.\n\n ### Issue: The application copies user input into a fixed-size buffer without checking length, allowing overwriting of adjacent memory.\n\n ### BUF-100\n\n ### Severity: Critical\n\n```c\n// Insecure: No boundary check\n#include <stdio.h>\n#include <string.h>\n\nvoid copy_input(char *input) {\n    char buffer[10];\n    strcpy(buffer, input);  // Vulnerable to buffer overflow\n    printf(\"Input copied: %s\\n\", buffer);\n}\n```\n\n ### Solution: Always perform bounds checking or use safe functions that enforce limits.\n\n```c\n// Secure: Use safe functions with length checks\n#include <stdio.h>\n#include <string.h>\n\nvoid copy_input(char *input) {\n    char buffer[10];\n    strncpy(buffer, input, sizeof(buffer) - 1);\n    buffer[sizeof(buffer) - 1] = '\\0';  // Ensure null-termination\n    printf(\"Input copied: %s\\n\", buffer);\n}\n```"
},
{
  "characteristic": "CWE-476: NULL Pointer Dereference",
  "description": "Occurs when an application dereferences a pointer that is expected to be valid but is actually NULL. This can lead to application crashes, denial of service, or unexpected behavior.",
  "abbreviation": "NUL-100",
  "example": "## NULL Pointer Dereference occurs when the application assumes a pointer is valid without checking for NULL.\n\n ### Issue: Dereferencing a NULL pointer leads to crashes or undefined behavior.\n\n ### NUL-100\n\n ### Severity: High\n\n```c\n// Insecure: Dereferencing without NULL check\n#include <stdio.h>\n\nvoid print_length(char *str) {\n    printf(\"Length: %zu\\n\", strlen(str));  // May crash if str is NULL\n}\n```\n\n ### Solution: Always validate pointers before dereferencing.\n\n```c\n// Secure: Check for NULL before use\n#include <stdio.h>\n\nvoid print_length(char *str) {\n    if (str == NULL) {\n        printf(\"Invalid input\\n\");\n        return;\n    }\n    printf(\"Length: %zu\\n\", strlen(str));\n}\n```"
},
{
  "characteristic": "CWE-798: Use of Hard-coded Credentials",
  "description": "Occurs when an application includes hard-coded credentials (such as usernames, passwords, API keys, or cryptographic keys) directly in the source code. Attackers can extract these credentials from binaries, configuration files, or repositories, leading to unauthorized access and potential system compromise.",
  "abbreviation": "CRD-100",
  "example": "## Use of Hard-coded Credentials occurs when authentication secrets are embedded in source code.\n\n ### Issue: The application uses a hard-coded database password, allowing attackers to gain access if they obtain the code or binary.\n\n ### CRD-100\n\n ### Severity: Critical\n\n```python\n# Insecure: Hard-coded database password\nimport mysql.connector\n\nconn = mysql.connector.connect(\n    host=\"localhost\",\n    user=\"admin\",\n    password=\"P@ssw0rd123\",  # Hard-coded credential\n    database=\"app_db\"\n)\n```\n\n ### Solution: Store credentials securely in environment variables, secret managers, or configuration files with restricted access.\n\n```python\n# Secure: Load credentials from environment variables\nimport os\nimport mysql.connector\n\nconn = mysql.connector.connect(\n    host=\"localhost\",\n    user=os.getenv(\"DB_USER\"),\n    password=os.getenv(\"DB_PASSWORD\"),\n    database=\"app_db\"\n)\n```"
},
{
  "characteristic": "CWE-190: Integer Overflow or Wraparound",
  "description": "Occurs when an arithmetic operation results in a value that exceeds the storage capacity of the integer type, causing it to wrap around or overflow. This can lead to unexpected behavior, logic errors, security vulnerabilities, or program crashes.",
  "abbreviation": "INT-100",
  "example": "## Integer Overflow occurs when arithmetic operations exceed the maximum value of the integer type.\n\n ### Issue: Incrementing an integer without bounds checking can wrap around to a negative value.\n\n ### INT-100\n\n ### Severity: High\n\n```c\n// Insecure: Integer overflow without validation\n#include <stdio.h>\n#include <limits.h>\n\nvoid increment(int value) {\n    int result = value + 1;\n    printf(\"Result: %d\\n\", result);  // May overflow if value is INT_MAX\n}\n```\n\n ### Solution: Validate values before arithmetic operations or use data types that can safely store larger numbers.\n\n```c\n// Secure: Check for overflow before incrementing\n#include <stdio.h>\n#include <limits.h>\n\nvoid increment(int value) {\n    if (value >= INT_MAX) {\n        printf(\"Overflow detected\\n\");\n        return;\n    }\n    int result = value + 1;\n    printf(\"Result: %d\\n\", result);\n}\n```"
},
{
  "characteristic": "CWE-400: Uncontrolled Resource Consumption",
  "description": "Occurs when an application fails to properly limit or manage the use of system resources such as memory, CPU, disk space, or network bandwidth. Attackers can exploit this to perform denial-of-service attacks, degrade performance, or exhaust system resources.",
  "abbreviation": "RES-100",
  "example": "## Uncontrolled Resource Consumption occurs when resource usage is not properly limited.\n\n ### Issue: The application allows clients to submit large numbers of requests or huge data payloads without throttling, causing resource exhaustion.\n\n ### RES-100\n\n ### Severity: High\n\n```python\n# Insecure: No limit on input processing\n@app.route('/process', methods=['POST'])\ndef process_data():\n    data = request.data\n    # Processing large input without limits\n    result = do_heavy_computation(data)\n    return str(result)\n```\n\n ### Solution: Implement resource limits, input validation, request throttling, and monitoring.\n\n```python\n# Secure: Limit input size and apply throttling\nfrom flask_limiter import Limiter\nfrom flask import request\n\nlimiter = Limiter(app, default_limits=[\"10 per minute\"])\n\n@app.route('/process', methods=['POST'])\n@limiter.limit(\"5 per minute\")\ndef process_data():\n    if len(request.data) > 1024 * 1024:  # Limit to 1MB\n        return \"Input too large\", 413\n    result = do_heavy_computation(request.data)\n    return str(result)\n```"
},
{
  "characteristic": "CWE-306: Missing Authentication for Critical Function",
  "description": "Occurs when an application allows access to sensitive or resource-intensive functionality without verifying the identity of the actor. Attackers can exploit this to perform unauthorized actions, manipulate critical operations, or consume excessive resources.",
  "abbreviation": "MIS-100",
  "example": "## Missing Authentication occurs when critical functions are accessible without verifying user identity.\n\n ### Issue: The application exposes an endpoint that deletes all user records without requiring authentication.\n\n ### MIS-100\n\n ### Severity: Critical\n\n```python\n# Insecure: Critical function accessible without authentication\n@app.route('/delete_all_users', methods=['POST'])\ndef delete_all_users():\n    db.execute(\"DELETE FROM users\")  # No authentication\n    return \"All users deleted\"\n```\n\n ### Solution: Require strong authentication and enforce access control on all critical functions.\n\n```python\n# Secure: Authenticate user before allowing critical operation\n@app.route('/delete_all_users', methods=['POST'])\ndef delete_all_users():\n    if 'admin' not in session.get('roles', []):\n        return \"Unauthorized\", 403\n    db.execute(\"DELETE FROM users\")\n    return \"All users deleted\"\n```"
}
]

cwe_mitre=[
{
  "characteristic": "CWE-79: Cross-Site Scripting (XSS)",
  "description": "Occurs when user-controlled input is not properly neutralized before being included in web page content. Attackers can inject malicious scripts into web pages viewed by other users, leading to theft of cookies, session hijacking, redirection, or execution of arbitrary code in the user's browser.",
  "abbreviation": "XSS-100",
  "example": "# CWE-79: Cross-Site Scripting (XSS)\n\n## Cross-site Scripting (XSS) occurs when applications fail to sanitize or escape user input before rendering it in the browser.\n\n### Issue: The application directly reflects user-supplied input into HTML output, allowing attackers to execute malicious scripts in the victim’s browser. Additional CWE Mapping: CWE-20 Improper Input Validation, OWASP Mapping: A03:2021 – Injection (includes XSS)\n\n### XSS-100\n\n### Severity: High\n\n```python\n# Insecure: Directly embedding user input into response\n@app.route('/search')\ndef search():\n    query = request.args.get('q')\n    return f\"<h1>Results for {query}</h1>\"  # Vulnerable to XSS\n```\n\n### Solution: Properly escape/encode user input before rendering it in HTML, and use frameworks or libraries that handle escaping automatically.\n\n```python\n# Secure: Escape user input before rendering\nfrom markupsafe import escape\n\n@app.route('/search')\ndef search():\n    query = request.args.get('q')\n    return f\"<h1>Results for {escape(query)}</h1>\"\n\n# Alternatively, use templating engines (like Jinja2) which escape input by default.\n```",
  "owasp_mapping": "A03:2021 – Injection (includes XSS)",
  "cwe_mapping": "CWE-20 Improper Input Validation"
},
{
  "characteristic": "CWE-787: Out-of-bounds Write",
  "description": "Occurs when a program writes data outside the bounds of allocated memory, either before the beginning or past the end of a buffer. This can corrupt memory, crash the application, or enable attackers to execute arbitrary code. Out-of-bounds writes are a critical memory corruption vulnerability often leading to severe security risks.",
  "abbreviation": "OOB-100",
  "example": "# CWE-787: Out-of-bounds Write\n\n## Out-of-bounds Write occurs when data is written outside the allocated memory buffer.\n\n### Issue: The program allocates a fixed-size buffer but writes more data than it can hold, overwriting adjacent memory. Additional CWE Mapping: CWE-119 Memory Buffer Errors, OWASP Mapping: A05:2021 – Security Misconfiguration\n\n### OOB-100\n\n### Severity: Critical\n\n```c\n// Insecure: Buffer overflow due to unchecked write\nvoid copy_input(char *input) {\n    char buffer[10];\n    strcpy(buffer, input);  // No bounds check -> out-of-bounds write\n}\n```\n\n### Solution: Validate buffer sizes and enforce bounds checking to prevent writes beyond allocated memory.\n\n```c\n// Secure: Use bounded copy to prevent overflow\nvoid copy_input(char *input) {\n    char buffer[10];\n    strncpy(buffer, input, sizeof(buffer) - 1);\n    buffer[sizeof(buffer) - 1] = '\\0';  // Ensure null termination\n}\n```",
  "cwe_mapping": "CWE-119 Memory Buffer Errors",
  "owasp_mapping": "A05:2021 – Security Misconfiguration"
},
{
  "characteristic": "CWE-89: SQL Injection",
  "description": "Occurs when an application constructs SQL queries by directly concatenating user-controlled input without proper sanitization or parameterization. Attackers can manipulate the SQL query to alter its logic, retrieve unauthorized data, bypass authentication, or modify the database. SQL Injection is one of the most severe and common web vulnerabilities.",
  "abbreviation": "INJ-100",
  "example": "# CWE-89: SQL Injection\n\n## SQL Injection occurs when user input is embedded directly into an SQL query without proper neutralization.\n\n### Issue: The application concatenates user-supplied input into the SQL statement, allowing attackers to inject malicious SQL payloads. Additional CWE Mapping: CWE-20 Improper Input Validation, OWASP Mapping: A03:2021 – Injection\n\n### INJ-100\n\n### Severity: Critical\n\n```python\n# Insecure: SQL query built using string concatenation\n@app.route('/login')\ndef login():\n    username = request.args.get('username')\n    password = request.args.get('password')\n    query = f\"SELECT * FROM users WHERE username='{username}' AND password='{password}'\"\n    result = db.execute(query)  # Vulnerable to SQL injection\n    return result\n```\n\n### Solution: Use parameterized queries (prepared statements) to separate SQL logic from user input.\n\n```python\n# Secure: Use parameterized query to prevent SQL injection\n@app.route('/login')\ndef login():\n    username = request.args.get('username')\n    password = request.args.get('password')\n    query = \"SELECT * FROM users WHERE username = %s AND password = %s\"\n    result = db.execute(query, (username, password))\n    return result\n```",
  "owasp_mapping": "A03:2021 – Injection",
  "cwe_mapping": "CWE-20 Improper Input Validation"
},
{
  "characteristic": "CWE-352: Cross-Site Request Forgery (CSRF)",
  "description": "The primary objective is to ensure the **Cross-Site Request Forgery (CSRF)** criteria of security in the code. Examine the following criteria: Implementing anti-CSRF tokens in web applications to prevent unauthorized actions on behalf of an authenticated user.",
  "abbreviation": "CSR-100",
  "example": "# CWE-352: Cross-Site Request Forgery (CSRF)\n\n## Cross-Site Request Forgery (CSRF) is an attack that tricks a user into executing unwanted actions on a web application in which they are authenticated. It exploits the trust a web application has in a user's browser, allowing malicious commands to be transmitted without the user's consent, potentially leading to unauthorized transactions or data compromise.\n\n### Issue: The application lacks CSRF protection on sensitive endpoints, allowing attackers to perform unauthorized actions on behalf of authenticated users. Additional CWE Mapping: CWE-284 Improper Access Control, OWASP Mapping: A05:2021 – Security Misconfiguration\n\n### CSR-100\n\n### Severity: High\n\n```python\nfrom flask import Flask, request\n\napp = Flask(__name__)\n\n@app.route('/transfer_funds', methods=['POST'])\ndef transfer_funds():\n    amount = request.form['amount']\n    recipient = request.form['recipient']\n    # Code to transfer funds\n    return 'Funds transferred successfully'\n```\n\n### Solution: Implement CSRF protection by incorporating a unique token in forms that is validated on the server side. This ensures that requests are intentional and originated from the authenticated user.\n\n```python\nfrom flask import Flask, request, session, render_template_string\nimport secrets\n\napp = Flask(__name__)\napp.secret_key = 'your_secret_key'\n\n@app.route('/transfer_funds', methods=['GET', 'POST'])\ndef transfer_funds():\n    if request.method == 'GET':\n        csrf_token = secrets.token_hex(16)\n        session['csrf_token'] = csrf_token\n        return render_template_string('''\n            <form method=\"POST\" action=\"/transfer_funds\">\n                <input type=\"number\" name=\"amount\" required>\n                <input type=\"text\" name=\"recipient\" required>\n                <input type=\"hidden\" name=\"csrf_token\" value=\"{{ csrf_token }}\">\n                <input type=\"submit\" value=\"Transfer Funds\">\n            </form>\n        ''', csrf_token=csrf_token)\n    else:\n        submitted_token = request.form.get('csrf_token')\n        if not submitted_token or submitted_token != session.get('csrf_token'):\n            return 'Invalid CSRF token', 400\n        amount = request.form['amount']\n        recipient = request.form['recipient']\n        # Code to transfer funds\n        return 'Funds transferred successfully'\n```",
  "owasp_mapping": "A05:2021 – Security Misconfiguration",
  "cwe_mapping": "CWE-284 Improper Access Control"
},
{
  "characteristic": "CWE-22: Path Traversal",
  "description": "Occurs when external input is used to construct file or directory paths without proper validation or neutralization of special elements (e.g., '../'). Attackers can exploit this to traverse directories and access files outside the intended restricted location, potentially exposing sensitive system files or application data.",
  "abbreviation": "PAT-100",
  "example": "# CWE-22: Path Traversal\n\n## Path Traversal occurs when applications fail to properly sanitize user-supplied file paths.\n\n### Issue: The application directly appends user input to a file path, allowing attackers to escape the restricted directory. Additional CWE Mapping: CWE-20 Improper Input Validation, OWASP Mapping: A05:2021 – Security Misconfiguration, A01:2021 – Broken Access Control\n\n### PAT-100\n\n### Severity: High\n\n```python\n# Insecure: User input directly used in file path\n@app.route('/read')\ndef read_file():\n    filename = request.args.get('file')\n    with open('/app/data/' + filename, 'r') as f:  # Vulnerable to ../ traversal\n        return f.read()\n```\n\n### Solution: Validate and restrict file paths to allowed directories, and use safe path-joining functions.\n\n```python\n# Secure: Normalize and validate path before access\nimport os\nfrom werkzeug.utils import secure_filename\n\n@app.route('/read')\ndef read_file():\n    filename = secure_filename(request.args.get('file'))\n    base_path = '/app/data/'\n    full_path = os.path.abspath(os.path.join(base_path, filename))\n\n    if not full_path.startswith(os.path.abspath(base_path)):\n        return \"Unauthorized\", 403\n\n    with open(full_path, 'r') as f:\n        return f.read()\n```",
  "cwe_mapping": "CWE-20 Improper Input Validation",
  "owasp_mapping": "A05:2021 – Security Misconfiguration, A01:2021 – Broken Access Control"
},
{
  "characteristic": "CWE-125: Out-of-bounds Read",
  "description": "Occurs when a program reads data outside the bounds of allocated memory, either before the beginning or past the end of a buffer. This can disclose sensitive information stored in adjacent memory, cause crashes, or lead to undefined behavior. Attackers may exploit out-of-bounds reads to leak secrets or bypass security mechanisms.",
  "abbreviation": "OOR-100",
  "example": "# CWE-125: Out-of-bounds Read\n\n## Out-of-bounds Read occurs when memory is accessed outside the allocated range.\n\n### Issue: The program reads more data than the buffer holds, allowing access to unintended memory. Additional CWE Mapping: CWE-119 Memory Buffer Errors, OWASP Mapping: Not directly in OWASP Top 10 – aligns with OWASP Proactive Controls (C2: Input Validation & Memory Safety)\n\n### OOR-100\n\n### Severity: High\n\n```c\n// Insecure: Reads beyond buffer boundary\nvoid print_data(char *input) {\n    char buffer[10];\n    strncpy(buffer, input, sizeof(buffer));\n    // No null terminator enforced, may read past buffer\n    printf(\"%s\", buffer);  // Potential out-of-bounds read\n}\n```\n\n### Solution: Enforce strict bounds checking and ensure proper string termination.\n\n```c\n// Secure: Enforce safe copy with explicit termination\nvoid print_data(char *input) {\n    char buffer[10];\n    strncpy(buffer, input, sizeof(buffer) - 1);\n    buffer[sizeof(buffer) - 1] = '\\0';  // Ensure null termination\n    printf(\"%s\", buffer);\n}\n```",
  "owasp_mapping": "Not directly in OWASP Top 10 – aligns with OWASP Proactive Controls (C2: Input Validation & Memory Safety)",
  "cwe_mapping": "CWE-119 Memory Buffer Errors"
},
{
  "characteristic": "CWE-78: OS Command Injection",
  "description": "Occurs when an application constructs system commands using externally-influenced input without proper neutralization. Attackers can inject malicious command-line arguments or chain additional commands, leading to arbitrary command execution, data exposure, or full system compromise. Safe fixes avoid invoking a shell, validate inputs (prefer allowlists), use argument lists for subprocesses, add timeouts and error handling, and — when possible — avoid calling system binaries entirely.",
  "abbreviation": "OMD-100",
  "example": "# CWE-78: OS Command Injection\n\n## Command Injection occurs when applications build operating system commands directly from untrusted input.\n\n### Issue: The application concatenates user input into a shell command without sanitization, allowing attackers to execute arbitrary system commands. Additional CWE Mapping: CWE-20 Improper Input Validation, OWASP Mapping: A03:2021 – Injection\n\n### CMD-100\n\n### Severity: Critical\n\n```python\n# Insecure — vulnerable to command injection\nfrom flask import Flask, request\nimport os\n\napp = Flask(__name__)\n\n@app.route('/ping')\ndef ping():\n    host = request.args.get('host', '')\n    # attacker could pass: \"example.com; rm -rf /\" or \"8.8.8.8 && shutdown now\"\n    exit_code = os.system(f\"ping -c 1 {host}\")  # uses shell -> injection\n    return f\"exit code: {exit_code}\"\n```\n\n### Why this is vulnerable\n- `os.system` with an f-string or concatenation uses a shell and allows command chaining or argument injection.\n- Returning the raw exit code is misleading and exposes no useful safe output.\n\n### Solution: avoid the shell, validate hosts, add timeouts and handle errors. Prefer allowlists or higher-level libraries instead of calling system binaries.\n\n```python\n# Safer: validate host properly and call subprocess with argument list (no shell)\nfrom flask import Flask, request\nimport subprocess\nimport re\nimport ipaddress\n\napp = Flask(__name__)\n\n# Simple hostname regex: labels separated by dots, labels 1-63 chars, overall <=253.\nHOSTNAME_RE = re.compile(r\"^(?=.{1,253}$)([a-zA-Z0-9-]{1,63}\\.)*[a-zA-Z0-9-]{1,63}$\")\n\ndef is_valid_host(host: str) -> bool:\n    if not host or len(host) > 253:\n        return False\n    # Accept valid IPv4/IPv6\n    try:\n        ipaddress.ip_address(host)\n        return True\n    except ValueError:\n        pass\n    # Accept hostnames matching pattern and no label starts/ends with '-'\n    if HOSTNAME_RE.match(host):\n        for label in host.split('.'):\n            if label.startswith('-') or label.endswith('-'):\n                return False\n        return True\n    return False\n\n@app.route('/ping')\ndef ping():\n    host = request.args.get('host', '').strip()\n    if not is_valid_host(host):\n        return \"Invalid host\", 400\n\n    # Use argument list to avoid invoking a shell; this prevents injection\n    cmd = [\"ping\", \"-c\", \"1\", host]  # on Windows use ['ping','-n','1', host]\n    try:\n        result = subprocess.run(\n            cmd,\n            capture_output=True,\n            text=True,\n            check=True,\n            timeout=5\n        )\n        return result.stdout or \"Ping succeeded\"\n    except subprocess.CalledProcessError:\n        return \"Ping failed\", 502\n    except subprocess.TimeoutExpired:\n        return \"Ping timed out\", 504\n    except FileNotFoundError:\n        return \"Ping command not available on server\", 500\n```\n\n### Additional recommendations\n- If possible, avoid shelling out entirely — use a library (e.g. `pythonping`) or perform an application-level TCP/HTTP check depending on requirements.\n- Use an allowlist of permissible hosts when the app only needs to contact a known set.\n- Rate-limit and log attempts to reduce abuse.\n- Consider platform differences (Windows vs Unix ping flags) when implementing a cross-platform solution.",
  "owasp_mapping": "A03:2021 – Injection",
  "cwe_mapping": "CWE-20 Improper Input Validation"
},
{
  "characteristic": "CWE-416: Use After Free",
  "description": "Occurs when a program continues to use a pointer after the memory it references has been freed. Since the memory may be reallocated for other purposes, dereferencing the freed pointer can cause crashes, data corruption, or enable attackers to execute arbitrary code. Use After Free is a critical memory corruption issue frequently exploited in real-world attacks.",
  "abbreviation": "UAF-100",
  "example": "# CWE-416: Use After Free\n\n## Use After Free occurs when code accesses memory that has already been freed.\n\n### Issue: The program frees memory but later continues to use the dangling pointer, leading to undefined behavior. Additional CWE Mapping: CWE-119 Memory Buffer Errors, OWASP Mapping: A04:2021 — Insecure Design\n\n### UAF-100\n\n### Severity: Critical\n\n```c\n// Insecure: Using pointer after freeing memory\nvoid example() {\n    char *data = (char *)malloc(10);\n    strcpy(data, \"test\");\n    free(data);\n    printf(\"%s\", data);  // Use after free\n}\n```\n\n### Solution: Set pointers to NULL after freeing and avoid dereferencing freed memory.\n\n```c\n// Secure: Prevent use after free by nullifying pointer\nvoid example() {\n    char *data = (char *)malloc(10);\n    strcpy(data, \"test\");\n    free(data);\n    data = NULL;  // Reset pointer to avoid dangling reference\n    if (data != NULL) {\n        printf(\"%s\", data);\n    }\n}\n```",
  "owasp_mapping": "A04:2021 — Insecure Design",
  "cwe_mapping": "CWE-119 Memory Buffer Errors"
},
{
  "characteristic": "CWE-862: Missing Authorization",
  "description": "Occurs when the application completely omits authorization checks for an action or resource. This means that as long as a user is authenticated (or sometimes not even authenticated), they can directly access sensitive functionality without verifying if they have the required privileges. Do not raise issues for public resources unless their exposure clearly allows privilege escalation or access to sensitive operations.",
  "abbreviation": "AUT-100",
  "example": "# CWE-862: Missing Authorization\n\n## Missing Authorization happens when a system does not check if an authenticated user is allowed to perform an action, leading to unauthorized access to sensitive functions.\n\n### Issue: Any authenticated user, regardless of role, can access the endpoint for deleting other users without a privilege check. Additional CWE Mapping: CWE-284 Improper Access Control, OWASP Mapping: A01:2021 — Broken Access Control\n\n### AUT-100\n\n### Severity: Critical\n\n```python\n# Insecure: No authorization check applied\n@app.route('/delete_user/<user_id>', methods=['POST'])\ndef delete_user(user_id):\n    # Directly executes critical action with no role/privilege validation\n    delete_user_from_db(user_id)\n    return \"User deleted!\"\n```\n\n### Solution: Enforce explicit authorization checks for all sensitive actions. Only authorized roles (e.g., admin) should be allowed to perform critical operations.\n\n```python\n# Secure: Explicit authorization required\n@app.route('/delete_user/<user_id>', methods=['POST'])\ndef delete_user(user_id):\n    current_user = get_current_user()\n    if not current_user or not current_user.is_admin:\n        return \"Unauthorized\", 403\n\n    delete_user_from_db(user_id)\n    return \"User deleted!\"\n```",
  "owasp_mapping": "A01:2021 — Broken Access Control",
  "cwe_mapping": "CWE-284 Improper Access Control"
},
{
  "characteristic": "CWE-434: Unrestricted Upload of File with Dangerous Type",
  "description": "Occurs when an application allows users to upload files without proper validation of type, content, or handling. Attackers can exploit this to upload executable scripts, malware, or other dangerous files that can be executed or processed by the server or client, leading to remote code execution, data compromise, or system takeover.",
  "abbreviation": "UNF-100",
  "example": "# CWE-434: Unrestricted Upload of File with Dangerous Type\n\n## Unrestricted File Upload occurs when applications accept dangerous file types without validation.\n\n### Issue: The application allows users to upload any file type, including executable scripts, which may later be executed on the server. Additional CWE Mapping: CWE-20 Improper Input Validation, OWASP Mapping: A05:2021 — Security Misconfiguration\n\n### UNF-100\n\n### Severity: Critical\n\n```python\n# Insecure: Directly saving uploaded file without validation\n@app.route('/upload', methods=['POST'])\ndef upload():\n    file = request.files['file']\n    file.save('/uploads/' + file.filename)  # Dangerous: may allow .php/.exe upload\n    return \"File uploaded\"\n```\n\n### Solution: Validate file types, enforce allowlists, and store uploads securely outside the web root.\n\n```python\n# Secure: Restrict upload types and sanitize filenames\nimport os\nfrom werkzeug.utils import secure_filename\n\nALLOWED_EXTENSIONS = {\"txt\", \"jpg\", \"png\", \"pdf\"}\n\n@app.route('/upload', methods=['POST'])\ndef upload():\n    file = request.files['file']\n    filename = secure_filename(file.filename)\n    ext = filename.rsplit('.', 1)[1].lower() if '.' in filename else ''\n\n    if ext not in ALLOWED_EXTENSIONS:\n        return \"File type not allowed\", 400\n\n    safe_path = os.path.join('/uploads', filename)\n    file.save(safe_path)\n    return \"File uploaded securely\"\n```",
  "owasp_mapping": "A05:2021 — Security Misconfiguration",
  "cwe_mapping": "CWE-20 Improper Input Validation"
},
{
  "characteristic": "CWE-798: Use of Hard-coded Credentials",
  "description": "Occurs when an application includes hard-coded credentials (such as usernames, passwords, API keys, or cryptographic keys) directly in the source code. Attackers can extract these credentials from binaries, configuration files, or repositories, leading to unauthorized access and potential system compromise.",
  "abbreviation": "CRD-100",
  "example": "# CWE-798: Use of Hard-coded Credentials\n\n## Use of Hard-coded Credentials occurs when authentication secrets are embedded in source code.\n\n### Issue: The application uses a hard-coded database password, which violates secure coding practices. This exposure can lead to unauthorized access and system compromise. Additional CWE Mapping: CWE-259 Use of Hard-coded Password. OWASP Mapping: A07:2021 — Identification and Authentication Failures.\n\n### CRD-100\n\n### Severity: Critical\n\n```python\n# Insecure: Hard-coded database password\nimport mysql.connector\n\nconn = mysql.connector.connect(\n    host=\"localhost\",\n    user=\"admin\",\n    password=\"P@ssw0rd123\",  # Hard-coded credential\n    database=\"app_db\"\n)\n```\n\n### Solution: Store credentials securely in environment variables, secret managers, or configuration files with restricted access.\n\n```python\n# Secure: Load credentials from environment variables\nimport os\nimport mysql.connector\n\nconn = mysql.connector.connect(\n    host=\"localhost\",\n    user=os.getenv(\"DB_USER\"),\n    password=os.getenv(\"DB_PASSWORD\"),\n    database=\"app_db\"\n)\n```",
  "owasp_mapping": "A07:2021 — Identification and Authentication Failures",
  "cwe_mapping": "CWE-259 Use of Hard-coded Password"
},
]

cwe_kev=[
{
  "characteristic": "CWE-787: Out-of-bounds Write",
  "description": "Occurs when a program writes data outside the bounds of allocated memory, either before the beginning or past the end of a buffer. This can corrupt memory, crash the application, or enable attackers to execute arbitrary code. Out-of-bounds writes are a critical memory corruption vulnerability often leading to severe security risks.",
  "abbreviation": "OOB-100",
  "example": "# CWE-787: Out-of-bounds Write\n\n## Out-of-bounds Write occurs when data is written outside the allocated memory buffer.\n\n### Issue: The program allocates a fixed-size buffer but writes more data than it can hold, overwriting adjacent memory. Additional CWE Mapping: CWE-119 Improper Restriction of Operations within the Bounds of a Memory Buffer, OWASP Mapping: A01:2021 — Broken Access Control\n\n### OOB-100\n\n### Severity: Critical\n\n```c\n// Insecure: Buffer overflow due to unchecked write\nvoid copy_input(char *input) {\n    char buffer[10];\n    strcpy(buffer, input);  // No bounds check -> out-of-bounds write\n}\n```\n\n### Solution: Validate buffer sizes and enforce bounds checking to prevent writes beyond allocated memory.\n\n```c\n// Secure: Use bounded copy to prevent overflow\nvoid copy_input(char *input) {\n    char buffer[10];\n    strncpy(buffer, input, sizeof(buffer) - 1);\n    buffer[sizeof(buffer) - 1] = '\\0';  // Ensure null termination\n}\n```",
  "owasp_mapping": "A01:2021 — Broken Access Control",
  "cwe_mapping": "CWE-119 Improper Restriction of Operations within the Bounds of a Memory Buffer"
},
{
  "characteristic": "CWE-78: OS Command Injection",
  "description": "Occurs when an application constructs system commands using externally-influenced input without proper neutralization. Attackers can inject malicious command-line arguments or chain additional commands, leading to arbitrary command execution, data exposure, or full system compromise. Safe fixes avoid invoking a shell, validate inputs (prefer allowlists), use argument lists for subprocesses, add timeouts and error handling, and — when possible — avoid calling system binaries entirely.",
  "abbreviation": "OMD-100",
  "example": "# CWE-78: OS Command Injection\n\n## Command Injection occurs when applications build operating system commands directly from untrusted input.\n\n### Issue: The application concatenates user input into a shell command without sanitization, allowing attackers to execute arbitrary system commands. Additional CWE Mapping: CWE-94 Improper Control of Generation of Code, OWASP Mapping: A01:2021 — Broken Access Control\n\n### OMD-100\n\n### Severity: Critical\n\n```python\n# Insecure — vulnerable to command injection\nfrom flask import Flask, request\nimport os\n\napp = Flask(__name__)\n\n@app.route('/ping')\ndef ping():\n    host = request.args.get('host', '')\n    # attacker could pass: \"example.com; rm -rf /\" or \"8.8.8.8 && shutdown now\"\n    exit_code = os.system(f\"ping -c 1 {host}\")  # uses shell -> injection\n    return f\"exit code: {exit_code}\"\n```\n\n### Solution: avoid the shell, validate hosts, add timeouts and handle errors. Prefer allowlists or higher-level libraries instead of calling system binaries.\n\n```python\n# Safer: validate host properly and call subprocess with argument list (no shell)\nfrom flask import Flask, request\nimport subprocess\nimport re\nimport ipaddress\n\napp = Flask(__name__)\nHOSTNAME_RE = re.compile(r\"^(?=.{1,253}$)([a-zA-Z0-9-]{1,63}\\.)*[a-zA-Z0-9-]{1,63}$\")\n\ndef is_valid_host(host: str) -> bool:\n    if not host or len(host) > 253:\n        return False\n    try:\n        ipaddress.ip_address(host)\n        return True\n    except ValueError:\n        pass\n    if HOSTNAME_RE.match(host):\n        for label in host.split('.'):\n            if label.startswith('-') or label.endswith('-'):\n                return False\n        return True\n    return False\n\n@app.route('/ping')\ndef ping():\n    host = request.args.get('host', '').strip()\n    if not is_valid_host(host):\n        return \"Invalid host\", 400\n\n    cmd = [\"ping\", \"-c\", \"1\", host]  # on Windows use ['ping','-n','1', host]\n    try:\n        result = subprocess.run(cmd, capture_output=True, text=True, check=True, timeout=5)\n        return result.stdout or \"Ping succeeded\"\n    except subprocess.CalledProcessError:\n        return \"Ping failed\", 502\n    except subprocess.TimeoutExpired:\n        return \"Ping timed out\", 504\n    except FileNotFoundError:\n        return \"Ping command not available on server\", 500\n```",
  "owasp_mapping": "A01:2021 — Broken Access Control",
  "cwe_mapping": "CWE-94 Improper Control of Generation of Code"
},
{
  "characteristic": "CWE-94: Code Injection",
  "description": "Occurs when an application generates or executes code based on user-controlled input without proper neutralization. Attackers can manipulate the input to alter the logic, inject arbitrary code, or execute malicious payloads within the application’s runtime environment. This can lead to full system compromise, data theft, or remote code execution.",
  "abbreviation": "COD-100",
  "example": "# CWE-94: Code Injection\n\n## Code Injection occurs when applications build or execute code directly from external input.\n\n### Issue: The application uses user-supplied input in dynamic code execution without validation, allowing attackers to run arbitrary commands. Additional CWE Mapping: CWE-78 OS Command Injection, OWASP Mapping: A01:2021 — Broken Access Control\n\n### COD-100\n\n### Severity: Critical\n\n```python\n# Insecure: Directly evaluating user input as code\n@app.route('/eval')\ndef eval_code():\n    user_input = request.args.get('code')\n    return str(eval(user_input))  # Vulnerable to code injection\n```\n\n### Solution: Avoid dynamic code execution on untrusted input. Use safe alternatives such as controlled interpreters, allowlists, or restricted parsers.\n\n```python\n# Secure: Implement restricted evaluation or safe handling\nimport ast\n\n@app.route('/eval')\ndef eval_code():\n    user_input = request.args.get('code')\n    try:\n        # Safely parse only expressions, not arbitrary code\n        parsed = ast.literal_eval(user_input)\n        return str(parsed)\n    except Exception:\n        return \"Invalid input\", 400\n```",
  "owasp_mapping": "A01:2021 — Broken Access Control",
  "cwe_mapping": "CWE-78 OS Command Injection"
},
{
  "characteristic": "CWE-502: Deserialization of Untrusted Data",
  "description": "Occurs when an application deserializes data from untrusted sources without validation. Attackers can craft malicious payloads that execute arbitrary code, escalate privileges, or tamper with application logic during the deserialization process.",
  "abbreviation": "DES-100",
  "example": "# CWE-502: Deserialization of Untrusted Data\n\n## Deserialization of Untrusted Data occurs when unsafe deserialization allows malicious objects to be executed.\n\n### Issue: The application directly deserializes user-provided data, enabling attackers to inject harmful payloads. Additional CWE Mapping: CWE-94 Code Injection, OWASP Mapping: A05:2021 — Security Misconfiguration\n\n### DES-100\n\n### Severity: Critical\n\n```python\n# Insecure: Directly deserializing untrusted input\nimport pickle\n@app.route('/load', methods=['POST'])\ndef load_data():\n    data = request.data\n    obj = pickle.loads(data)  # Vulnerable to code execution\n    return str(obj)\n```\n\n### Solution: Avoid unsafe deserialization. Use safe formats like JSON and enforce strict validation.\n\n```python\n# Secure: Use safe serialization format\nimport json\n@app.route('/load', methods=['POST'])\ndef load_data():\n    try:\n        data = json.loads(request.data)\n        return str(data)\n    except Exception:\n        return \"Invalid input\", 400\n```",
  "owasp_mapping": "A05:2021 — Security Misconfiguration",
  "cwe_mapping": "CWE-94 Code Injection"
},
{
  "characteristic": "CWE-22: Path Traversal",
  "description": "Occurs when external input is used to construct file or directory paths without proper validation or neutralization of special elements (e.g., '../'). Attackers can exploit this to traverse directories and access files outside the intended restricted location, potentially exposing sensitive system files or application data.",
  "abbreviation": "PAT-100",
  "example": "# CWE-22: Path Traversal\n\n## Path Traversal occurs when applications fail to properly sanitize user-supplied file paths.\n\n### Issue: The application directly appends user input to a file path, allowing attackers to escape the restricted directory. Additional CWE Mapping: CWE-20 Improper Input Validation, OWASP Mapping: A05:2021 – Security Misconfiguration, A01:2021 – Broken Access Control\n\n### PAT-100\n\n### Severity: High\n\n```python\n# Insecure: User input directly used in file path\n@app.route('/read')\ndef read_file():\n    filename = request.args.get('file')\n    with open('/app/data/' + filename, 'r') as f:  # Vulnerable to ../ traversal\n        return f.read()\n```\n\n### Solution: Validate and restrict file paths to allowed directories, and use safe path-joining functions.\n\n```python\n# Secure: Normalize and validate path before access\nimport os\nfrom werkzeug.utils import secure_filename\n\n@app.route('/read')\ndef read_file():\n    filename = secure_filename(request.args.get('file'))\n    base_path = '/app/data/'\n    full_path = os.path.abspath(os.path.join(base_path, filename))\n\n    if not full_path.startswith(os.path.abspath(base_path)):\n        return \"Unauthorized\", 403\n\n    with open(full_path, 'r') as f:\n        return f.read()\n```",
  "cwe_mapping": "CWE-20 Improper Input Validation",
  "owasp_mapping": "A05:2021 – Security Misconfiguration, A01:2021 – Broken Access Control"
},
{
  "characteristic": "CWE-306: Missing Authentication for Critical Function",
  "description": "Occurs when an application allows access to sensitive or resource-intensive functionality without verifying the identity of the actor. Attackers can exploit this to perform unauthorized actions, manipulate critical operations, or consume excessive resources.",
  "abbreviation": "MIS-100",
  "example": "# CWE-306: Missing Authentication for Critical Function\n\n## Missing Authentication occurs when critical functions are accessible without verifying user identity.\n\n### Issue: The application exposes an endpoint that deletes all user records without requiring authentication. Additional CWE Mapping: CWE-285 — Improper Authorization, OWASP Mapping: A01:2021 — Broken Access Control\n\n### MIS-100\n\n### Severity: Critical\n\n```python\n# Insecure: Critical function accessible without authentication\n@app.route('/delete_all_users', methods=['POST'])\ndef delete_all_users():\n    db.execute(\"DELETE FROM users\")  # No authentication\n    return \"All users deleted\"\n```\n\n### Solution: Require strong authentication and enforce access control on all critical functions.\n\n```python\n# Secure: Authenticate user before allowing critical operation\n@app.route('/delete_all_users', methods=['POST'])\ndef delete_all_users():\n    if 'admin' not in session.get('roles', []):\n        return \"Unauthorized\", 403\n    db.execute(\"DELETE FROM users\")\n    return \"All users deleted\"\n```",
  "owasp_mapping": "A01:2021 — Broken Access Control",
  "cwe_mapping": "CWE-285 — Improper Authorization"
},
{
  "characteristic": "CWE-89: SQL Injection",
  "description": "Occurs when an application constructs SQL queries by directly concatenating user-controlled input without proper sanitization or parameterization. Attackers can manipulate the SQL query to alter its logic, retrieve unauthorized data, bypass authentication, or modify the database. SQL Injection is one of the most severe and common web vulnerabilities.",
  "abbreviation": "INJ-100",
  "example": "# CWE-89: SQL Injection\n\n## SQL Injection occurs when user input is embedded directly into an SQL query without proper neutralization.\n\n### Issue: The application concatenates user-supplied input into the SQL statement, allowing attackers to inject malicious SQL payloads. Additional CWE Mapping: CWE-89 SQL Injection, OWASP Mapping: A03:2021 — Injection\n\n### INJ-100\n\n### Severity: Critical\n\n```python\n# Insecure: SQL query built using string concatenation\n@app.route('/login')\ndef login():\n    username = request.args.get('username')\n    password = request.args.get('password')\n    query = f\"SELECT * FROM users WHERE username='{username}' AND password='{password}'\"\n    result = db.execute(query)  # Vulnerable to SQL injection\n    return result\n```\n\n### Solution: Use parameterized queries (prepared statements) to separate SQL logic from user input.\n\n```python\n# Secure: Use parameterized query to prevent SQL injection\n@app.route('/login')\ndef login():\n    username = request.args.get('username')\n    password = request.args.get('password')\n    query = \"SELECT * FROM users WHERE username = %s AND password = %s\"\n    result = db.execute(query, (username, password))\n    return result\n```",
  "owasp_mapping": "A03:2021 — Injection",
  "cwe_mapping": "CWE-89 SQL Injection"
},
{
  "characteristic": "CWE-416: Use After Free",
  "description": "Occurs when a program continues to use a pointer after the memory it references has been freed. Since the memory may be reallocated for other purposes, dereferencing the freed pointer can cause crashes, data corruption, or enable attackers to execute arbitrary code. Use After Free is a critical memory corruption issue frequently exploited in real-world attacks.",
  "abbreviation": "UAF-100",
  "example": "# CWE-416: Use After Free\n\n## Use After Free occurs when code accesses memory that has already been freed.\n\n### Issue: The program frees memory but later continues to use the dangling pointer, leading to undefined behavior. Additional CWE Mapping: CWE-119 Improper Restriction of Operations within the Bounds of a Memory Buffer, OWASP Mapping: A09:2021 — Security Logging and Monitoring Failures\n\n### UAF-100\n\n### Severity: Critical\n\n```c\n// Insecure: Using pointer after freeing memory\nvoid example() {\n    char *data = (char *)malloc(10);\n    strcpy(data, \"test\");\n    free(data);\n    printf(\"%s\", data);  // Use after free\n}\n```\n\n### Solution: Set pointers to NULL after freeing and avoid dereferencing freed memory.\n\n```c\n// Secure: Prevent use after free by nullifying pointer\nvoid example() {\n    char *data = (char *)malloc(10);\n    strcpy(data, \"test\");\n    free(data);\n    data = NULL;  // Reset pointer to avoid dangling reference\n    if (data != NULL) {\n        printf(\"%s\", data);\n    }\n}\n```",
  "owasp_mapping": "A09:2021 — Security Logging and Monitoring Failures",
  "cwe_mapping": "CWE-119 Improper Restriction of Operations within the Bounds of a Memory Buffer"
},
{
  "characteristic": "CWE-77: Command Injection",
  "description": "Occurs when an application constructs system commands using externally-influenced input without proper neutralization. Attackers can inject malicious command-line arguments or chain additional commands, leading to arbitrary command execution, data exposure, or full system compromise.",
  "abbreviation": "CMD-100",
  "example": "# CWE-77: Command Injection\n\n## Command Injection occurs when applications build operating system commands directly from untrusted input.\n\n### Issue: The application concatenates user input into a shell command without sanitization, allowing attackers to execute arbitrary system commands. Additional CWE Mapping: CWE-78 OS Command Injection, OWASP Mapping: A01:2021 — Broken Access Control\n\n### CMD-100\n\n### Severity: Critical\n\n```python\n# Insecure: Using user input directly in a shell command\n@app.route('/ping')\ndef ping():\n    host = request.args.get('host')\n    result = os.system(f\"ping -c 1 {host}\")  # Vulnerable to command injection\n    return str(result)\n```\n\n### Solution: Use safe APIs that avoid shell invocation. Validate and sanitize inputs with strict allowlists.\n\n```python\n# Secure: Use subprocess with controlled arguments\nimport subprocess\n\n@app.route('/ping')\ndef ping():\n    host = request.args.get('host')\n    if not host.isalnum():\n        return \"Invalid host\", 400\n    try:\n        result = subprocess.run([\"ping\", \"-c\", \"1\", host], capture_output=True, text=True, check=True)\n        return result.stdout\n    except subprocess.CalledProcessError:\n        return \"Ping failed\", 400\n```",
  "owasp_mapping": "A01:2021 — Broken Access Control",
  "cwe_mapping": "CWE-78 OS Command Injection"
},
{
  "characteristic": "CWE-798: Use of Hard-coded Credentials",
  "description": "Occurs when an application includes hard-coded credentials (such as usernames, passwords, API keys, or cryptographic keys) directly in the source code. Attackers can extract these credentials from binaries, configuration files, or repositories, leading to unauthorized access and potential system compromise.",
  "abbreviation": "CRD-100",
  "example": "# CWE-798: Use of Hard-coded Credentials\n\n## Use of Hard-coded Credentials occurs when authentication secrets are embedded in source code.\n\n### Issue: The application uses a hard-coded database password, which violates secure coding practices. This exposure can lead to unauthorized access and system compromise. Additional CWE Mapping: CWE-259 Use of Hard-coded Password. OWASP Mapping: A07:2021 — Identification and Authentication Failures.\n\n### CRD-100\n\n### Severity: Critical\n\n```python\n# Insecure: Hard-coded database password\nimport mysql.connector\n\nconn = mysql.connector.connect(\n    host=\"localhost\",\n    user=\"admin\",\n    password=\"P@ssw0rd123\",  # Hard-coded credential\n    database=\"app_db\"\n)\n```\n\n### Solution: Store credentials securely in environment variables, secret managers, or configuration files with restricted access.\n\n```python\n# Secure: Load credentials from environment variables\nimport os\nimport mysql.connector\n\nconn = mysql.connector.connect(\n    host=\"localhost\",\n    user=os.getenv(\"DB_USER\"),\n    password=os.getenv(\"DB_PASSWORD\"),\n    database=\"app_db\"\n)\n```",
  "owasp_mapping": "A07:2021 — Identification and Authentication Failures",
  "cwe_mapping": "CWE-259 Use of Hard-coded Password"
}
]

Factors = {
    "maintainability": maintainability,
    "performance": performance,
    "reliability": reliability,
    "resilience": resilience,
    "scalability": scalability,
    "unittestability": unittestability,
    "security": security,
    "power_analysis": power_analysis,
    "power_analysis_small": power_analysis_small,
    "owasp": owasp,
    "soc2": soc2,
    "cwe": cwe,
    "cwe_mitre":cwe_mitre,
    "cwe_kev":cwe_kev
}

char_factor_map = {
    "maintainability": "Cyclomatic Complexity, Modularity, Simplicity",
    "performance": "Algorithm Efficiency, Caching Mechanism, Concurrency and Parallelism, Data Structures, Database Queries, Function Calls, Hardware Utilization, I/O Operations, Memory Leaks, Network Calls, Redundant Code and Loops, Space Complexity, Time Complexity",
    "reliability": "Error Handling and Exception Management, Robustness to External Changes, Consistency in Data and State Management, Resource Management, Testing and Coverage, Failover and Redundancy Mechanisms, Boundary Conditions, Concurrency Control, Recovery and Error Reporting, Dependency Management, Analyzability, Modifiability, Testability",
    "resilience": "Exception Handling, Retry Mechanism, Circuit Breaker Pattern, Timeouts, Fallback Strategies, Rate Limiting, Bulkheading, Configuration Management, Dependency Isolation, State Management, Connection Management, Monitoring and Logging, Thread Safety, Data Resilience, Immutable Infrastructure, Health Checks, Disaster Recovery, Network Resilience, Resource Management",
    "unittestability": "Boundary Conditions, Dynamism, Environment Dependencies, State Management, Input/Output, Tight Coupling, Errors and Exceptions, Size and Complexity, Modularity and Structure, Interfaces and Abstractions and Dependency Injection, External Dependencies, Static Methods and Singletons",
    "scalability": "Multi-threading, Asynchronous programming, Non-blocking I/O, Efficient data structures, Algorithm optimization, Memory management, File handles, Invalidation strategy, Third-party Services and Libraries , State Management",
    "security": "Authentication, Authorization, Session Management, Cryptography, Cross-Site Scripting (XSS), Cross-Site Request Forgery (CSRF), File and Resource Access, Code Injection, Input Validation",
    "power_analysis": "Modularity, Resource Utilization, Exception Handling,  Monitoring and Logging, Dependency Injection, Code Injection, Input Validation",
    "power_analysis_small": "Comprehensive Power Analysis",
    "owasp": "OWASP A01:2021 Broken Access Control, OWASP A02:2021 Cryptographic Failures, OWASP A03:2021 Injection, OWASP A04:2021 Insecure Design, OWASP A05:2021 Security Misconfiguration, OWASP A07:2021 Identification and Authentication Failures, OWASP A08:2021 Software and Data Integrity Failures, OWASP A09:2021 Security Logging and Monitoring Failures, OWASP A10:2021 Server-Side Request Forgery (SSRF)",
    "cwe": "CWE-79: Cross-site Scripting, CWE-787: Out-of-bounds Write, CWE-89: SQL Injection, CWE-352: Cross-Site Request Forgery (CSRF), CWE-22: Path Traversal, CWE-125: Out-of-bounds Read, CWE-78: OS Command Injection, CWE-287: Improper Authentication, CWE-306: Missing Authentication for Critical Function, CWE-400: Uncontrolled Resource Consumption, CWE-416: Use After Free, CWE-476: NULL Pointer Dereference, CWE-502: Deserialization of Untrusted Data, CWE-200: Exposure of Sensitive Information to an Unauthorized Actor, CWE-862: Missing Authorization, CWE-863: Incorrect Authorization, CWE-94: Code Injection, CWE-77: Command Injection, CWE-190: Integer Overflow or Wraparound, CWE-798: Use of Hard-coded Credentials, CWE-119: Improper Restriction of Operations within the Bounds of a Memory Buffer, CWE-434: Unrestricted Upload of File with Dangerous Type, CWE-20: Input Validation, CWE-269: Improper Privilege Management, CWE-918: Server-Side Request Forgery (SSRF)",
    "soc2": "Input Validation, Output Encoding, Access Control (Least Privilege) , Error Handling , Secure Authentication, Encryption (Data at Rest/Transit), Audit Logging",
    "cwe_mitre": "CWE-434: Unrestricted Upload of File with Dangerous Type, CWE-862: Missing Authorization, CWE-416: Use After Free, CWE-77: Command Injection, CWE-125: Out-of-bounds Read, CWE-352: Cross-Site Request Forgery (CSRF), CWE-89: SQL Injection, CWE-787: Out-of-bounds Write, CWE-79: Cross-Site Scripting (XSS), CWE-798: Use of Hard-coded Credentials, CWE-22: Path Traversal",
    "cwe_kev": "CWE-77: Command Injection, CWE-416: Use After Free, CWE-89: SQL Injection, CWE-306: Missing Authentication for Critical Function, CWE-22: Path Traversal, CWE-502: Deserialization of Untrusted Data, CWE-94: Code Injection, CWE-78: OS Command Injection, CWE-787: Out-of-bounds Write, CWE-798: Use of Hard-coded Credentials"
}

OWASP_GROUPS = {
    "Data & Access Control Risks": [
        "OWASP A01:2021 Broken Access Control",
        "OWASP A02:2021 Cryptographic Failures",
    ],
    "Input Handling & Authentication Risks": [
        "OWASP A03:2021 Injection",
        "OWASP A07:2021 Identification and Authentication Failures",
        "OWASP A05:2021 Security Misconfiguration",
    ],
    "Application Design & Integrity Risks": [
        "OWASP A04:2021 Insecure Design",
        "OWASP A08:2021 Software and Data Integrity Failures",
        "OWASP A09:2021 Security Logging and Monitoring Failures",
        "OWASP A10:2021 Server-Side Request Forgery (SSRF)",
    ],
}


CWE_GROUPS = {
    "Input Validation & Injection Risks": [
        "CWE-79: Cross-site Scripting",
        "CWE-89: SQL Injection",
        "CWE-78: OS Command Injection",
        "CWE-77: Command Injection",
        "CWE-94: Code Injection",
        "CWE-20: Improper Input Validation",
    ],
    "Access Control & Authentication Risks": [
        "CWE-287: Improper Authentication",
        "CWE-306: Missing Authentication for Critical Function",
        "CWE-862: Missing Authorization",
        "CWE-863: Incorrect Authorization",
        "CWE-798: Use of Hard-coded Credentials",
        "CWE-269: Improper Privilege Management",
    ],
    "Memory & Buffer Handling Risks": [
        "CWE-787: Out-of-bounds Write",
        "CWE-125: Out-of-bounds Read",
        "CWE-416: Use After Free",
        "CWE-476: NULL Pointer Dereference",
        "CWE-119: Improper Restriction of Operations within the Bounds of a Memory Buffer",
        "CWE-190: Integer Overflow or Wraparound",
    ],
    "Resource & Availability Risks": [
        "CWE-400: Uncontrolled Resource Consumption",
    ],
    "Data Integrity & File Handling Risks": [
        "CWE-352: Cross-Site Request Forgery (CSRF)",
        "CWE-22: Path Traversal",
        "CWE-502: Deserialization of Untrusted Data",
        "CWE-434: Unrestricted Upload of File with Dangerous Type",
        "CWE-918: Server-Side Request Forgery (SSRF)",
        "CWE-200: Exposure of Sensitive Information to an Unauthorized Actor",
    ],
}


CWE_GROUPS = {
    "Input Validation & Injection Risks": [
        "CWE-79: Cross-site Scripting",
        "CWE-89: SQL Injection",
        "CWE-78: OS Command Injection",
        "CWE-77: Command Injection",
        "CWE-94: Code Injection",
        "CWE-20: Improper Input Validation",
    ],
    "Access Control & Authentication Risks": [
        "CWE-287: Improper Authentication",
        "CWE-306: Missing Authentication for Critical Function",
        "CWE-862: Missing Authorization",
        "CWE-863: Incorrect Authorization",
        "CWE-798: Use of Hard-coded Credentials",
        "CWE-269: Improper Privilege Management",
    ],
    "Memory & Buffer Handling Risks": [
        "CWE-787: Out-of-bounds Write",
        "CWE-125: Out-of-bounds Read",
        "CWE-416: Use After Free",
        "CWE-476: NULL Pointer Dereference",
        "CWE-119: Improper Restriction of Operations within the Bounds of a Memory Buffer",
        "CWE-190: Integer Overflow or Wraparound",
    ],
    "Resource & Availability Risks": [
        "CWE-400: Uncontrolled Resource Consumption",
    ],
    "Data Integrity & File Handling Risks": [
        "CWE-352: Cross-Site Request Forgery (CSRF)",
        "CWE-22: Path Traversal",
        "CWE-502: Deserialization of Untrusted Data",
        "CWE-434: Unrestricted Upload of File with Dangerous Type",
        "CWE-918: Server-Side Request Forgery (SSRF)",
        "CWE-200: Exposure of Sensitive Information to an Unauthorized Actor",
    ],
}

